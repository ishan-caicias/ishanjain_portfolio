# Optional: NASA Astronomy Picture of the Day API key.
# If set, the Hubble modal can optionally fetch from NASA APOD.
# Leave empty to use only the local Hubble dataset.
# Get a key at https://api.nasa.gov/
PUBLIC_NASA_API_KEY=
