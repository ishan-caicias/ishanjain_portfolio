# TR-116 — Documentation-drift correction pass: PF-09/PF-10 status, gap-analysis confirmation, review-doc banners

**Date:** 2026-09-02
**Nature:** Documentation-only. No source, test, or config file was changed; no build/test/e2e/budget
gate applies to this pass. Follows this repo's corrections-are-additive convention throughout —
every wrong claim found below is struck through or superseded in place with a dated forward
pointer, never silently rewritten.

## Why this exists

CLAUDE.md treats documentation drift as a defect, not untidiness, and names PF-10's own C4.3 row as
exactly this class of problem once found. This pass investigates and corrects four instances of
drift located across `docs/delivery-plan/` and `docs/analysis/`, all traced back to status text that
was accurate when written and went stale as later work shipped without updating it.

## What was found and corrected

### 1 — PF-10 C4.3 marked "not started"; it had shipped

`docs/delivery-plan/PF-10-gaia-dataset-realism.md`'s C4 sub-phase table listed C4.3 ("Rotation,
self-shadowing, tier/budget gating from real-device data") as **not started**. Verified against
source that all three items are code-complete:

| Item               | Shipped                                        | Evidence                                                                                                                                                                            |
| ------------------ | ---------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Rotation           | 2026-07-21, commit `34ae8ac`                   | `src/lib/planet-sphere.ts:221-276` (`ROTATION_PERIOD_S`, `rotationAngle()`), wired in `src/lib/babylon-engine.ts:4169-4175`, unit-tested `tests/unit/planet-sphere.test.ts:295-309` |
| Self-shadowing     | 2026-07-23, PF-11 D6.3.3 ([TR-087](TR-087.md)) | GLSL `planet-sphere.ts:836-840`, WGSL `planet-sphere.ts:1105-1107` — line-parallel twins, declared SIMPLIFIED                                                                       |
| Tier/budget gating | shipped alongside self-shadowing               | `src/lib/babylon-tiers.ts:55-88` (`QUALITY_BUDGETS.planetTexture` ladder), unit-tested                                                                                              |

One genuine gap survives and is now named explicitly rather than implied by a blanket "not
started": Astra's [planetary-sphere science brief](../analysis/2026-07-21-planetary-sphere-topography-science-brief.md#L270-L275)
recommended a fuller ray-march self-shadowing approach; what shipped (TR-087) is a declared-
SIMPLIFIED terminator-elevation darkening term instead. That is recorded as a real, open
simplification — not claimed as full parity with the brief. Separately, TR-087's own "Known
limitations" flags that the `ultra` tier rung has never been exercised by any automated run
(Playwright/SwiftShader resolves to `balanced`), which is real-hardware residue rather than
missing code.

**Fix:** `PF-10-gaia-dataset-realism.md`'s C4.3 table cell struck through in place with a dated
correction; a new note block added directly below the table explaining what shipped, citing TR-087
and the exact source locations, and re-classifying C4.3 as substantially complete with only the
`ultra`-tier hardware confirmation open — folded into the same real-device pass the plan's own
status header already says B6/PF-10 is waiting on, not tracked as separate work. The file's
top-of-doc status paragraph (which already listed C4.3 among the "wants real hardware data" items)
got a matching parenthetical rather than being left to imply C4.3 was still unbuilt.
`docs/delivery-plan/README.md`'s PF-10 summary row updated to match.

### 2 — PF-09 gap-analysis GAP-17…27 status: confirmed accurate, not corrected

Read `docs/delivery-plan/PF-09-babylon-havok-realism.md` and
`docs/analysis/2026-07-19-webgl-babylon-cutover-gap-analysis.md` in full. Both already state the
target claim correctly and in detail: GAP-17 through GAP-21, GAP-24, GAP-25 and GAP-26 are fully
resolved (TR-060/TR-062); GAP-22's per-device fps rows (checklist §A1-A4) and GAP-27's manual
screen-reader pass remain open and are explicitly named as owner/hardware-gated — two independent
automated instruments were tried against both and failed for the same underlying reason (no
available tooling composites a real frame or operates a real screen reader), which is evidence for
the hardware-gated classification, not against it. GAP-23's residual piece is the same GAP-22
device residue, not a separate code gap — its bundle numbers are already real and current.

No claim in either file needed correcting. Per the task's own conditional ("if the gap-analysis
doc's per-gap status lines don't already say this clearly, add a dated summary note"), a short
**VERIFIED 2026-09-02** confirmation note was still added to the gap-analysis doc's "Recommended
triage" section, stating the GAP-22/23/27 characterization explicitly in one place rather than
requiring a reader to reconstruct it from five separate update blocks. `docs/delivery-plan/README.md`'s
PF-09 summary row (which previously just said "GAP-01…GAP-27 resolved" — technically imprecise,
since three of those are correctly still open) was tightened to name the GAP-22/27 residue and its
hardware-gated nature.

### 3 — PF-11 D5+D6 and D7+D9 code-review docs vs. their remediation TRs

Spot-checked three findings from the two review docs against current source to confirm TR-110 and
TR-113 actually landed what they claim:

| Finding                                                               | Review doc              | Verified in source                                                                                                                                             |
| --------------------------------------------------------------------- | ----------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Search box dead after Escape+type until blur/refocus                  | D5+D6 review, finding 1 | `MissionControlBar.tsx:135-137` — `onChange` now calls `setOpen(true)`, with a comment naming the exact bug TR-110 fixed                                       |
| `toBeltSpace`/`fromBeltSpace` per-frame allocation in the render loop | D5+D6 review, finding 3 | `babylon-asteroids.ts` — `beltPullAccelInto()` (line 475) and a module scratch (`_pullScratch`, line 532) exist; both hot loops confirmed allocation-free      |
| Renderer freeze from `clearCachedData()` on the rebuildable star mesh | D7+D9 review, C1        | `babylon-engine.ts:3185-3212` — the call is removed from `_applyStarFieldGeometry`, with an in-source comment citing TR-113 and stating the rule going forward |

All three confirmed fixed exactly as their remediation TRs describe. Both `TR-110` and `TR-113` were
already indexed in `docs/test-reports/README.md` (rows for 2026-07-29) — no index gap found.

**Fix:** the D5+D6 review doc already carried a `✅ RESOLVED, same day — TR-110` banner (added at
the time TR-110 landed); it needed no further change. The D7+D9 review doc had no such banner —
added one at the top, matching the existing D5+D6 doc's pattern, citing TR-113, the 172/172 E2E
result, and the specific fix location, while leaving the document's own CRITICAL/BLOCKED verdict
standing as the record of what review found before remediation.

## Verification

This is a documentation-only pass — no source, test, or build artefact changed, so CLAUDE.md #25's
full gate (`build && test && test:e2e && budget:check`) does not apply. What was checked:

- Every source citation above (`planet-sphere.ts`, `babylon-engine.ts`, `babylon-tiers.ts`,
  `MissionControlBar.tsx`, `babylon-asteroids.ts`) was read directly at the cited lines, not
  assumed from prior TRs.
- `git log -S"ROTATION_PERIOD_S" --oneline -- src/lib/planet-sphere.ts` confirms the rotation
  constant was introduced in commit `34ae8ac` (2026-07-21), matching the claim it corrects toward.
- `docs/test-reports/README.md` grepped for `TR-110`/`TR-113` — both present with full-detail rows.
- `npm run docs:check` — see Known limitations; not run as part of this pass (see below).

## Known limitations

- `npm run docs:check`, `npm run build`, `npm run test`, `npm run test:e2e`, and
  `npm run budget:check` were **not run** for this pass, per the task's own scope (documentation-only
  correction, verification deferred to the orchestrating session's separate full-suite pass). Every
  fact cited above was instead checked directly against source and git history, which is the
  applicable verification for a docs-only change.
- This TR does not itself constitute a CLAUDE.md #25 vertical-slice gate — it documents corrections
  to other documents, not a code slice.

## Files changed

- `docs/delivery-plan/PF-10-gaia-dataset-realism.md` — C4.3 table cell struck through + corrected;
  new explanatory note added below the C4 table; top-of-doc status paragraph given a matching
  parenthetical.
- `docs/analysis/2026-07-19-webgl-babylon-cutover-gap-analysis.md` — dated confirmation note added
  to "Recommended triage," no existing claim altered.
- `docs/analysis/2026-07-29-pf11-d7-d9-code-review.md` — `✅ RESOLVED — see TR-113` banner added
  at the top, matching the sibling D5+D6 review doc's existing pattern.
- `docs/delivery-plan/README.md` — PF-09 and PF-10 status-table cells updated to match the
  corrections above.
- `docs/test-reports/README.md` — this row added.
