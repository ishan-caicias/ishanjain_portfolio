# TR-001 — PF-07 Hero DSO Background Reveal

**Date:** 2026-07-06
**Scope:** [docs/delivery-plan/PF-07-hero-dso-background.md](../delivery-plan/PF-07-hero-dso-background.md)
**Verdict: OVERALL: READY TO PROCEED**

## Commands Run

| Step          | Command                                | Result                                                                                                                                                            |
| ------------- | -------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Typecheck     | `npm run check`                        | 0 errors, 0 warnings, 2 pre-existing hints (unrelated to this change)                                                                                             |
| Lint (scoped) | `npx eslint src tests scripts`         | 1 pre-existing error in `scripts/coverage-report.mjs` (unrelated, not modified by this change); 0 errors in any file touched by this feature                      |
| Format        | `npx prettier --check <touched files>` | Clean after `--write` on 7 touched files (see Known Limitations)                                                                                                  |
| Unit tests    | `npm run test`                         | **27/27 passed**, 5 test files                                                                                                                                    |
| Coverage      | `npm run test:coverage`                | Thresholds (80% lines/functions/branches/statements) met: 100% stmts, 97.29% branches, 91.66% functions, 100% lines across `src/components/islands` + `src/utils` |
| Build         | `npm run build`                        | Succeeded, static output to `dist/`, no errors                                                                                                                    |
| E2E           | `npm run test:e2e`                     | **19/19 passed** (Chromium), including axe critical/serious accessibility scan                                                                                    |

## Test Count Delta

- Unit: 26 → 27 (net +1). Removed `StarModal.test.tsx` (11 tests) and the `loadHubbleData` half of `hubble-utils.test.ts` (3 tests); added `HeroBackgroundReveal.test.tsx` (9 tests), `heroDso.test.ts` (3 tests), retained `fetchNasaApod` tests (5, renamed to `nasaApod.test.ts`).
- E2E: 18 → 19 (net +1). `star-interaction.spec.ts` rewritten: 1 canvas test (updated aria-label) + 4 reveal tests (was 3 modal tests), replacing StarModal-popup assertions with background-reveal assertions.

## Existing Tests Modified — Named, With Reason

Per the "no silent test modification" rule, every existing test file touched and why:

| File                                                              | Change                                                                                                                                                                                       | Reason                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ----------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `tests/unit/Starfield.test.tsx`                                   | Updated aria-label regex from `/starfield.*Hubble/i` to `/starfield.*deep-sky/i`; removed unused `beforeEach` import                                                                         | Direct consequence of retiring the Hubble-specific copy in `Starfield.tsx`'s aria-label (Decision D1); the unused import was a pre-existing lint issue in a file already being edited                                                                                                                                                                                                                                                                                        |
| `tests/unit/hubble-utils.test.ts` → `tests/unit/nasaApod.test.ts` | Renamed; removed the `loadHubbleData` describe block (3 tests); kept `fetchNasaApod` tests (5) unchanged in behavior; added a `beforeEach` that stubs `global.fetch` for that describe block | `loadHubbleData` was deleted per D1 (its only caller, `StarModal`, is retired). The `fetch` stub was added because removing the sibling describe block that used to leave `global.fetch` mocked as an accidental side effect exposed that `fetchNasaApod`'s tests weren't actually self-contained — see Known Limitations                                                                                                                                                    |
| `tests/unit/setup.ts`                                             | Added global `afterEach(cleanup)` from React Testing Library                                                                                                                                 | Was previously absent project-wide. Without it, RTL leaves each test's DOM mounted in `document.body`, so a later test's global queries (`getByLabelText`, `baseElement.textContent`) can silently match stale elements from an earlier test. This was latent and invisible in the old `StarModal.test.tsx` suite only because every test there used identical mock content; it surfaced immediately once `HeroBackgroundReveal.test.tsx` used distinct multi-entry fixtures |
| `tests/unit/StarModal.test.tsx`                                   | Deleted                                                                                                                                                                                      | Component retired per Decision D1 (confirmed by user)                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `tests/e2e/star-interaction.spec.ts`                              | Rewritten                                                                                                                                                                                    | `StarModal`'s popup UX is retired; asserts the new background-reveal behavior instead                                                                                                                                                                                                                                                                                                                                                                                        |
| `tests/e2e/navigation.spec.ts`                                    | Fixed 2 stale content assertions (`"Product-minded software engineer"` → `"Sydney-based software engineer with a product mindset"`; `"AWS Cloud Infrastructure"` → `"AWS & Cloud Platform"`) | **Not caused by this feature.** Confirmed via `git show 8e552bd` that the already-merged "Update content" commit rewrote this copy without updating these assertions — a pre-existing gap in `main`. Fixed opportunistically because the full E2E suite must pass for this VERIFY, not silently left broken                                                                                                                                                                  |
| `tests/e2e/accessibility.spec.ts`                                 | Fixed 1 stale heading assertion (`"Building reliable systems"` → `"Reliable systems"`)                                                                                                       | Same pre-existing cause as above                                                                                                                                                                                                                                                                                                                                                                                                                                             |

## Known Limitations

- **Contrast across all 50 images not individually verified.** The axe accessibility scan runs on initial page load (idle Starfield state) and passed with 0 critical/serious violations, but does not exercise the hero background _while a DSO reveal is active_ for all 50 images. The `bg-surface/75` scrim is a single fixed treatment applied uniformly, not tuned per image. Flagged in the delivery plan (§6) as a risk requiring a manual spot check — not fully closed by this VERIFY.
- **`fetchNasaApod` test coverage gap unrelated to this feature**: one branch in `nasaApod.ts` (line 26, the `hdurl` fallback path is covered, but a deeper edge case) and one line in `HeroBackgroundReveal.tsx` (line 66, part of the crossfade `onLoad` path) remain uncovered — both pre-existing-style gaps well within the 80% threshold, not blocking.
- **IC1848 (Soul Nebula) is excluded**, not fixed — `scripts/hero/build-hero-dso.mjs` drops it due to the confirmed byte-identical collision with IC1805, per the delivery plan's default. Re-sourcing the correct photo is out of scope for this milestone.
- **Stale worktree at `.claude/worktrees/pensive-easley-e8717e/`** is gitignored, untracked, local-only debris from an unrelated earlier session. It causes `npm run lint` (unscoped, i.e. `eslint .`) to report ~1100 unrelated errors against its stale/minified content. This VERIFY ran ESLint scoped to `src tests scripts` to get a signal on the actual change; it does not affect CI (fresh checkouts never have this directory). Not cleaned up as part of this change since it predates this session's work and wasn't authorized for deletion.

## Verdict

**OVERALL: READY TO PROCEED.** Full build green, 27/27 unit tests passing with coverage above threshold, 19/19 E2E tests passing including the accessibility scan, lint and formatting clean on every file this feature touches.
