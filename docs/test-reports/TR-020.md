# TR-020 — PF-07 Ship-v2 Phase 5: Hero Revision, Default-On Rollout & Release Hardening

**Date:** 2026-07-18
**Scope:** [PF-07-spaceship-v2.md](../delivery-plan/PF-07-spaceship-v2.md) Phase 5 (the plan's
final phase; the owner's "P6" maps to its rollout/deploy-readiness half)
**Verdict: OVERALL: READY TO PROCEED** — feature complete; deploy is the owner's push

## Owner Revision Applied First (visual sign-off follow-up)

Sign-off received with two changes: **2.5× ship scale** and **center stage**. Applied in the
station table: hero station scale 0.45 → 1.125 (exactly 2.5×) and target recentred so the ship
sits at screen center behind the hero copy. The other stations were scaled proportionately
rather than literally — parked 2× (featured under dossiers), corner escort 1.5× (coexists with
page content while reading) — an interpretation, stated for redirect. Re-captured: hull plating
and emissive cockpit detail now clearly visible; headline remains legible over the hull.

## P5 Deliverables

1. **Default-on rollout** — `resolveCraftAttribute`'s final fallback is now the auto device
   policy (2K capable / 1K constrained). Opt-outs unchanged: `?craft=off`, the quality
   selector's Off, stored overrides.
2. **Deliberate test modifications for the release flip** (named per policy):
   - `tests/unit/craft-tier.test.ts`: "nothing set → off (pre-P5)" became "nothing set → auto
     policy", plus a new explicit-opt-out test carrying the old guarantee.
   - `tests/e2e/craft-ship.spec.ts`: "without the flag, the craft never engages" replaced by
     "default page loads the craft via the auto policy" + "?craft=off restores the wireframe".
3. **CC-BY-4.0 attribution** — credits panel row (title, author, Sketchfab source, license
   link, conversion note), matching the license.txt obligation; `LICENSE.txt` already ships
   beside the GLBs (P0).
4. **Hardening artefacts** — [rollback runbook](../runbooks/2026-07-18-craft-rollback.md)
   (per-visitor and one-line site-wide paths) and the
   [release validation checklist](../validation-checklist/2026-07-18-ship-v2-release.md)
   with the post-deploy verification steps (Netlify GLB binary check, production craft-ready
   check, real-device performance).

## Commands Run / Verification

| Step             | Command / check            | Result                                                                                                                           |
| ---------------- | -------------------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| Build            | `npm run build`            | Exit 0 (lint, Prettier, astro check clean)                                                                                       |
| Unit             | `npm test`                 | **47/47 passed** (default-flip tests updated as named above)                                                                     |
| E2E              | `npm run test:e2e`         | **44/44 passed** — default-on proven (2K desktop), opt-out proven                                                                |
| Shell payload    | dist measurement           | **0.997 MiB** (document+scripts+styles+fonts) vs 10 MiB budget; GLB lazy: 0.412 / 1.019 MiB                                      |
| Dependency audit | `npm audit`                | **0 vulnerabilities**                                                                                                            |
| Frame-time probe | Engine `_ft` EMA, headless | 115.9 ms under SwiftShader **software rendering — not a real-device signal**; real-device check is the standing post-deploy item |
| Visual           | Full-page capture          | 2.5× center-stage hero verified                                                                                                  |

## Verdict

**OVERALL: READY TO PROCEED.** The PF-07 ship-v2 delivery plan is functionally complete:
all six phases delivered and verified, rollout default flipped, attribution shipped, rollback
documented. Remaining actions belong to the owner: **commit the batch, push/PR, deploy, then
run the checklist's post-deploy section.**

## Known Limitations

- Real-device GPU performance (Safari + mid-tier Android) is unmeasurable in this environment —
  first item on the post-deploy checklist, as planned since TR-015.
- The 1K tier at the new 2.5× hero scale is softer than 2K by design; constrained devices get
  it deliberately. Revisit only if it reads poorly on a real handset.
