# Delivery Timesheet — PF-11 Cinematic Journey & Scale Honesty

Plan: [docs/delivery-plan/PF-11-cinematic-journey-and-scale-honesty.md](../delivery-plan/PF-11-cinematic-journey-and-scale-honesty.md)

```json
[
  {
    "project": "ishanjain-portfolio",
    "deliveryPlan": "PF-11-cinematic-journey-and-scale-honesty",
    "phase": "PLAN — plan authoring + audits",
    "detail": "Author the PF-11 plan from the PF-10 tail + 14 owner requirements: Astra science brief (docs/analysis/2026-07-22-pf11-sky-frames-and-travel-science-brief.md), 10 parallel codebase audits (band/belt gating, arrival seam, cards, where-to console, travel physics, loading stages, perf/memory, PF-10 remainders, DR3 Tiny inventory), gaiasky.space + texture-licensing research into docs/research/, experience-designer + user-research passes",
    "status": "complete",
    "start": "2026-07-22T00:00:00Z",
    "end": "2026-07-22T00:00:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "Same recorded exception as PF-09 B0 / PF-10: no plan-specific scorecard; milestone card governs; re-run per-slice at IMPLEMENT. Same session also delivered TR-080 (the two owner-reported E2E failures root-caused and fixed; full suite 86/87 with the one failure A/B-confirmed as the documented cumulative-load class, promoted to plan slice D0.1) and CLAUDE.md non-negotiable #25 (per-slice full gate + manual regression). IMPLEMENT not started — awaiting owner confirmation and the D3.3/D6.x owner decisions listed in the plan."
  },
  {
    "project": "ishanjain-portfolio",
    "deliveryPlan": "PF-11-cinematic-journey-and-scale-honesty",
    "phase": "PLAN — owner decisions + implementation companion",
    "detail": "Record the owner decision set (ADR-0010): goHome=abort + retarget queue (D3.3), Reinhard curve (D6.1), asset-weight unblocked via opt-in layers (D6.5), RANDOM JUMP/RETURN HOME renames + Earth home-orbit R16 (D5.1), sourced owner-approved card drafts (D4.4), full 2.55M DR3 Tiny (D8.1); add the new R15 Render Console requirement as phase D9; author docs/implementation/PF-11-implementation-plan.md with bidirectional per-phase links",
    "status": "complete",
    "start": "2026-07-22T00:00:00Z",
    "end": "2026-07-22T00:00:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "Plan status moved DRAFT -> CONFIRMED (owner decisions constitute confirmation; recorded additively in the plan). D6.2 (belt frame) explicitly remains open — the owner passed on it. New docs/implementation/ directory established with its README contract (delivery plan owns WHAT/WHY, implementation plan owns HOW, delivery plan wins on conflict). Sequencing updated: D7.1/D7.2 lifecycle work precedes D9; D8 ships as a D9 layer. IMPLEMENT may start at D0 next session."
  },
  {
    "project": "ishanjain-portfolio",
    "deliveryPlan": "PF-11-cinematic-journey-and-scale-honesty",
    "phase": "PLAN — D6.2 decision (belt frame)",
    "detail": "Owner approved GO on the belt-frame re-expression (hours after the ADR-0010 decision set, same day): belt model + data move into the obliquity-inclined basis, --frame equatorial regen, m42 route re-tune, Kirkwood regression must pass unchanged, Astra audit closes the slice",
    "status": "complete",
    "start": "2026-07-22T00:00:00Z",
    "end": "2026-07-22T00:00:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "Recorded additively in ADR-0010 (dated update), the delivery plan (Decisions log, D6 table, open-decisions list, status header), the implementation plan (D6.2 section now GO with a 5-step sequence, recommended alongside/after D2.1 which touches the same code paths), and all four affected README indexes. ALL PF-11 owner decisions are now taken; the only remaining choices are data-driven calibrations (D9.4 presets, boot-critical budget value). Owner: D0 implementation starts within hours."
  },
  {
    "project": "ishanjain-portfolio",
    "deliveryPlan": "PF-11-cinematic-journey-and-scale-honesty",
    "phase": "D0.1 — frame-production liveness (band build)",
    "detail": "Replace the Milky Way band's 20-rows-per-rendered-frame build with MilkyWayBandBuilder: time-budgeted slices paced against an 8s wall-clock deadline, capped at 50ms so frames outrank the band, yielding to an in-flight warp, driven by both the render loop and a setTimeout(0) chain. Audit the other bulk builders (none per-frame chunked). Recalibrate craft-ship.spec.ts's six 20s ready ceilings to one 45s constant. Expose bandProgress on sceneStats for D1.1",
    "status": "complete",
    "start": "2026-07-22T00:00:00Z",
    "end": "2026-07-22T00:00:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "TR-081. The implementation plan's prescribed mechanism (setTimeout(0) chain) was DISPROVED by measurement — 10 timer callbacks vs 12 rAF callbacks in 5s on the rendering page — and made the build 65s; corrected additively in the implementation plan, with a read-across warning for D7.3/D7.4. A heavy 8-worker load recipe was also disproved as an instrument: a stash-and-rebuild control showed the BASELINE flakes identically, so it measures SwiftShader starvation, not liveness. Verification at moderate 4-worker load plus one quiet run."
  },
  {
    "project": "ishanjain-portfolio",
    "deliveryPlan": "PF-11-cinematic-journey-and-scale-honesty",
    "phase": "D0.2 — planet-sphere pixel proof (handoff B4)",
    "detail": "Add aimAt(bodyId) harness-only camera-bearing hook (gated ?testhooks) to babylon-engine.ts, next to travelTo/goHome/randomBody/setStations. Add tests/e2e/planet-pixels.spec.ts: screenshot the canvas post-arrival-and-aim, assert luminance+variance floors on a centred crop for Mars and Moon. Capture and commit the first-ever reference pixel proofs of a planet sphere",
    "status": "complete",
    "start": "2026-07-22T00:00:00Z",
    "end": "2026-07-22T00:00:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "TR-082. Root cause found this session, after five prior sessions of engine-state-correct-but-no-pixels: camera standoff was always right, orientation never was — _tickWarp's idle branch re-derives _camQuat from free-look _yaw/_pitch every frame, and nothing had ever pointed them at the arrived body. Full gate 561/561 unit / 88/89 E2E (one pre-existing D3.2-scoped GAP-17 flip-window flake, confirmed unrelated by diff scope) / budgets green. Manual regression again hit this session's MCP Browser-pane backgrounded-tab limitation (same as TR-081); worked around with a throwaway scripted-chromium pass against the built preview, deleted after use. Earth-class screenshot deferred to D6.4 per this slice's own exit note."
  },
  {
    "project": "ishanjain-portfolio",
    "deliveryPlan": "PF-11-cinematic-journey-and-scale-honesty",
    "phase": "D0.3 — real-device pass, round 3 (owner-involved)",
    "detail": "Started a LAN-reachable preview server (astro preview --host) so the owner could test from Galaxy S24 Ultra and Galaxy S20 FE over Wi-Fi. Owner reported ?perf=1 cold-load readings for desktop and both phones. Validated against CLAUDE.md's instrument checks, cross-referenced against PF-09's two prior device-pass rounds (TR-068/069), and recorded in TR-083",
    "status": "complete",
    "start": "2026-07-22T00:00:00Z",
    "end": "2026-07-22T00:00:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "TR-083. This is round 3 of PF-09's still-open B6 device gate, not a fresh measurement in isolation — TR-068 (round 1) and TR-069 (round 2) had established a reproducible flagship-underperforms-mid-tier anomaly (S24 Ultra losing to S20 FE on render fps both times) with the root cause (DPR/tier/backend) unconfirmed because the overlay's ENGINE/TIER/device-signature line was never reported. This round REVERSED the anomaly (S24 Ultra 60fps vs S20 FE 45fps, both now clearing the >=40fps floor) but the SAME line went unreported a third consecutive time, so the reversal is real but still unexplained. No instrument-validity check rejected any reading. Did not unilaterally close B6 — A1 (desktop) still fails 60fps, A2 (iPad)/A4 (WebGL2 fallback) remain untested, same non-rubber-stamping standard TR-068/069 set. D0.3's own exit criterion (real per-device numbers feeding D6.3/D8/D9.4) is satisfied regardless of B6's separate status. Updated in the same pass: PF-09 delivery-plan status header (additive), the B6 cutover checklist rows A1/A3/E.5, PF-11's own status header, and both timesheets."
  },
  {
    "project": "ishanjain-portfolio",
    "deliveryPlan": "PF-11-cinematic-journey-and-scale-honesty",
    "phase": "D1.1 — real progress instrumentation (the honesty contract)",
    "detail": "New pure module src/lib/load-progress.ts: fetchWithProgress over the ReadableStream reader (throttled 4Hz, unthrottled done, no-body fallback), StageAggregator summing multi-URL stages, BOOT_CRITICAL_ASSETS. New additive cosmos:stage event; cosmos:progress untouched. Nine stages wired into babylon-engine.ts (star-catalog, atlas-map, craft-glb, havok-wasm, bonus-layers, sdss-field, asteroid-belt + engine-init/first-frame checkpoints). budgets.config.mjs gains bootCriticalDownloadKB, unit-asserted against real on-disk bytes",
    "status": "complete",
    "start": "2026-07-22T00:00:00Z",
    "end": "2026-07-22T00:00:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "TR-084. ROUTED PER THE SCORECARD'S OWN SPLIT for this slice ('pure module is Sonnet-grade; _boot touchpoints are Opus'): load-progress.ts + its unit tests were built on Sonnet 5 high, then the owner switched to Opus 4.8 high for the engine wiring, which is TR-059-guarded hot boot path. TWO ASSETS WERE STRUCTURALLY UNOBSERVABLE BEFORE THIS SLICE and are the substantive findings: Havok's 2.09 MB WASM (Emscripten fetches it INSIDE the factory where nothing can watch) — solved by streaming it here and handing over the buffer via wasmBinary, with the VENDORED RUNTIME PROBED FIRST (HavokPhysics_es.js's getBinarySync returns new Uint8Array(wasmBinary) before any network path) to confirm no double fetch rather than assuming it, per TR-081's probe-before-building lesson; locateFile deliberately KEPT alongside so a future Havok bump that ignores wasmBinary degrades to fetching from the same origin — losing progress is acceptable, losing the ADR-0005 origin guarantee is not. And the craft GLB, where ImportMeshAsync's onProgress had simply never been passed. EVERY reported byte total equals its file's real on-disk size byte-for-byte (verified in both E2E and the manual pass). Stage-closing discipline was the non-obvious correctness requirement: several engine paths degrade gracefully rather than throwing, so every aggregator closes in a finally with an idempotent finish(), and the no-WebGL branch explicitly closes all four boot-critical stages (verified manually — that is D1-AC7's foundation). THREE SCOPE POINTS RECORDED ADDITIVELY rather than resolved silently: the plan's prose names two cosmos:stage emitters its own type block omits (band progress already has a real signal, sceneStats().bandProgress, handed over by TR-081 — a band LoadStage would have created a second source of truth); atlas-photo (3.5MB) deliberately deferred because Babylon's Texture exposes completion only and wiring it means re-routing the GAP-02 layer through a blob: URL that deserves its own verification; D1-AC1 deferred to D1.2 since 'visible progress' asserts a rendered surface D1.1 does not ship. Full gate 572/572 unit (+11) / 93/93 E2E FULLY GREEN (first since TR-081; includes GAP-17 which failed in D0.2's run — still D3.2's to fix, its passing here is not evidence it is fixed) / budgets green. A comment-only edit landed after the suite started; rather than claim comments cannot matter, dist/_astro's 233 emitted JS chunks were hashed before and after a rebuild and proven byte-for-byte identical, so no re-run was needed. Manual regression again went through a scripted chromium — the MCP Browser pane's backgrounded-tab limitation has now recurred three slices running."
  },
  {
    "project": "ishanjain-portfolio",
    "deliveryPlan": "PF-11-cinematic-journey-and-scale-honesty",
    "phase": "D1.2 — PRE-FLIGHT dossier UI (loading becomes a real gate)",
    "detail": "New PreFlight.tsx + preflight-state.ts (pure state machine, stall detection, byte formatting) + shared focus-utils.ts (moveFocusTo/restoreFocusTo, D4.3 reuses it). Replaces the automatic body.ij-loading clear (ready&&craftDone with 2.5s/8s grace timers) with a real gate: LAUNCH (disabled/ARMING% until every boot-critical cosmos:stage closes, then auto-focuses), SKIP INTRO (available from mount per the plan's own wording, not the state diagram's S2-only reading), a 10s stall detector showing PROCEED ANYWAY, and a sessionStorage returning-visitor bypass",
    "status": "complete",
    "start": "2026-07-23T00:00:00Z",
    "end": "2026-07-23T00:00:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "TR-085. Built on Sonnet 5 high per the scorecard's own D1.2 routing (React chrome). JUDGMENT CALL recorded: the experience review's state diagram draws SKIP INTRO as an S2-armed-only transition, but its own prose ('as soon as boot-critical stages allow') and this delivery plan's own D1.2 text ('a SKIP INTRO link during loading') say otherwise, and the review's risk section names pre-arm availability as the explicit mitigation for the 10-20s hotel-Wi-Fi gate risk -- implemented per the prose, against the diagram, recorded rather than silently picked. TWO REAL DEFECTS FOUND DURING VERIFY, one by each half of the gate: (1) unit tests caught moveFocusTo stamping tabindex=-1 UNCONDITIONALLY rather than only when needed -- would have marked every focus target including LAUNCH itself, caught by focus-utils.test.ts's first assertion before any browser ran it; (2) the full E2E suite caught the pre-existing 9s CSS no-JS/hydration failsafe firing during ENTIRELY NORMAL operation once loading became visitor-gated (craft alone is calibrated up to 45s elsewhere in this suite) -- it was force-revealing content out from under a still-working PreFlight. Fixed by scoping the failsafe to body.ij-loading:not(.ij-preflight-mounted), a body class PreFlight sets on mount; the 9s VALUE stayed exactly as instructed, only the CONDITION was corrected, and only because measurement (not a guess) showed it wrong. FIVE NAMED E2E TEST CHANGES, all one root cause: #ij-preflight structurally shares its screen region with #ij-mission-bar (RNG button) and the viewport-center coordinate two pre-existing free-look-drag tests use -- by design, both occupy the same lower-hero dock at different phases. Fixed by adding a SKIP INTRO dismissal (what a real visitor would do) ahead of each affected interaction; no assertion changed. THREE FULL-SUITE RUNS this session (CLAUDE.md #27's budget, used in full): 97/101 -> 99/101 -> 100/101, each round finding one more instance of the same collision class before the final gate closed on only the pre-existing D3.2 GAP-17 flake. SCOPE CALL: no separate Astro/SSR shell built for identity -- every other scene-chrome surface in this codebase (HUD, WarpOverlay, ArrivalVista) is React-only too, and PreFlight renders identity on mount (client:load hydrates near-instantly); revisit only if a measured hydration-gap ever shows it matters. Full gate 595/595 unit (+23: 15 preflight-state, 8 focus-utils) / 100/101 E2E / budgets green (JS +2KB). Manual regression again via scripted chromium -- fourth slice running into the MCP Browser pane's backgrounded-tab limitation."
  },
  {
    "project": "ishanjain-portfolio",
    "deliveryPlan": "PF-11-cinematic-journey-and-scale-honesty",
    "phase": "D1.4 -- first-party funnel instrumentation (?funnel=1)",
    "detail": "New pure funnel.ts (FunnelRecorder with ring-buffer localStorage persistence keyed by a monotonic session id, resolveFunnelOverlay mirroring resolveEngine's URL->stored->default shape, derived-flag helpers: armed->press delta, accidental-warp detector, zero-result searches, isPlausibleSession) + FunnelOverlay.tsx (debug panel, zero runtime style props). Recorder always runs (window.__ijFunnel(), same convention as __ijPerf); only the visible overlay is URL/stored-gated. Milestones wired at the real cosmos:* listeners/UI callbacks already in SpaceScene.tsx",
    "status": "complete",
    "start": "2026-07-23T00:00:00Z",
    "end": "2026-07-23T00:00:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "TR-086. Sequencing note: the delivery plan lists D1.4 ahead of D1.3 in its own document order (the implementation plan lists D1.3 first); D1.3 has not shipped, so this slice was scoped to work correctly either way -- a defensive cosmos:ascent-done listener records cinematic-done additively, and simply never fires until D1.3 lands. TWO REAL BUGS FOUND, neither anticipated by the plan: (1) a React error #310 hooks-order crash on EVERY page load -- the first version recorded search-keystroke from a useEffect placed after SpaceScene.tsx's pre-existing 'if (!engineReady) return null' early exit, making the hook run conditionally; every E2E spec failed identically (babylon-scene never appearing) until a throwaway page.on('pageerror') probe surfaced the real error, not the symptom -- fixed by extracting a top-level computeSuggestions(cmd) function and recording synchronously inside the existing onCmdChange callback instead of adding an effect; (2) cosmos:warp proved structurally unable to detect its own start -- cosmos:select's own handler already sets state.warp to the aim-phase object before the first cosmos:warp tick arrives (TR-081: cosmos:warp fires once per rendered frame for the WHOLE journey, not once at its start), so a null-check there could never fire -- fixed by moving the travel milestone to cosmos:select's non-quiet branch instead, which is if anything a MORE accurate timestamp for the accidental-warp detector. NAMING DECISION recorded: the recorder stores repeatable action names (travel, search-keystroke, vista-dismissed, ...) rather than the plan's literal first-X labels, because vista-dismissed structurally needs every occurrence (not just the first) to drive the accidental-warp detector against a LATER dismissal -- firstEventTime() recovers the plan's exact first-X semantic on demand for display; the four genuinely one-shot gate milestones (dossier-visible/launch-armed/launch-pressed/cinematic-done) DO dedupe to their first occurrence. ALSO FOUND, DELIBERATELY NOT FIXED: a pre-existing z-index overlap between #ij-mission-bar (z-62) and ArrivalVista's button row (z-61) -- no prior spec had ever clicked OPEN COLLECTOR CARD before this slice's E2E did, and a real coordinate-based click there (even with force:true, which only skips Playwright's own actionability check, not the browser's real hit-testing) lands on the WHERE-TO input instead; worked around in the E2E spec with a direct element.click() dispatch, handed to D4.1 (whose vista-dismissal rework replaces the whole interaction) rather than fixed out of scope here. FOUR ISOLATED FIX-VERIFY CYCLES during VERIFY (CLAUDE.md #27's economics -- single-spec isolation runs, never a full-suite re-run, for all four): the two bugs above, a test-timing race against the vista's real 5.5s auto-timeout (tightened by clicking OPEN COLLECTOR CARD the instant it appears rather than after two separate polls), and the click-interception fix. Full gate 619/619 unit (+24, all funnel.test.ts) / 107/108 E2E (+7 new; the one failure is D3.2's already-documented GAP-17 flake, confirmed unrelated via 3 isolation runs: 2 passed, 1 failed with the identical intermittent signature) / budgets green (JS +1.4KB). Manual regression again via scripted chromium -- fifth slice running into the MCP Browser pane's backgrounded-tab limitation, first noted TR-081."
  },
  {
    "project": "ishanjain-portfolio",
    "deliveryPlan": "PF-11-cinematic-journey-and-scale-honesty",
    "phase": "D6.1 + D6.3 -- exposure, the linear pipeline, tier-gated planet textures",
    "detail": "planet-sphere.ts: sRGB decode of surfaceTex only (exact piecewise EOTF both twins), opposition surge relocated from uAlbedo into refl via a new uSurgeB0 uniform + PLANET_SURGE/exposureTermsFor, Reinhard L/(1+L) before a single output encode, EXPOSURE_GAIN 3.6->1.9, relief self-shadowing gated on uHasHeight/uHasNormal. babylon-tiers.ts: QualityBudget gains planetTexture (base/high/ultra-progressive). babylon-engine.ts: tier-aware first rung + ultra fetched only at the top tier, uAtmosphere reads a real manifest flag. build-planet-textures.mjs: atmosphere declaration + METADATA_FIELDS/syncManifestMetadata reconcile",
    "status": "complete",
    "start": "2026-07-23T00:00:00Z",
    "end": "2026-07-23T00:00:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "TR-087 + REALISM-AUDIT docs/analysis/2026-07-23-pf11-d6.1-exposure-realism-review.md. SHIPPED AS ONE SLICE because the implementation plan requires it -- D6.3.1's sRGB decode moves every number the D6.1 Reinhard decision was priced against, so landing them apart would have meant tuning the exposure twice and throwing the first answer away. MEASURED BEFORE CHANGING, and validated the instrument first: re-derived the cos-latitude-weighted mean luminance of every shipped surface map and reproduced Astra's independent A2.3 table TO THREE DECIMALS on all nine bodies both covered, before trusting any number derived from it. That instrument then found what A2.3 could not: EIGHT bodies clip today, not six -- Saturn (1.500) and Jupiter (1.207) were never in the brief's table, which sampled nine of sixteen shipped bodies. Recorded additively. THE EXIT CRITERION AS WRITTEN WAS UNACHIEVABLE and that is the substantive finding: 'six clipping bodies < 1.0; Moon/Mars unchanged within ~5%' cannot hold with the gain left at 3.6, because Reinhard alone takes the Moon -22% and the decode moves it further -- so the gain was SOLVED (3.6 -> 1.9) rather than tuned, landing Moon -2.8% and Mars -0.5% with nothing clipping at all. A single shared gain was chosen over per-body multipliers deliberately: per-body gains destroy the ratios, which is the one thing the exposure section exists to protect. THE SURGE RELOCATION IS UNIT-ASSERTED AS LOSSLESS -- base*(1+B0) reconstructs each published geometric albedo exactly at zero phase, which is what makes it a relocation rather than a re-grade; Mars is excluded because its Bond albedo EXCEEDS its geometric albedo so its coefficient would be negative (Astra: 'Leave Mars alone'), and that exclusion is itself a test. THE sRGB DECODE'S EXCLUSIONS were the careful part, not the decode: cloudTex and specularTex are coverage fractions (Astra had already validated the cloud map's RAW byte against Earth's real geometric albedo -- 7% closure vs 33% miss if decoded) and heightTex/normalTex/detailTex carry elevation and vectors, so a blanket 'decode every texture' would have silently corrupted four of five samplers. TWO DEFECTS CAUGHT BEFORE SHIPPING: an existing guard caught a literal backtick in the WGSL source on the first unit run (TR-078 Part 5's silent-compile-failure trap, from an escaped-backtick comment inside the template literal), and astro check caught a type error in this slice's own new test -- worth recording that my first verification command piped npm run build through tail, which MASKS the pipeline exit code, so budget:check ran anyway and the type error nearly slipped past; the repo's gate is fine, the invocation was mine. A STRUCTURAL PIPELINE GAP FIXED ON THE WAY: --if-stale judges staleness on image mtimes and was therefore blind to a declaration change that adds a manifest field without touching a pixel, so the uAtmosphere flag could not land without either re-emitting all 88 images or hand-editing a generated file (CLAUDE.md #22) -- fixed with a syncManifestMetadata reconcile pass sharing one METADATA_FIELDS list with the full-build path so the two modes cannot drift. E2E HONESTY: run 1 was 106/108 with both failures in the exact subsystem this slice changed, so they were NOT written off as the known flake -- four isolation experiments (the two specs alone 2/2; engine-select alone 32/32; the exact 51-test preceding prefix 51/51) plus a fully green 108/108 run 2 establish they are intermittent and load-dependent, and the TR states plainly that no baseline A/B was run so 'pre-existing' is inherited from prior TRs rather than demonstrated. No test was modified. Full gate 635/635 unit (+16) / 108/108 E2E / budgets green with no asset re-emitted. Pixel evidence captured to NEW d6.1-named files rather than through the spec's PF11_CAPTURE_REFERENCE flag, because overwriting the committed D0.2 references would have made TR-082 appear to show an exposure it never proved -- corrections are additive, including visual ones. Tethys and Dione now render full-disc crater detail where they previously clipped to flat white; Mars is visually indistinguishable from its D0.2 reference, confirming the -0.5% prediction by eye. Manual regression via scripted chromium -- sixth slice running into the MCP Browser pane's backgrounded-tab limitation."
  },
  {
    "project": "ishanjain-portfolio",
    "deliveryPlan": "PF-11-cinematic-journey-and-scale-honesty",
    "phase": "D6.4 -- Earth goHome reveal at 90 degree phase + the R16 home orbit",
    "detail": "Night-light assets resurrected (MAP_TIER_CAP per-map tier ceiling so night ships base+high; PACK_RECKONING entry preserved and superseded in place; ADR-0009 ceilings raised 169.5->170.0 and 256.5->257.0). Night-lights shader term in both twins, masked on the true N.L, floor-subtracted, composited in linear before Reinhard. Earth-at-origin sphere path keyed on the home orbit rather than arrivedId, with uSunDir read from the Sun own catalog entry. homeOrbitPosition() pure orbit math + HOME_ORBIT_* constants; _isAtHomeVantage() replacing the |cam|<1 predicate in BOTH places it lived. New tests/e2e/earth-home.spec.ts (5 specs) + 11 unit tests",
    "status": "complete",
    "start": "2026-07-23T00:00:00Z",
    "end": "2026-07-23T00:00:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "TR-088 + REALISM-AUDIT docs/analysis/2026-07-23-pf11-d6.4-earth-home-realism-review.md. UNBLOCKS D1.3 -- which was the point of the owner request, and worth noting the request said it unblocks D1.4: D1.4 shipped earlier the same session (TR-086) and the plan sequencing line is \"D6.4 must precede D1.3\". HAD TO FOLLOW TR-087, not precede it: city lights added AFTER a tone map are additive in display space and clip to flat white blobs with no structure, so they must composite in linear before the curve -- a unit test now pins that ordering in both twins rather than leaving it to be rediscovered. THE OWNER DECISION CAME IN FAR UNDER BUDGET: base+high night lights measured 0.25 MB (0.05 + 0.20) against the ~1.8 MB approved and ~6.4 MB for three tiers, because the map is mostly black and JPEG compresses it to nearly nothing -- so the ceiling raise was 0.5 MB, not 2. THREE REAL DEFECTS FOUND AND FIXED, each worth its own note: (1) the reveal fired AT BOOT, where cam=[0,0,0] is INSIDE the radius-26 sphere, and pulled ~2.5 MB of Earth textures into startup on every page load -- caught by reading probe output, and the uncomfortable part is that the E2E I had already written ASSERTED THE BUGGY BEHAVIOUR because I wrote it from what I observed rather than from the contract; the spec was corrected to assert the contract and now fails against the old behaviour; (2) the orbit ran 8x SLOW (1.5 deg in 6s against the 12 deg a 180s period implies) because the idle branch used the SHIP_MAX_DT-clamped dt, and the fix was ALREADY DOCUMENTED twenty lines below in the same function where the warp integration explains that progress must track wall clock while only the physics step is clamped -- switched to _dtWarpS, re-measured 16 deg in ~6s; (3) a canvas tabindex=1 axe violation (CLAUDE.md #23) whose window is BEFORE the first rendered frame, which a per-frame re-assert structurally cannot close -- now a MutationObserver, disconnected on teardown, so an accessibility contract no longer depends on frame pacing; pre-existing race that this slice larger shader made more likely rather than one it introduced, and said so. NAMED TEST CHANGE (#15): the \"does NOT ship Earth night lights\" assertion -- described in its own file as the highest-value assertion there -- is INVERTED, on the precondition PACK_RECKONING itself named (\"Ship it if the arrival geometry ever changes\"). Every word of the original reasoning is still true about travelTo; what changed is the geometry. The test now pins \"no night map WITHOUT the 90 degree vantage that earns it\", the reckoning entry is preserved rather than deleted, and a NEW test recomputes the 90.0000 degree phase from the Sun own catalog entry so the lights lose their licence the moment that geometry moves. THREE FULL-SUITE RUNS (CLAUDE.md #27 budget, used in full) -- and one of them was WASTED because npm run build failed on an unformatted doc and Playwright silently served a stale dist, which is #27 corollary paid in full and recorded as such. Final gate 646/646 unit (+11) / 112/113 E2E, the one failure being the WGSL hardware spec timing out at its 30s ceiling in a run that took 22.1 min against the usual 14-18 (loaded machine), confirmed 3/3 in isolation at 4.3s. CARRIED FORWARD, flagged by the realism audit rather than left implicit: Earth PLANET_LUNAR_L=0 and albedo 0.213 were BOTH fitted at alpha=0 and are now rendered at alpha=90, which the Earth brief predicts is a 37-67% error -- wants Astra numbers before D1.3 reuses this geometry. Manual regression via scripted chromium (seventh slice hitting the MCP Browser pane backgrounded-tab limitation): boot console-clean, near/far travel, dossier, SOL reveal showing sphere=earth at phase=90 with the orbit advancing 14->46 deg, classic-view toggle, 0 console/page errors. First terminator any body in this scene has ever shown, captured in docs/test-reports/assets/pf11-d6.4-earth-home.png."
  },
  {
    "project": "ishanjain-portfolio",
    "deliveryPlan": "PF-11-cinematic-journey-and-scale-honesty",
    "phase": "D1.3 -- launch-from-Earth ascent + console reveal (Phase D1 cinematic close)",
    "detail": "Vega-discipline motion-spec authored before IMPLEMENT (docs/experience-design/2026-07-23-pf11-d1.3-ascent-motion-spec.md). New pure ascent.ts (easeCam=k^3 vs easeAlt=smoothstep, skyMix=exp(-h/8.5), starFade, limbIntensity, + limb-glow shader twins). Engine: warp.mode=ascent + _tickAscent (camera along the home sight-line, sky onto scene.clearColor, band uFade, fresnel-rim limb shell), beginAscent/skipAscent, wall-clock dt, handoff re-arms the home orbit + emits cosmos:ascent-done. Host: onLaunch(ascent) threads the LAUNCH-only cinematic; console reveal deferred to ascent-done; AscentSkip overlay. global.css dock formalized + reveal rise. sceneStats ascent fields",
    "status": "complete",
    "start": "2026-07-23T00:00:00Z",
    "end": "2026-07-23T00:00:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "TR-089 + MOTION-SPEC (authored pre-IMPLEMENT, Vega discipline, Procyon) + Astra REALISM-AUDIT docs/analysis/2026-07-23-pf11-d1.3-ascent-realism-review.md. CINEMATIC SLICE, so both sisters paired as the operating model prescribes: the Astra physics pre-existed as the sky-frames brief §4 (authored FOR this feature), the motion design was authored fresh before writing engine code, and both were audited/confirmed after. THE BACKBONE INVARIANT: LAUNCH is the D6.4 goHome reveal played FORWARD -- same sphere, same 90deg terminator geometry, same final pose -- so the ascent end standoff is UNIT-ASSERTED equal to ARRIVE_STANDOFF/HOME_ORBIT_RADIUS and post-launch == post-goHome as one frame; confirmed by eye (the handoff screenshot is visually identical to a goHome arrival). REQUEST-FRAMING NOTE recorded: the owner asked D1.3 to unblock D1.4, but D1.4 already shipped earlier the same day (TR-086); the plan sequencing is D6.4-precedes-D1.3 and D6.4 (TR-088) is what actually unblocked this -- D1.3 completes Phase D1 rather than unblocking D1.4. FIVE SCOPE DECISIONS recorded not silently resolved: the ascent is a RADIAL climb not a lateral flight (vertical-from-surface IS radial in a geocentric frame); the star field has no fade uniform so only the band uFade is driven and the star reveal emerges from the darkening background (cheaper AND more physical, no ijStar twin change); the limb ring is a fresnel-rim SHELL not a billboard arc (declared SIMPLIFIED, sun-gated); progress uses wall-clock _dtWarpS not the clamped dt (D6.4 orbit lesson applied pre-emptively -- measured ~12-14s on SwiftShader vs ~8s real, stretched never sped up); only the real LAUNCH plays the ascent (skip/bypass go straight to content). FIVE NAMED TEST CHANGES (CLAUDE.md #15), all one cause: D1.2 preflight tests asserted ij-loading clears IMMEDIATELY on LAUNCH, but D1.3 inserts the ~8s ascent before the reveal -- widened the reveal-timing window (no assertion weakened; the animated reveal itself is covered by the new ascent.spec.ts), and dropped the returning-visitor test first-launch wait (sessionStorage is set synchronously in PreFlight.launch). A TEST-DESIGN LESSON recorded: the skip E2E first failed with element-detached because sceneStats() evals take MULTIPLE SECONDS each under SwiftShader load (one measured 12s), so a stats-based precheck consumed enough wall-clock that the ascent completed inside it -- fixed by detecting ascent state through the synchronously-rendered SKIP overlay (React, no eval) instead. ASTRA ESCALATION carried forward: Earth PLANET_LUNAR_L=0 and albedo 0.213 were fitted at alpha=0 and D1.3 is now the SECOND feature rendering Earth at alpha=90; the D6.4 review asked this be closed before D1.3 built on it, it was not, so the audit escalates -- blocks nothing shipped but should block the next Earth-visual change. Full gate 665/665 unit (+19 ascent.test.ts) / full E2E green after the named preflight changes (the 3 failures were exactly the reveal-timing change, verified fixed) / budgets green (JS +5KB). Manual regression via scripted chromium (eighth slice hitting the MCP Browser pane backgrounded-tab limitation): LAUNCH plays the ascent console-clean, hands off to the Earth home vantage at phase 90, console reveals -- captured in docs/test-reports/assets/pf11-d1.3-ascent-done.png; mid-ascent visual (blue-black sky, limb arc) needs real hardware, flagged owner-confirmed. The two cheapest UX validations the plan pre-build-gates (hallway x5, dossier-vs-spinner) are human studies, owner-run, flagged not blocking."
  },
  {
    "project": "ishanjain-portfolio",
    "deliveryPlan": "PF-11-cinematic-journey-and-scale-honesty",
    "phase": "D2.1 + D2.2 -- the frame ladder (solar-system furniture gating + extragalactic collapse)",
    "detail": "Shared uLayerFade mechanism: ijStar fragment twins (GLSL+WGSL) gain a per-layer fade multiply; main star field -> _localMat clone, belt -> _beltMat clone, SDSS stays on base _starMat at 1. Clones kept in lockstep on uTime/aberration/uViewport/uHaloAmp. New ship-dynamics.ts pure fns (furnitureVisibility, localFieldVisibility, frameLadderFade + thresholds); _updateFrameLadder drives fades from destination lyTotal+prog captured in _beginWarp. _tickAsteroids sleeps (skip forces + gate collision-shake) on _beltPhysicsAwake. _tickMilkyWay uFade = bandFadeAmt x _localFieldFade every frame. Constellation alpha x _localFieldFade. New procedural (license-clean) impostor: milkyWayImpostorPixel/buildMilkyWayImpostor + ijImpostor shader twins + _setupImpostor/_updateImpostor (billboard quad, real angular size floored, astern of the target). HUD LOCAL FIELD BELOW RESOLUTION line via SpaceScene farField prop. sceneStats gains furnitureFade/localFieldFade/beltPhysicsAwake/impostorVisible/impostorTextureReady; bandFade now effective opacity. New tests/unit/frame-ladder.test.ts (15) + tests/e2e/frame-ladder.spec.ts; webgpu-hardware.spec gains an extragalactic-hop impostor-WGSL check",
    "status": "complete",
    "start": "2026-07-23T00:00:00Z",
    "end": "2026-07-23T00:00:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "TR-090 + Astra REALISM-AUDIT docs/analysis/2026-07-23-pf11-d2-frame-ladder-realism-review.md. SHIPPED AS ONE SLICE because D2.1 and D2.2 share the uLayerFade fade mechanism (the plan's own 'shared mechanism first' note) -- same reasoning D6.1+D6.3 landed together. THE KEY ENGINEERING CALL: per-mesh fade is via MATERIAL CLONES, not material.onBindObservable -- on WebGPU a shared material's UBO uploads once, so a per-mesh set inside onBind would give every mesh the LAST value; the clone approach the plan specifies is the WebGPU-safe one, at the cost of fanning uTime/aberration/uViewport/uHaloAmp out to the clones (recorded in the impl-plan notes-as-built). SDSS deliberately stays on the base material at fade 1 (Astra: isotropic, effectively infinite -- never fades). THE IMPOSTOR IS PROCEDURAL, NOT THE PHOTOGRAPH: the Risinger Milky-Way pano is credited without a printed licence (the standing licensing tripwire), so it is NOT shipped -- milkyWayImpostorPixel builds an inclined disc from the band's OWN tone model instead, license-clean and declared SIMPLIFIED (ledger L12); its on-screen size uses the real angular subtense theta=2*atan(15kpc/d) floored to a visible minimum (ledger L13, same declared-overbright class as the belt). TWO HONEST SCOPE CUTS, realism-audited not silently dropped: (1) sun glare / planet billboards are NOT individually faded -- they share one billboard material with no per-body fade channel, and the dominant artefact (a 154,662-speck belt ring at every DSO) IS gated; (2) the Havok sleep skips the force loop + the collision-shake callback but does NOT yet halt integration (setMotionType STATIC / zero timestep), which is D7.5's measured refinement -- beltPhysicsAwake is the assertable signal either way. THREE E2E TIMING LESSONS: the band's boot fade-in is SLOW on SwiftShader (heavy scene, low fps), so the collapse is proven RELATIVELY (bandFade = ramp x collapse -> exactly 0 at nbg regardless of the ramp; restored >= pre-collapse at home) rather than waiting the ramp out; the goHome-from-extragalactic settle needed a 15s poll (all fade-settle polls given the same SwiftShader-load budget the D0.2/craft specs use); one early 5s-budget flake was load-induced settle, not logic (values correct 2/3), fixed by the timeout, then 3/3 stable in isolation. THE GATE WAS RUN UNDER SUSTAINED MACHINE LOAD (my own concurrent stability runs + the environment): BOTH budgeted full-suite runs took 27-29 min vs the usual 14-18 and hit ROTATING SwiftShader-contention failures -- run 1 failed NGC2000/mars/mobile-menu, run 2 failed a DIFFERENT pair accessibility/GAP-17 -- and ALL FIVE distinct specs pass in isolation (4/4 and 2/2 verified), the textbook single-emulator rotating-phantom class CLAUDE.md #16 names and TR-082/085/086/088/089 each recorded; GAP-17 is the already-documented flip-window flake. Characterized via isolation per #27, did NOT keep re-running the full suite to chase green. Full gate 680/680 unit (+15) / 119 E2E (+1, 3/3 in isolation) / budgets green (JS +5KB, no new shipped asset -- impostor texture is runtime-generated). MANUAL REGRESSION on REAL WebGPU via the MCP Browser pane: boot console-clean (backend=webgpu, starSource=catalog, zero errors) and -- the check that matters here -- impostorTextureReady=true at boot, i.e. the impostor sampler is bound on real hardware before the mesh draws (#9). Driving warps to completion + screenshots hit the SAME backgrounded-tab throttle D6.4/D1.3 documented (rAF throttled, warps never integrate, compositor screenshot hangs); the behavioural ladder is fully driven+asserted by frame-ladder.spec on the SwiftShader path and the impostor WGSL draw gets a real-GPU check in webgpu-hardware.spec. The belt-fade/band-collapse/impostor LOOK is flagged owner-confirmed-visual. D2.3 (constellation dissolve at ly 50-500 + the full 4-entry ledger) remains -- the figures here fade only as part of the extragalactic collapse. FABLE-5 REVIEW (same day, post-gate, TR-090 addendum): an adversarial fresh-eyes pass found ONE REAL DEFECT every assertion had missed -- the impostor was DEPTH-OCCLUDED behind the band skybox, which is an opaque depth-writing shell at a constant 2000 from the CAMERA (infiniteDistance), while the impostor sat world-fixed at 1800 from the ORIGIN = ~2840 from the camera at an nbg arrival; every fragment depth-failed, state said visible, zero pixels reached the screen -- the exact B4 state-correct-but-no-pixels class, missed because every impostor assertion was state-based and the manual-regression tab throttle blocked a screenshot. Fixed camera-relative at a constant IMPOSTOR_DIST=1500 (also the more honest physics: zero parallax at Mly distances, the band own infiniteDistance reasoning) and regression-proofed with PIXELS: aimAt(impostor) testhook + screenshot luminance/variance/bright-pixel floors in frame-ladder.spec (2/2 green in isolation). Two review suspicions CLEARED against sources: ShaderMaterial.clone DOES copy stored uniform values (Babylon 8 source read), and maxZ=6000 clears frustum clipping. Rode along: HUD farField O(4829) find per idle re-render (7.5Hz aim readout) -> module-level one-entry cache, deliberately NOT a useMemo (would sit below the engineReady early return -- the TR-086 hooks-order class). Re-gate: 680/680 unit, astro check 0/0/0, budgets green, no test weakened (pixel assertion purely additive). The review lesson recorded for D9/later: any backdrop mesh must reason about its distance from the CAMERA vs the band shell 2000, not from the origin."
  },
  {
    "project": "ishanjain-portfolio",
    "deliveryPlan": "PF-11-cinematic-journey-and-scale-honesty",
    "phase": "D2.3 -- constellation dissolve + license ledger completion (Phase D2 close)",
    "detail": "New ship-dynamics.ts figureVisibility(ly) = 1-smoothstep(FIGURE_FADE_START_LY=50, FIGURE_GONE_LY=500, ly) -- same shape as furnitureVisibility, a separate nearer window than localFieldVisibility's binary 1e6-ly collapse. Engine: _figureFade/_figureFadeFrom/_figureFadeTo fields, captured in _beginWarp, scheduled each frame in _updateFrameLadder through the existing frameLadderFade curve. No new shader uniform -- the figures already drive their alpha CPU-side in _pushAberration's uColor call, so this slice adds one more multiplicand (_figureFade) to the existing (0.34*(1-beta)*_localFieldFade) expression. sceneStats gains figureFade. tests/e2e/frame-ladder.spec.ts gains a Polaris (433 ly) leg proving a partial mid-band fade plus figureFade assertions at every existing checkpoint (Mars=1, M42=0 on its own schedule while localFieldFade stays 1 there, nbg=0, home=1 restored); timeout 150s->180s for the added leg. tests/unit/frame-ladder.test.ts gains a figureVisibility describe block (full/gone/monotonic/partial-at-Polaris/independent-of-localFieldVisibility) plus a source-string pin on the _conMat alpha expression. License ledger completed: dated addendum on the science brief (L14 belt overbrightness formalised, L15 seconds-scale warp-duration compression derived from warpDurationForLy against the brief's brachistochrone table, L16 band+star-field persistence narrowed to exclude the figures, L17 NEW -- the ly 50-500 window itself declared a scene-wide approximation, not a per-figure derivation) cross-referencing L9 (intro timing, D1.3) and L1 (Reinhard, D6.1) rather than duplicating them",
    "status": "complete",
    "start": "2026-07-24T00:00:00Z",
    "end": "2026-07-24T00:00:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "TR-091 + Astra REALISM-AUDIT (D2.3 section) appended to docs/analysis/2026-07-23-pf11-d2-frame-ladder-realism-review.md, ledger addendum appended to docs/analysis/2026-07-22-pf11-sky-frames-and-travel-science-brief.md. PHASE D2 IS NOW COMPLETE (D2.1 furniture gating + D2.2 extragalactic collapse, both TR-090; D2.3 this slice). THE SCOPE DECISION THAT SHAPED THE WHOLE SLICE: D2.2's constellation alpha was already multiplied by _localFieldFade (fading the figures at 1e6 ly along with everything else), so the honest question was whether that counted as 'done' -- it does not, because Astra brief section 1 calls the figures parallax accidents of NEARBY stars specifically, meaning they go wrong at destinations two to three orders of magnitude nearer than an extragalactic arrival. Split into its own _figureFade rather than widening the existing one, so D2.2's mechanism (still correctly serving the band+star-field extragalactic collapse) is untouched. LEDGER COMPLETION WAS THE LARGER HALF OF THIS SLICE: the implementation plan asked for 'the 4 from the brief + Reinhard ratio compression' as a dated addendum, and the honest way to do that without duplicating existing content was to formalise the three genuinely-new entries (L14-L16) and CROSS-REFERENCE the two already ledgered at their point of origin (L9 intro timing at D1.3, L1 Reinhard at D6.1) rather than re-deriving them. ASTRA'S KEY FINDING: the ly 50-500 window is NOT a derived physical threshold -- real constellation-figure stars span roughly 80 ly (Merak) to 2,600 ly (Deneb), so a scene-wide constant necessarily over-fades near figures (Ursa Major members ~80-125 ly: already ~36 deg parallax-distorted AT the fade-start threshold, computed via the small-angle Delta-theta ~= x/d formula) and under-fades far ones (Orion members ~1,260-2,000 ly: only ~23 deg distorted AT the fade-gone threshold) -- graded DECLARED LICENSE (new ledger entry L17), not ACCURATE, with the numbers recorded so a future per-figure refinement has a starting point rather than needing to re-derive why the window exists. TEST DESIGN: the existing Mars(=1)/M42(=0) checkpoints alone would have proven the endpoints but not the actual ly 50-500 mechanism, so a Polaris (433 ly) leg was added specifically to land inside the dissolve band and assert a value strictly between 0 and 1 -- the test that would have failed against a naive 'just reuse localFieldFade' implementation. MANUAL REGRESSION hit the SAME documented backgrounded-tab throttle every PF-11 travel-cinematic slice since D6.4 has recorded (document.hidden/hasFocus false when the MCP Browser pane isn't foregrounded -- rAF throttled, travelTo never reaches arrival, page.screenshot times out); confirmed boot console-clean on real WebGPU with figureFade=1 correctly initialised at idle, and relied on frame-ladder.spec (Playwright/SwiftShader, no foregrounded-tab concept) for the actual behavioural ladder proof, consistent with TR-088/089/090's documented workaround."
  }
]
```
