# Delivery Timesheet — PF-11 Cinematic Journey & Scale Honesty

Plan: [docs/delivery-plan/PF-11-cinematic-journey-and-scale-honesty.md](../delivery-plan/PF-11-cinematic-journey-and-scale-honesty.md)

```json
[
  {
    "project": "ishanjain-portfolio",
    "deliveryPlan": "PF-11-cinematic-journey-and-scale-honesty",
    "phase": "PLAN — plan authoring + audits",
    "detail": "Author the PF-11 plan from the PF-10 tail + 14 owner requirements: Astra science brief (docs/analysis/2026-07-22-pf11-sky-frames-and-travel-science-brief.md), 10 parallel codebase audits (band/belt gating, arrival seam, cards, where-to console, travel physics, loading stages, perf/memory, PF-10 remainders, DR3 Tiny inventory), gaiasky.space + texture-licensing research into docs/research/, experience-designer + user-research passes",
    "status": "complete",
    "start": "2026-07-22T00:00:00Z",
    "end": "2026-07-22T00:00:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "Same recorded exception as PF-09 B0 / PF-10: no plan-specific scorecard; milestone card governs; re-run per-slice at IMPLEMENT. Same session also delivered TR-080 (the two owner-reported E2E failures root-caused and fixed; full suite 86/87 with the one failure A/B-confirmed as the documented cumulative-load class, promoted to plan slice D0.1) and CLAUDE.md non-negotiable #25 (per-slice full gate + manual regression). IMPLEMENT not started — awaiting owner confirmation and the D3.3/D6.x owner decisions listed in the plan."
  },
  {
    "project": "ishanjain-portfolio",
    "deliveryPlan": "PF-11-cinematic-journey-and-scale-honesty",
    "phase": "PLAN — owner decisions + implementation companion",
    "detail": "Record the owner decision set (ADR-0010): goHome=abort + retarget queue (D3.3), Reinhard curve (D6.1), asset-weight unblocked via opt-in layers (D6.5), RANDOM JUMP/RETURN HOME renames + Earth home-orbit R16 (D5.1), sourced owner-approved card drafts (D4.4), full 2.55M DR3 Tiny (D8.1); add the new R15 Render Console requirement as phase D9; author docs/implementation/PF-11-implementation-plan.md with bidirectional per-phase links",
    "status": "complete",
    "start": "2026-07-22T00:00:00Z",
    "end": "2026-07-22T00:00:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "Plan status moved DRAFT -> CONFIRMED (owner decisions constitute confirmation; recorded additively in the plan). D6.2 (belt frame) explicitly remains open — the owner passed on it. New docs/implementation/ directory established with its README contract (delivery plan owns WHAT/WHY, implementation plan owns HOW, delivery plan wins on conflict). Sequencing updated: D7.1/D7.2 lifecycle work precedes D9; D8 ships as a D9 layer. IMPLEMENT may start at D0 next session."
  },
  {
    "project": "ishanjain-portfolio",
    "deliveryPlan": "PF-11-cinematic-journey-and-scale-honesty",
    "phase": "PLAN — D6.2 decision (belt frame)",
    "detail": "Owner approved GO on the belt-frame re-expression (hours after the ADR-0010 decision set, same day): belt model + data move into the obliquity-inclined basis, --frame equatorial regen, m42 route re-tune, Kirkwood regression must pass unchanged, Astra audit closes the slice",
    "status": "complete",
    "start": "2026-07-22T00:00:00Z",
    "end": "2026-07-22T00:00:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "Recorded additively in ADR-0010 (dated update), the delivery plan (Decisions log, D6 table, open-decisions list, status header), the implementation plan (D6.2 section now GO with a 5-step sequence, recommended alongside/after D2.1 which touches the same code paths), and all four affected README indexes. ALL PF-11 owner decisions are now taken; the only remaining choices are data-driven calibrations (D9.4 presets, boot-critical budget value). Owner: D0 implementation starts within hours."
  },
  {
    "project": "ishanjain-portfolio",
    "deliveryPlan": "PF-11-cinematic-journey-and-scale-honesty",
    "phase": "D0.1 — frame-production liveness (band build)",
    "detail": "Replace the Milky Way band's 20-rows-per-rendered-frame build with MilkyWayBandBuilder: time-budgeted slices paced against an 8s wall-clock deadline, capped at 50ms so frames outrank the band, yielding to an in-flight warp, driven by both the render loop and a setTimeout(0) chain. Audit the other bulk builders (none per-frame chunked). Recalibrate craft-ship.spec.ts's six 20s ready ceilings to one 45s constant. Expose bandProgress on sceneStats for D1.1",
    "status": "complete",
    "start": "2026-07-22T00:00:00Z",
    "end": "2026-07-22T00:00:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "TR-081. The implementation plan's prescribed mechanism (setTimeout(0) chain) was DISPROVED by measurement — 10 timer callbacks vs 12 rAF callbacks in 5s on the rendering page — and made the build 65s; corrected additively in the implementation plan, with a read-across warning for D7.3/D7.4. A heavy 8-worker load recipe was also disproved as an instrument: a stash-and-rebuild control showed the BASELINE flakes identically, so it measures SwiftShader starvation, not liveness. Verification at moderate 4-worker load plus one quiet run."
  },
  {
    "project": "ishanjain-portfolio",
    "deliveryPlan": "PF-11-cinematic-journey-and-scale-honesty",
    "phase": "D0.2 — planet-sphere pixel proof (handoff B4)",
    "detail": "Add aimAt(bodyId) harness-only camera-bearing hook (gated ?testhooks) to babylon-engine.ts, next to travelTo/goHome/randomBody/setStations. Add tests/e2e/planet-pixels.spec.ts: screenshot the canvas post-arrival-and-aim, assert luminance+variance floors on a centred crop for Mars and Moon. Capture and commit the first-ever reference pixel proofs of a planet sphere",
    "status": "complete",
    "start": "2026-07-22T00:00:00Z",
    "end": "2026-07-22T00:00:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "TR-082. Root cause found this session, after five prior sessions of engine-state-correct-but-no-pixels: camera standoff was always right, orientation never was — _tickWarp's idle branch re-derives _camQuat from free-look _yaw/_pitch every frame, and nothing had ever pointed them at the arrived body. Full gate 561/561 unit / 88/89 E2E (one pre-existing D3.2-scoped GAP-17 flip-window flake, confirmed unrelated by diff scope) / budgets green. Manual regression again hit this session's MCP Browser-pane backgrounded-tab limitation (same as TR-081); worked around with a throwaway scripted-chromium pass against the built preview, deleted after use. Earth-class screenshot deferred to D6.4 per this slice's own exit note."
  },
  {
    "project": "ishanjain-portfolio",
    "deliveryPlan": "PF-11-cinematic-journey-and-scale-honesty",
    "phase": "D0.3 — real-device pass, round 3 (owner-involved)",
    "detail": "Started a LAN-reachable preview server (astro preview --host) so the owner could test from Galaxy S24 Ultra and Galaxy S20 FE over Wi-Fi. Owner reported ?perf=1 cold-load readings for desktop and both phones. Validated against CLAUDE.md's instrument checks, cross-referenced against PF-09's two prior device-pass rounds (TR-068/069), and recorded in TR-083",
    "status": "complete",
    "start": "2026-07-22T00:00:00Z",
    "end": "2026-07-22T00:00:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "TR-083. This is round 3 of PF-09's still-open B6 device gate, not a fresh measurement in isolation — TR-068 (round 1) and TR-069 (round 2) had established a reproducible flagship-underperforms-mid-tier anomaly (S24 Ultra losing to S20 FE on render fps both times) with the root cause (DPR/tier/backend) unconfirmed because the overlay's ENGINE/TIER/device-signature line was never reported. This round REVERSED the anomaly (S24 Ultra 60fps vs S20 FE 45fps, both now clearing the >=40fps floor) but the SAME line went unreported a third consecutive time, so the reversal is real but still unexplained. No instrument-validity check rejected any reading. Did not unilaterally close B6 — A1 (desktop) still fails 60fps, A2 (iPad)/A4 (WebGL2 fallback) remain untested, same non-rubber-stamping standard TR-068/069 set. D0.3's own exit criterion (real per-device numbers feeding D6.3/D8/D9.4) is satisfied regardless of B6's separate status. Updated in the same pass: PF-09 delivery-plan status header (additive), the B6 cutover checklist rows A1/A3/E.5, PF-11's own status header, and both timesheets."
  },
  {
    "project": "ishanjain-portfolio",
    "deliveryPlan": "PF-11-cinematic-journey-and-scale-honesty",
    "phase": "D1.1 — real progress instrumentation (the honesty contract)",
    "detail": "New pure module src/lib/load-progress.ts: fetchWithProgress over the ReadableStream reader (throttled 4Hz, unthrottled done, no-body fallback), StageAggregator summing multi-URL stages, BOOT_CRITICAL_ASSETS. New additive cosmos:stage event; cosmos:progress untouched. Nine stages wired into babylon-engine.ts (star-catalog, atlas-map, craft-glb, havok-wasm, bonus-layers, sdss-field, asteroid-belt + engine-init/first-frame checkpoints). budgets.config.mjs gains bootCriticalDownloadKB, unit-asserted against real on-disk bytes",
    "status": "complete",
    "start": "2026-07-22T00:00:00Z",
    "end": "2026-07-22T00:00:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "TR-084. ROUTED PER THE SCORECARD'S OWN SPLIT for this slice ('pure module is Sonnet-grade; _boot touchpoints are Opus'): load-progress.ts + its unit tests were built on Sonnet 5 high, then the owner switched to Opus 4.8 high for the engine wiring, which is TR-059-guarded hot boot path. TWO ASSETS WERE STRUCTURALLY UNOBSERVABLE BEFORE THIS SLICE and are the substantive findings: Havok's 2.09 MB WASM (Emscripten fetches it INSIDE the factory where nothing can watch) — solved by streaming it here and handing over the buffer via wasmBinary, with the VENDORED RUNTIME PROBED FIRST (HavokPhysics_es.js's getBinarySync returns new Uint8Array(wasmBinary) before any network path) to confirm no double fetch rather than assuming it, per TR-081's probe-before-building lesson; locateFile deliberately KEPT alongside so a future Havok bump that ignores wasmBinary degrades to fetching from the same origin — losing progress is acceptable, losing the ADR-0005 origin guarantee is not. And the craft GLB, where ImportMeshAsync's onProgress had simply never been passed. EVERY reported byte total equals its file's real on-disk size byte-for-byte (verified in both E2E and the manual pass). Stage-closing discipline was the non-obvious correctness requirement: several engine paths degrade gracefully rather than throwing, so every aggregator closes in a finally with an idempotent finish(), and the no-WebGL branch explicitly closes all four boot-critical stages (verified manually — that is D1-AC7's foundation). THREE SCOPE POINTS RECORDED ADDITIVELY rather than resolved silently: the plan's prose names two cosmos:stage emitters its own type block omits (band progress already has a real signal, sceneStats().bandProgress, handed over by TR-081 — a band LoadStage would have created a second source of truth); atlas-photo (3.5MB) deliberately deferred because Babylon's Texture exposes completion only and wiring it means re-routing the GAP-02 layer through a blob: URL that deserves its own verification; D1-AC1 deferred to D1.2 since 'visible progress' asserts a rendered surface D1.1 does not ship. Full gate 572/572 unit (+11) / 93/93 E2E FULLY GREEN (first since TR-081; includes GAP-17 which failed in D0.2's run — still D3.2's to fix, its passing here is not evidence it is fixed) / budgets green. A comment-only edit landed after the suite started; rather than claim comments cannot matter, dist/_astro's 233 emitted JS chunks were hashed before and after a rebuild and proven byte-for-byte identical, so no re-run was needed. Manual regression again went through a scripted chromium — the MCP Browser pane's backgrounded-tab limitation has now recurred three slices running."
  },
  {
    "project": "ishanjain-portfolio",
    "deliveryPlan": "PF-11-cinematic-journey-and-scale-honesty",
    "phase": "D1.2 — PRE-FLIGHT dossier UI (loading becomes a real gate)",
    "detail": "New PreFlight.tsx + preflight-state.ts (pure state machine, stall detection, byte formatting) + shared focus-utils.ts (moveFocusTo/restoreFocusTo, D4.3 reuses it). Replaces the automatic body.ij-loading clear (ready&&craftDone with 2.5s/8s grace timers) with a real gate: LAUNCH (disabled/ARMING% until every boot-critical cosmos:stage closes, then auto-focuses), SKIP INTRO (available from mount per the plan's own wording, not the state diagram's S2-only reading), a 10s stall detector showing PROCEED ANYWAY, and a sessionStorage returning-visitor bypass",
    "status": "complete",
    "start": "2026-07-23T00:00:00Z",
    "end": "2026-07-23T00:00:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "TR-085. Built on Sonnet 5 high per the scorecard's own D1.2 routing (React chrome). JUDGMENT CALL recorded: the experience review's state diagram draws SKIP INTRO as an S2-armed-only transition, but its own prose ('as soon as boot-critical stages allow') and this delivery plan's own D1.2 text ('a SKIP INTRO link during loading') say otherwise, and the review's risk section names pre-arm availability as the explicit mitigation for the 10-20s hotel-Wi-Fi gate risk -- implemented per the prose, against the diagram, recorded rather than silently picked. TWO REAL DEFECTS FOUND DURING VERIFY, one by each half of the gate: (1) unit tests caught moveFocusTo stamping tabindex=-1 UNCONDITIONALLY rather than only when needed -- would have marked every focus target including LAUNCH itself, caught by focus-utils.test.ts's first assertion before any browser ran it; (2) the full E2E suite caught the pre-existing 9s CSS no-JS/hydration failsafe firing during ENTIRELY NORMAL operation once loading became visitor-gated (craft alone is calibrated up to 45s elsewhere in this suite) -- it was force-revealing content out from under a still-working PreFlight. Fixed by scoping the failsafe to body.ij-loading:not(.ij-preflight-mounted), a body class PreFlight sets on mount; the 9s VALUE stayed exactly as instructed, only the CONDITION was corrected, and only because measurement (not a guess) showed it wrong. FIVE NAMED E2E TEST CHANGES, all one root cause: #ij-preflight structurally shares its screen region with #ij-mission-bar (RNG button) and the viewport-center coordinate two pre-existing free-look-drag tests use -- by design, both occupy the same lower-hero dock at different phases. Fixed by adding a SKIP INTRO dismissal (what a real visitor would do) ahead of each affected interaction; no assertion changed. THREE FULL-SUITE RUNS this session (CLAUDE.md #27's budget, used in full): 97/101 -> 99/101 -> 100/101, each round finding one more instance of the same collision class before the final gate closed on only the pre-existing D3.2 GAP-17 flake. SCOPE CALL: no separate Astro/SSR shell built for identity -- every other scene-chrome surface in this codebase (HUD, WarpOverlay, ArrivalVista) is React-only too, and PreFlight renders identity on mount (client:load hydrates near-instantly); revisit only if a measured hydration-gap ever shows it matters. Full gate 595/595 unit (+23: 15 preflight-state, 8 focus-utils) / 100/101 E2E / budgets green (JS +2KB). Manual regression again via scripted chromium -- fourth slice running into the MCP Browser pane's backgrounded-tab limitation."
  },
  {
    "project": "ishanjain-portfolio",
    "deliveryPlan": "PF-11-cinematic-journey-and-scale-honesty",
    "phase": "D1.4 -- first-party funnel instrumentation (?funnel=1)",
    "detail": "New pure funnel.ts (FunnelRecorder with ring-buffer localStorage persistence keyed by a monotonic session id, resolveFunnelOverlay mirroring resolveEngine's URL->stored->default shape, derived-flag helpers: armed->press delta, accidental-warp detector, zero-result searches, isPlausibleSession) + FunnelOverlay.tsx (debug panel, zero runtime style props). Recorder always runs (window.__ijFunnel(), same convention as __ijPerf); only the visible overlay is URL/stored-gated. Milestones wired at the real cosmos:* listeners/UI callbacks already in SpaceScene.tsx",
    "status": "complete",
    "start": "2026-07-23T00:00:00Z",
    "end": "2026-07-23T00:00:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "TR-086. Sequencing note: the delivery plan lists D1.4 ahead of D1.3 in its own document order (the implementation plan lists D1.3 first); D1.3 has not shipped, so this slice was scoped to work correctly either way -- a defensive cosmos:ascent-done listener records cinematic-done additively, and simply never fires until D1.3 lands. TWO REAL BUGS FOUND, neither anticipated by the plan: (1) a React error #310 hooks-order crash on EVERY page load -- the first version recorded search-keystroke from a useEffect placed after SpaceScene.tsx's pre-existing 'if (!engineReady) return null' early exit, making the hook run conditionally; every E2E spec failed identically (babylon-scene never appearing) until a throwaway page.on('pageerror') probe surfaced the real error, not the symptom -- fixed by extracting a top-level computeSuggestions(cmd) function and recording synchronously inside the existing onCmdChange callback instead of adding an effect; (2) cosmos:warp proved structurally unable to detect its own start -- cosmos:select's own handler already sets state.warp to the aim-phase object before the first cosmos:warp tick arrives (TR-081: cosmos:warp fires once per rendered frame for the WHOLE journey, not once at its start), so a null-check there could never fire -- fixed by moving the travel milestone to cosmos:select's non-quiet branch instead, which is if anything a MORE accurate timestamp for the accidental-warp detector. NAMING DECISION recorded: the recorder stores repeatable action names (travel, search-keystroke, vista-dismissed, ...) rather than the plan's literal first-X labels, because vista-dismissed structurally needs every occurrence (not just the first) to drive the accidental-warp detector against a LATER dismissal -- firstEventTime() recovers the plan's exact first-X semantic on demand for display; the four genuinely one-shot gate milestones (dossier-visible/launch-armed/launch-pressed/cinematic-done) DO dedupe to their first occurrence. ALSO FOUND, DELIBERATELY NOT FIXED: a pre-existing z-index overlap between #ij-mission-bar (z-62) and ArrivalVista's button row (z-61) -- no prior spec had ever clicked OPEN COLLECTOR CARD before this slice's E2E did, and a real coordinate-based click there (even with force:true, which only skips Playwright's own actionability check, not the browser's real hit-testing) lands on the WHERE-TO input instead; worked around in the E2E spec with a direct element.click() dispatch, handed to D4.1 (whose vista-dismissal rework replaces the whole interaction) rather than fixed out of scope here. FOUR ISOLATED FIX-VERIFY CYCLES during VERIFY (CLAUDE.md #27's economics -- single-spec isolation runs, never a full-suite re-run, for all four): the two bugs above, a test-timing race against the vista's real 5.5s auto-timeout (tightened by clicking OPEN COLLECTOR CARD the instant it appears rather than after two separate polls), and the click-interception fix. Full gate 619/619 unit (+24, all funnel.test.ts) / 107/108 E2E (+7 new; the one failure is D3.2's already-documented GAP-17 flake, confirmed unrelated via 3 isolation runs: 2 passed, 1 failed with the identical intermittent signature) / budgets green (JS +1.4KB). Manual regression again via scripted chromium -- fifth slice running into the MCP Browser pane's backgrounded-tab limitation, first noted TR-081."
  },
  {
    "project": "ishanjain-portfolio",
    "deliveryPlan": "PF-11-cinematic-journey-and-scale-honesty",
    "phase": "D6.1 + D6.3 -- exposure, the linear pipeline, tier-gated planet textures",
    "detail": "planet-sphere.ts: sRGB decode of surfaceTex only (exact piecewise EOTF both twins), opposition surge relocated from uAlbedo into refl via a new uSurgeB0 uniform + PLANET_SURGE/exposureTermsFor, Reinhard L/(1+L) before a single output encode, EXPOSURE_GAIN 3.6->1.9, relief self-shadowing gated on uHasHeight/uHasNormal. babylon-tiers.ts: QualityBudget gains planetTexture (base/high/ultra-progressive). babylon-engine.ts: tier-aware first rung + ultra fetched only at the top tier, uAtmosphere reads a real manifest flag. build-planet-textures.mjs: atmosphere declaration + METADATA_FIELDS/syncManifestMetadata reconcile",
    "status": "complete",
    "start": "2026-07-23T00:00:00Z",
    "end": "2026-07-23T00:00:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "TR-087 + REALISM-AUDIT docs/analysis/2026-07-23-pf11-d6.1-exposure-realism-review.md. SHIPPED AS ONE SLICE because the implementation plan requires it -- D6.3.1's sRGB decode moves every number the D6.1 Reinhard decision was priced against, so landing them apart would have meant tuning the exposure twice and throwing the first answer away. MEASURED BEFORE CHANGING, and validated the instrument first: re-derived the cos-latitude-weighted mean luminance of every shipped surface map and reproduced Astra's independent A2.3 table TO THREE DECIMALS on all nine bodies both covered, before trusting any number derived from it. That instrument then found what A2.3 could not: EIGHT bodies clip today, not six -- Saturn (1.500) and Jupiter (1.207) were never in the brief's table, which sampled nine of sixteen shipped bodies. Recorded additively. THE EXIT CRITERION AS WRITTEN WAS UNACHIEVABLE and that is the substantive finding: 'six clipping bodies < 1.0; Moon/Mars unchanged within ~5%' cannot hold with the gain left at 3.6, because Reinhard alone takes the Moon -22% and the decode moves it further -- so the gain was SOLVED (3.6 -> 1.9) rather than tuned, landing Moon -2.8% and Mars -0.5% with nothing clipping at all. A single shared gain was chosen over per-body multipliers deliberately: per-body gains destroy the ratios, which is the one thing the exposure section exists to protect. THE SURGE RELOCATION IS UNIT-ASSERTED AS LOSSLESS -- base*(1+B0) reconstructs each published geometric albedo exactly at zero phase, which is what makes it a relocation rather than a re-grade; Mars is excluded because its Bond albedo EXCEEDS its geometric albedo so its coefficient would be negative (Astra: 'Leave Mars alone'), and that exclusion is itself a test. THE sRGB DECODE'S EXCLUSIONS were the careful part, not the decode: cloudTex and specularTex are coverage fractions (Astra had already validated the cloud map's RAW byte against Earth's real geometric albedo -- 7% closure vs 33% miss if decoded) and heightTex/normalTex/detailTex carry elevation and vectors, so a blanket 'decode every texture' would have silently corrupted four of five samplers. TWO DEFECTS CAUGHT BEFORE SHIPPING: an existing guard caught a literal backtick in the WGSL source on the first unit run (TR-078 Part 5's silent-compile-failure trap, from an escaped-backtick comment inside the template literal), and astro check caught a type error in this slice's own new test -- worth recording that my first verification command piped npm run build through tail, which MASKS the pipeline exit code, so budget:check ran anyway and the type error nearly slipped past; the repo's gate is fine, the invocation was mine. A STRUCTURAL PIPELINE GAP FIXED ON THE WAY: --if-stale judges staleness on image mtimes and was therefore blind to a declaration change that adds a manifest field without touching a pixel, so the uAtmosphere flag could not land without either re-emitting all 88 images or hand-editing a generated file (CLAUDE.md #22) -- fixed with a syncManifestMetadata reconcile pass sharing one METADATA_FIELDS list with the full-build path so the two modes cannot drift. E2E HONESTY: run 1 was 106/108 with both failures in the exact subsystem this slice changed, so they were NOT written off as the known flake -- four isolation experiments (the two specs alone 2/2; engine-select alone 32/32; the exact 51-test preceding prefix 51/51) plus a fully green 108/108 run 2 establish they are intermittent and load-dependent, and the TR states plainly that no baseline A/B was run so 'pre-existing' is inherited from prior TRs rather than demonstrated. No test was modified. Full gate 635/635 unit (+16) / 108/108 E2E / budgets green with no asset re-emitted. Pixel evidence captured to NEW d6.1-named files rather than through the spec's PF11_CAPTURE_REFERENCE flag, because overwriting the committed D0.2 references would have made TR-082 appear to show an exposure it never proved -- corrections are additive, including visual ones. Tethys and Dione now render full-disc crater detail where they previously clipped to flat white; Mars is visually indistinguishable from its D0.2 reference, confirming the -0.5% prediction by eye. Manual regression via scripted chromium -- sixth slice running into the MCP Browser pane's backgrounded-tab limitation."
  },
  {
    "project": "ishanjain-portfolio",
    "deliveryPlan": "PF-11-cinematic-journey-and-scale-honesty",
    "phase": "D6.4 -- Earth goHome reveal at 90 degree phase + the R16 home orbit",
    "detail": "Night-light assets resurrected (MAP_TIER_CAP per-map tier ceiling so night ships base+high; PACK_RECKONING entry preserved and superseded in place; ADR-0009 ceilings raised 169.5->170.0 and 256.5->257.0). Night-lights shader term in both twins, masked on the true N.L, floor-subtracted, composited in linear before Reinhard. Earth-at-origin sphere path keyed on the home orbit rather than arrivedId, with uSunDir read from the Sun own catalog entry. homeOrbitPosition() pure orbit math + HOME_ORBIT_* constants; _isAtHomeVantage() replacing the |cam|<1 predicate in BOTH places it lived. New tests/e2e/earth-home.spec.ts (5 specs) + 11 unit tests",
    "status": "complete",
    "start": "2026-07-23T00:00:00Z",
    "end": "2026-07-23T00:00:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "TR-088 + REALISM-AUDIT docs/analysis/2026-07-23-pf11-d6.4-earth-home-realism-review.md. UNBLOCKS D1.3 -- which was the point of the owner request, and worth noting the request said it unblocks D1.4: D1.4 shipped earlier the same session (TR-086) and the plan sequencing line is \"D6.4 must precede D1.3\". HAD TO FOLLOW TR-087, not precede it: city lights added AFTER a tone map are additive in display space and clip to flat white blobs with no structure, so they must composite in linear before the curve -- a unit test now pins that ordering in both twins rather than leaving it to be rediscovered. THE OWNER DECISION CAME IN FAR UNDER BUDGET: base+high night lights measured 0.25 MB (0.05 + 0.20) against the ~1.8 MB approved and ~6.4 MB for three tiers, because the map is mostly black and JPEG compresses it to nearly nothing -- so the ceiling raise was 0.5 MB, not 2. THREE REAL DEFECTS FOUND AND FIXED, each worth its own note: (1) the reveal fired AT BOOT, where cam=[0,0,0] is INSIDE the radius-26 sphere, and pulled ~2.5 MB of Earth textures into startup on every page load -- caught by reading probe output, and the uncomfortable part is that the E2E I had already written ASSERTED THE BUGGY BEHAVIOUR because I wrote it from what I observed rather than from the contract; the spec was corrected to assert the contract and now fails against the old behaviour; (2) the orbit ran 8x SLOW (1.5 deg in 6s against the 12 deg a 180s period implies) because the idle branch used the SHIP_MAX_DT-clamped dt, and the fix was ALREADY DOCUMENTED twenty lines below in the same function where the warp integration explains that progress must track wall clock while only the physics step is clamped -- switched to _dtWarpS, re-measured 16 deg in ~6s; (3) a canvas tabindex=1 axe violation (CLAUDE.md #23) whose window is BEFORE the first rendered frame, which a per-frame re-assert structurally cannot close -- now a MutationObserver, disconnected on teardown, so an accessibility contract no longer depends on frame pacing; pre-existing race that this slice larger shader made more likely rather than one it introduced, and said so. NAMED TEST CHANGE (#15): the \"does NOT ship Earth night lights\" assertion -- described in its own file as the highest-value assertion there -- is INVERTED, on the precondition PACK_RECKONING itself named (\"Ship it if the arrival geometry ever changes\"). Every word of the original reasoning is still true about travelTo; what changed is the geometry. The test now pins \"no night map WITHOUT the 90 degree vantage that earns it\", the reckoning entry is preserved rather than deleted, and a NEW test recomputes the 90.0000 degree phase from the Sun own catalog entry so the lights lose their licence the moment that geometry moves. THREE FULL-SUITE RUNS (CLAUDE.md #27 budget, used in full) -- and one of them was WASTED because npm run build failed on an unformatted doc and Playwright silently served a stale dist, which is #27 corollary paid in full and recorded as such. Final gate 646/646 unit (+11) / 112/113 E2E, the one failure being the WGSL hardware spec timing out at its 30s ceiling in a run that took 22.1 min against the usual 14-18 (loaded machine), confirmed 3/3 in isolation at 4.3s. CARRIED FORWARD, flagged by the realism audit rather than left implicit: Earth PLANET_LUNAR_L=0 and albedo 0.213 were BOTH fitted at alpha=0 and are now rendered at alpha=90, which the Earth brief predicts is a 37-67% error -- wants Astra numbers before D1.3 reuses this geometry. Manual regression via scripted chromium (seventh slice hitting the MCP Browser pane backgrounded-tab limitation): boot console-clean, near/far travel, dossier, SOL reveal showing sphere=earth at phase=90 with the orbit advancing 14->46 deg, classic-view toggle, 0 console/page errors. First terminator any body in this scene has ever shown, captured in docs/test-reports/assets/pf11-d6.4-earth-home.png."
  },
  {
    "project": "ishanjain-portfolio",
    "deliveryPlan": "PF-11-cinematic-journey-and-scale-honesty",
    "phase": "D1.3 -- launch-from-Earth ascent + console reveal (Phase D1 cinematic close)",
    "detail": "Vega-discipline motion-spec authored before IMPLEMENT (docs/experience-design/2026-07-23-pf11-d1.3-ascent-motion-spec.md). New pure ascent.ts (easeCam=k^3 vs easeAlt=smoothstep, skyMix=exp(-h/8.5), starFade, limbIntensity, + limb-glow shader twins). Engine: warp.mode=ascent + _tickAscent (camera along the home sight-line, sky onto scene.clearColor, band uFade, fresnel-rim limb shell), beginAscent/skipAscent, wall-clock dt, handoff re-arms the home orbit + emits cosmos:ascent-done. Host: onLaunch(ascent) threads the LAUNCH-only cinematic; console reveal deferred to ascent-done; AscentSkip overlay. global.css dock formalized + reveal rise. sceneStats ascent fields",
    "status": "complete",
    "start": "2026-07-23T00:00:00Z",
    "end": "2026-07-23T00:00:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "TR-089 + MOTION-SPEC (authored pre-IMPLEMENT, Vega discipline, Procyon) + Astra REALISM-AUDIT docs/analysis/2026-07-23-pf11-d1.3-ascent-realism-review.md. CINEMATIC SLICE, so both sisters paired as the operating model prescribes: the Astra physics pre-existed as the sky-frames brief §4 (authored FOR this feature), the motion design was authored fresh before writing engine code, and both were audited/confirmed after. THE BACKBONE INVARIANT: LAUNCH is the D6.4 goHome reveal played FORWARD -- same sphere, same 90deg terminator geometry, same final pose -- so the ascent end standoff is UNIT-ASSERTED equal to ARRIVE_STANDOFF/HOME_ORBIT_RADIUS and post-launch == post-goHome as one frame; confirmed by eye (the handoff screenshot is visually identical to a goHome arrival). REQUEST-FRAMING NOTE recorded: the owner asked D1.3 to unblock D1.4, but D1.4 already shipped earlier the same day (TR-086); the plan sequencing is D6.4-precedes-D1.3 and D6.4 (TR-088) is what actually unblocked this -- D1.3 completes Phase D1 rather than unblocking D1.4. FIVE SCOPE DECISIONS recorded not silently resolved: the ascent is a RADIAL climb not a lateral flight (vertical-from-surface IS radial in a geocentric frame); the star field has no fade uniform so only the band uFade is driven and the star reveal emerges from the darkening background (cheaper AND more physical, no ijStar twin change); the limb ring is a fresnel-rim SHELL not a billboard arc (declared SIMPLIFIED, sun-gated); progress uses wall-clock _dtWarpS not the clamped dt (D6.4 orbit lesson applied pre-emptively -- measured ~12-14s on SwiftShader vs ~8s real, stretched never sped up); only the real LAUNCH plays the ascent (skip/bypass go straight to content). FIVE NAMED TEST CHANGES (CLAUDE.md #15), all one cause: D1.2 preflight tests asserted ij-loading clears IMMEDIATELY on LAUNCH, but D1.3 inserts the ~8s ascent before the reveal -- widened the reveal-timing window (no assertion weakened; the animated reveal itself is covered by the new ascent.spec.ts), and dropped the returning-visitor test first-launch wait (sessionStorage is set synchronously in PreFlight.launch). A TEST-DESIGN LESSON recorded: the skip E2E first failed with element-detached because sceneStats() evals take MULTIPLE SECONDS each under SwiftShader load (one measured 12s), so a stats-based precheck consumed enough wall-clock that the ascent completed inside it -- fixed by detecting ascent state through the synchronously-rendered SKIP overlay (React, no eval) instead. ASTRA ESCALATION carried forward: Earth PLANET_LUNAR_L=0 and albedo 0.213 were fitted at alpha=0 and D1.3 is now the SECOND feature rendering Earth at alpha=90; the D6.4 review asked this be closed before D1.3 built on it, it was not, so the audit escalates -- blocks nothing shipped but should block the next Earth-visual change. Full gate 665/665 unit (+19 ascent.test.ts) / full E2E green after the named preflight changes (the 3 failures were exactly the reveal-timing change, verified fixed) / budgets green (JS +5KB). Manual regression via scripted chromium (eighth slice hitting the MCP Browser pane backgrounded-tab limitation): LAUNCH plays the ascent console-clean, hands off to the Earth home vantage at phase 90, console reveals -- captured in docs/test-reports/assets/pf11-d1.3-ascent-done.png; mid-ascent visual (blue-black sky, limb arc) needs real hardware, flagged owner-confirmed. The two cheapest UX validations the plan pre-build-gates (hallway x5, dossier-vs-spinner) are human studies, owner-run, flagged not blocking."
  },
  {
    "project": "ishanjain-portfolio",
    "deliveryPlan": "PF-11-cinematic-journey-and-scale-honesty",
    "phase": "D2.1 + D2.2 -- the frame ladder (solar-system furniture gating + extragalactic collapse)",
    "detail": "Shared uLayerFade mechanism: ijStar fragment twins (GLSL+WGSL) gain a per-layer fade multiply; main star field -> _localMat clone, belt -> _beltMat clone, SDSS stays on base _starMat at 1. Clones kept in lockstep on uTime/aberration/uViewport/uHaloAmp. New ship-dynamics.ts pure fns (furnitureVisibility, localFieldVisibility, frameLadderFade + thresholds); _updateFrameLadder drives fades from destination lyTotal+prog captured in _beginWarp. _tickAsteroids sleeps (skip forces + gate collision-shake) on _beltPhysicsAwake. _tickMilkyWay uFade = bandFadeAmt x _localFieldFade every frame. Constellation alpha x _localFieldFade. New procedural (license-clean) impostor: milkyWayImpostorPixel/buildMilkyWayImpostor + ijImpostor shader twins + _setupImpostor/_updateImpostor (billboard quad, real angular size floored, astern of the target). HUD LOCAL FIELD BELOW RESOLUTION line via SpaceScene farField prop. sceneStats gains furnitureFade/localFieldFade/beltPhysicsAwake/impostorVisible/impostorTextureReady; bandFade now effective opacity. New tests/unit/frame-ladder.test.ts (15) + tests/e2e/frame-ladder.spec.ts; webgpu-hardware.spec gains an extragalactic-hop impostor-WGSL check",
    "status": "complete",
    "start": "2026-07-23T00:00:00Z",
    "end": "2026-07-23T00:00:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "TR-090 + Astra REALISM-AUDIT docs/analysis/2026-07-23-pf11-d2-frame-ladder-realism-review.md. SHIPPED AS ONE SLICE because D2.1 and D2.2 share the uLayerFade fade mechanism (the plan's own 'shared mechanism first' note) -- same reasoning D6.1+D6.3 landed together. THE KEY ENGINEERING CALL: per-mesh fade is via MATERIAL CLONES, not material.onBindObservable -- on WebGPU a shared material's UBO uploads once, so a per-mesh set inside onBind would give every mesh the LAST value; the clone approach the plan specifies is the WebGPU-safe one, at the cost of fanning uTime/aberration/uViewport/uHaloAmp out to the clones (recorded in the impl-plan notes-as-built). SDSS deliberately stays on the base material at fade 1 (Astra: isotropic, effectively infinite -- never fades). THE IMPOSTOR IS PROCEDURAL, NOT THE PHOTOGRAPH: the Risinger Milky-Way pano is credited without a printed licence (the standing licensing tripwire), so it is NOT shipped -- milkyWayImpostorPixel builds an inclined disc from the band's OWN tone model instead, license-clean and declared SIMPLIFIED (ledger L12); its on-screen size uses the real angular subtense theta=2*atan(15kpc/d) floored to a visible minimum (ledger L13, same declared-overbright class as the belt). TWO HONEST SCOPE CUTS, realism-audited not silently dropped: (1) sun glare / planet billboards are NOT individually faded -- they share one billboard material with no per-body fade channel, and the dominant artefact (a 154,662-speck belt ring at every DSO) IS gated; (2) the Havok sleep skips the force loop + the collision-shake callback but does NOT yet halt integration (setMotionType STATIC / zero timestep), which is D7.5's measured refinement -- beltPhysicsAwake is the assertable signal either way. THREE E2E TIMING LESSONS: the band's boot fade-in is SLOW on SwiftShader (heavy scene, low fps), so the collapse is proven RELATIVELY (bandFade = ramp x collapse -> exactly 0 at nbg regardless of the ramp; restored >= pre-collapse at home) rather than waiting the ramp out; the goHome-from-extragalactic settle needed a 15s poll (all fade-settle polls given the same SwiftShader-load budget the D0.2/craft specs use); one early 5s-budget flake was load-induced settle, not logic (values correct 2/3), fixed by the timeout, then 3/3 stable in isolation. THE GATE WAS RUN UNDER SUSTAINED MACHINE LOAD (my own concurrent stability runs + the environment): BOTH budgeted full-suite runs took 27-29 min vs the usual 14-18 and hit ROTATING SwiftShader-contention failures -- run 1 failed NGC2000/mars/mobile-menu, run 2 failed a DIFFERENT pair accessibility/GAP-17 -- and ALL FIVE distinct specs pass in isolation (4/4 and 2/2 verified), the textbook single-emulator rotating-phantom class CLAUDE.md #16 names and TR-082/085/086/088/089 each recorded; GAP-17 is the already-documented flip-window flake. Characterized via isolation per #27, did NOT keep re-running the full suite to chase green. Full gate 680/680 unit (+15) / 119 E2E (+1, 3/3 in isolation) / budgets green (JS +5KB, no new shipped asset -- impostor texture is runtime-generated). MANUAL REGRESSION on REAL WebGPU via the MCP Browser pane: boot console-clean (backend=webgpu, starSource=catalog, zero errors) and -- the check that matters here -- impostorTextureReady=true at boot, i.e. the impostor sampler is bound on real hardware before the mesh draws (#9). Driving warps to completion + screenshots hit the SAME backgrounded-tab throttle D6.4/D1.3 documented (rAF throttled, warps never integrate, compositor screenshot hangs); the behavioural ladder is fully driven+asserted by frame-ladder.spec on the SwiftShader path and the impostor WGSL draw gets a real-GPU check in webgpu-hardware.spec. The belt-fade/band-collapse/impostor LOOK is flagged owner-confirmed-visual. D2.3 (constellation dissolve at ly 50-500 + the full 4-entry ledger) remains -- the figures here fade only as part of the extragalactic collapse. FABLE-5 REVIEW (same day, post-gate, TR-090 addendum): an adversarial fresh-eyes pass found ONE REAL DEFECT every assertion had missed -- the impostor was DEPTH-OCCLUDED behind the band skybox, which is an opaque depth-writing shell at a constant 2000 from the CAMERA (infiniteDistance), while the impostor sat world-fixed at 1800 from the ORIGIN = ~2840 from the camera at an nbg arrival; every fragment depth-failed, state said visible, zero pixels reached the screen -- the exact B4 state-correct-but-no-pixels class, missed because every impostor assertion was state-based and the manual-regression tab throttle blocked a screenshot. Fixed camera-relative at a constant IMPOSTOR_DIST=1500 (also the more honest physics: zero parallax at Mly distances, the band own infiniteDistance reasoning) and regression-proofed with PIXELS: aimAt(impostor) testhook + screenshot luminance/variance/bright-pixel floors in frame-ladder.spec (2/2 green in isolation). Two review suspicions CLEARED against sources: ShaderMaterial.clone DOES copy stored uniform values (Babylon 8 source read), and maxZ=6000 clears frustum clipping. Rode along: HUD farField O(4829) find per idle re-render (7.5Hz aim readout) -> module-level one-entry cache, deliberately NOT a useMemo (would sit below the engineReady early return -- the TR-086 hooks-order class). Re-gate: 680/680 unit, astro check 0/0/0, budgets green, no test weakened (pixel assertion purely additive). The review lesson recorded for D9/later: any backdrop mesh must reason about its distance from the CAMERA vs the band shell 2000, not from the origin."
  },
  {
    "project": "ishanjain-portfolio",
    "deliveryPlan": "PF-11-cinematic-journey-and-scale-honesty",
    "phase": "D2.3 -- constellation dissolve + license ledger completion (Phase D2 close)",
    "detail": "New ship-dynamics.ts figureVisibility(ly) = 1-smoothstep(FIGURE_FADE_START_LY=50, FIGURE_GONE_LY=500, ly) -- same shape as furnitureVisibility, a separate nearer window than localFieldVisibility's binary 1e6-ly collapse. Engine: _figureFade/_figureFadeFrom/_figureFadeTo fields, captured in _beginWarp, scheduled each frame in _updateFrameLadder through the existing frameLadderFade curve. No new shader uniform -- the figures already drive their alpha CPU-side in _pushAberration's uColor call, so this slice adds one more multiplicand (_figureFade) to the existing (0.34*(1-beta)*_localFieldFade) expression. sceneStats gains figureFade. tests/e2e/frame-ladder.spec.ts gains a Polaris (433 ly) leg proving a partial mid-band fade plus figureFade assertions at every existing checkpoint (Mars=1, M42=0 on its own schedule while localFieldFade stays 1 there, nbg=0, home=1 restored); timeout 150s->180s for the added leg. tests/unit/frame-ladder.test.ts gains a figureVisibility describe block (full/gone/monotonic/partial-at-Polaris/independent-of-localFieldVisibility) plus a source-string pin on the _conMat alpha expression. License ledger completed: dated addendum on the science brief (L14 belt overbrightness formalised, L15 seconds-scale warp-duration compression derived from warpDurationForLy against the brief's brachistochrone table, L16 band+star-field persistence narrowed to exclude the figures, L17 NEW -- the ly 50-500 window itself declared a scene-wide approximation, not a per-figure derivation) cross-referencing L9 (intro timing, D1.3) and L1 (Reinhard, D6.1) rather than duplicating them",
    "status": "complete",
    "start": "2026-07-24T00:00:00Z",
    "end": "2026-07-24T00:00:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "TR-091 + Astra REALISM-AUDIT (D2.3 section) appended to docs/analysis/2026-07-23-pf11-d2-frame-ladder-realism-review.md, ledger addendum appended to docs/analysis/2026-07-22-pf11-sky-frames-and-travel-science-brief.md. FABLE-5 REVIEW (same day, post-gate, TR-091 addendum): one comment-honesty defect, no code defects -- the FIGURE_FADE_START_LY comment claimed '50 ly is well past every star the figures are drawn from', factually backwards (figure stars span ~43-2,600 ly) and contradicted by the slice's own L17 audit; rewritten to the audit's actual rationale. Cleared: Polaris assertion non-vacuous (0.06), one-frame _pushAberration lag is the pre-existing D2.2 pattern, all warp entry paths capture endpoints, no other uColor writer, docs:check green. Untracked dev/null/* identified as Git-LFS hook stubs (Windows hookspath mangling), excluded from the commit, flagged to owner. Committed 92cd8d3 (D2.2 review fix + D2.3, one commit -- shared files, one gate). PHASE D2 IS NOW COMPLETE (D2.1 furniture gating + D2.2 extragalactic collapse, both TR-090; D2.3 this slice). THE SCOPE DECISION THAT SHAPED THE WHOLE SLICE: D2.2's constellation alpha was already multiplied by _localFieldFade (fading the figures at 1e6 ly along with everything else), so the honest question was whether that counted as 'done' -- it does not, because Astra brief section 1 calls the figures parallax accidents of NEARBY stars specifically, meaning they go wrong at destinations two to three orders of magnitude nearer than an extragalactic arrival. Split into its own _figureFade rather than widening the existing one, so D2.2's mechanism (still correctly serving the band+star-field extragalactic collapse) is untouched. LEDGER COMPLETION WAS THE LARGER HALF OF THIS SLICE: the implementation plan asked for 'the 4 from the brief + Reinhard ratio compression' as a dated addendum, and the honest way to do that without duplicating existing content was to formalise the three genuinely-new entries (L14-L16) and CROSS-REFERENCE the two already ledgered at their point of origin (L9 intro timing at D1.3, L1 Reinhard at D6.1) rather than re-deriving them. ASTRA'S KEY FINDING: the ly 50-500 window is NOT a derived physical threshold -- real constellation-figure stars span roughly 80 ly (Merak) to 2,600 ly (Deneb), so a scene-wide constant necessarily over-fades near figures (Ursa Major members ~80-125 ly: already ~36 deg parallax-distorted AT the fade-start threshold, computed via the small-angle Delta-theta ~= x/d formula) and under-fades far ones (Orion members ~1,260-2,000 ly: only ~23 deg distorted AT the fade-gone threshold) -- graded DECLARED LICENSE (new ledger entry L17), not ACCURATE, with the numbers recorded so a future per-figure refinement has a starting point rather than needing to re-derive why the window exists. TEST DESIGN: the existing Mars(=1)/M42(=0) checkpoints alone would have proven the endpoints but not the actual ly 50-500 mechanism, so a Polaris (433 ly) leg was added specifically to land inside the dissolve band and assert a value strictly between 0 and 1 -- the test that would have failed against a naive 'just reuse localFieldFade' implementation. MANUAL REGRESSION hit the SAME documented backgrounded-tab throttle every PF-11 travel-cinematic slice since D6.4 has recorded (document.hidden/hasFocus false when the MCP Browser pane isn't foregrounded -- rAF throttled, travelTo never reaches arrival, page.screenshot times out); confirmed boot console-clean on real WebGPU with figureFade=1 correctly initialised at idle, and relied on frame-ladder.spec (Playwright/SwiftShader, no foregrounded-tab concept) for the actual behavioural ladder proof, consistent with TR-088/089/090's documented workaround."
  },
  {
    "project": "ishanjain-portfolio",
    "deliveryPlan": "PF-11-cinematic-journey-and-scale-honesty",
    "phase": "D1.3 closeout -- Earth alpha=90 photometry (the escalated carried-forward item; Phase D1 fully closed)",
    "detail": "Astra NUMBERS: disc-integrated the shipped planet-sphere shader math (400k-sample Fibonacci sphere, instrument validated against analytic Lambert Phi(90)=0.3183) -> shipped composite Phi(90)=0.354 vs Earth's MEASURED 0.236 (Mallama 2017 phase polynomial, EPOXI-fitted near quadrature) = x1.50 over-bright at the only phase Earth is shown. Surface L=0 CONFIRMED at all phases (no-regolith mechanism is phase-independent); 0.213 CONFIRMED (alpha=0 anchor corroborated, not the deficit's source); the gap was the cloud slab's missing Mie forward-scattering deficit. Fix: CLOUD_QUAD_DEFICIT=0.539 solved by bisection on the disc integral (the EXPOSURE_GAIN method), applied to cloudRefl in BOTH twins as (1 - d*min(alpha/90,1)) -- exactly 1 at alpha=0 so every travelTo arrival and Venus are bit-identical; only goHome reveal + ascent change. Solver committed as scripts/solve-earth-quadrature.mjs. +3 unit pins (constant, both twin strings, surface-constants-stand)",
    "status": "complete",
    "start": "2026-07-24T00:00:00Z",
    "end": "2026-07-24T00:00:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "TR-092 + Earth brief Addendum 3 (docs/analysis/2026-07-21-earth-sphere-science-brief.md, the full derivation + ledger L18) + closure addenda on the D6.4 and D1.3 realism reviews (the carried-forward/escalated item both docs tracked). THE KEY SCIENCE CALL: the 37-67% figure the audits carried was the COUNTERFACTUAL error (had Mars/Moon regolith L been reused) -- the real defect was different and smaller-but-real: the L=0.9 conservative-slab cloud form has no forward-scattering deficit, so the composite integrated to Phi(90)=0.354 vs measured 0.236. THE CALIBRATION IS A RATIO, deliberately: exposure conventions cancel, and it is robust to the LIVE 2025 LITERATURE DISPUTE surfaced during the frontier check (PSJ July 2025, arXiv:2507.22258, revises Earth p_V to 0.242+/-0.005 vs Mallama 2017's 0.434 -- a full-phase-extrapolation dispute; both fit the same quadrature data this scene renders; date-stamped, watch-listed, NOT rebased mid-slice). LEDGER L18: linear-in-alpha interpolation between the two calibrated endpoints is the declared license (real curve convex between; scene renders only the endpoints, both measured-exact); min() clamp prevents extrapolation past 90 where real crescent Earth shows forward-scatter EXCESS. Recorded not relitigated: the shader adds Rayleigh atop the 0.213 clear-sky base (possible modest alpha=0 double-count, absorbed by TR-087's exposure re-solve, consistent at both phases so the ratio is unaffected) -- flagged for whoever next rebases the Earth budget. The D1.3 audit's gated judgement (terminator softness / twilight band cannot be fairly judged until the photometry under them is right) is UNBLOCKED. Gate: 688/688 unit (+3), build clean, budgets green (JS +0.2KB), full E2E run recorded in TR-092. Owner-eyes item: the x1.50-dimmer cloud deck at the home vantage on real hardware -- the numbers say it is correct; the look is flagged owner-confirmed-visual per convention."
  },
  {
    "project": "ishanjain-portfolio",
    "deliveryPlan": "PF-11-cinematic-journey-and-scale-honesty",
    "phase": "D3.1 -- deceleration legibility (Phase D3 opens: chase-open, FOV breathing, BRAKING label)",
    "detail": "Vega SHOT-BRIEF authored before IMPLEMENT + MOTION-AUDIT after (cinematic-slice seam). ship-dynamics.ts: CHASE_WAYPOINTS decel segment reworked -- old [0.8,3.5]+[0.92,2.2] (which CLOSED the camera to 2.2, driving the ship 37% LARGER than arrival size mid-brake) replaced by [0.72,0.25,elev(5.8),5.8] pull-back peak + [0.92,0,elev(3.4),3.4] settle, so back-distance stays >= SHIP_VIEW_DEPTH for the entire brake; new pure warpFovMult(dsdk)=min(1+0.06*dsdk/2,1.08) + WARP_FOV_GAIN/WARP_FOV_MAX_MULT. babylon-engine.ts: _baseFov captured at camera construction, FOV breathes _baseFov*warpFovMult(dsdk) per warp frame (off under reduced motion, reset to base at arrival), wphase now sourced from the shared WARP_ACCEL_END/WARP_DECEL_START constants instead of hard-coded literals, sceneStats gains fov+baseFov. WarpOverlay.tsx: DECELERATION BURN -> BRAKING BURN. +7 unit tests (3 chase decel-legibility invariants + 4 warpFovMult); E2E FOV breathe/relax cue-proof added to the chase-camera choreography test",
    "status": "complete",
    "start": "2026-07-24T00:00:00Z",
    "end": "2026-07-24T00:00:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "TR-093 + Vega SHOT-BRIEF (docs/experience-design/2026-07-24-pf11-d3.1-deceleration-legibility-motion-spec.md, authored BEFORE any code) + Vega MOTION-AUDIT (same slug, -motion-review.md). PHASE D3 OPENS. THE BRIEF-BEFORE-BUILD STEP PAID FOR ITSELF IMMEDIATELY: it caught that SHIP_BASE_FOV (70deg) is a SHIP-MESH SCALE constant that is NEVER assigned to the camera -- the FreeCamera runs at Babylon's default ~0.8 rad -- so implementing the plan's literal 'BASE_FOV*(1+GAIN*dsdk/2)' against SHIP_BASE_FOV would have SNAPPED the lens 45.8->70 degrees on every warp start. Shipped breathing around a captured _baseFov = camera.fov instead. THE CORE FIX IS AN INVARIANT, NOT A NUMBER: the old decel table closed the chase to back=2.2 while SHIP_VIEW_DEPTH (arrival framing) is 3.0, i.e. the ship was 37% LARGER mid-brake than at the dock -- textbook CONTRADICTS, and the mechanical root of owner complaint R6 ('decel doesn't slow'). The new table's load-bearing property is back >= SHIP_VIEW_DEPTH across the WHOLE braking range (unit-asserted every 0.005 of k), so the ship can never loom while braking; the 5.8 pull-back peak is the cinematic garnish on top. A CONTINUITY-DRIVEN REFINEMENT OF THE BRIEF, recorded not hidden: Vega specified the settle waypoint at k=0.9/back 4.3; shipped k=0.92/back 3.4 because the existing 30deg-elevation-hold test asserts -u/b=tan30 across k in [0.1,0.92], which only holds where BOTH segment endpoints sit on the elevation locus -- placing the settle waypoint at 0.92-on-locus keeps EVERY existing chaseOffsetAt test passing UNCHANGED at zero perceptual cost (audit graded the deviation READS). AN E2E DESIGN LESSON worth the read-across: the FOV cue-proof was first added to the GAP-17 flight-sequence test and failed on phases.indexOf('flip')===-1; isolation x3 (fail/pass/pass) confirmed the DOCUMENTED GAP-17 flip-skip flake (TR-081: cosmos:warp fires once per rendered frame and the ~195ms flip window is skipped whenever a SwiftShader frame runs longer than it -- precisely what D3.2's screen-time floor exists to fix, and D3.1 does not touch the flip window). Rather than accept a new assertion riding a known-flaky carrier, GAP-17 was restored VERBATIM to its pre-D3.1 form and the proof moved to the stable chase-camera choreography test, whose peak-FOV tracker only needs the warp to cross k=0.5 (every completed journey does) -- 3/3 stable in isolation. Rule recorded for later slices: do not hang new assertions on a known-flaky spec. ONE NAMED TEST CHANGE (#15): the 'DECELERATION BURN' E2E label assertion -> 'BRAKING BURN', a direct consequence of the rename, documented at the assertion; the cosmos:warp phase:'decel' STRING is unchanged so flight-v3's phase-order assertion and the whole event contract are untouched (#3 additive). Gate: 695/695 unit (+7), astro check 0/0/0, build clean, budgets green (pure logic slice, no new asset), full E2E recorded in TR-093. Cue-source verification per plan step 3: streaks (real frame-to-frame camera displacement) and aberration beta (explicit dsdk) were ALREADY derivative-driven -- confirmed and left alone; plume intensity is phase-boolean and its cutoff-at-flip/relight-retrograde is D3.2's, scoped out honestly rather than half-done. Owner-eyes items flagged per convention: whether the pull-back reads as 'easing back' vs 'shrinking away', and whether the +2.75deg peak lens breath is felt (subliminal by design). Astra ledger confirmation for the FOV/chase framing choices deferred to the post-D3 full-journey REALISM-AUDIT (the plan's own D3 exit criterion), expected to fall under the standing cinematic flight-feel license."
  },
  {
    "project": "ishanjain-portfolio",
    "deliveryPlan": "PF-11-cinematic-journey-and-scale-honesty",
    "phase": "D3.2 -- design pass (ADR-0011 + Vega SHOT-BRIEF; implementation not started)",
    "detail": "DESIGN-mode pass before the D3.2 IMPLEMENT, owner-requested. Produced ADR-0011 (warp velocity profile v4: trapezoid coast plateau + flip screen-time floor via in-window k-rate multiplier r = min(1, 0.12*warpDur/1500), with coast-slope compensation m/r keeping world velocity exactly continuous at the window edges; cue driver warpSpeedNorm replaces the dsdk triangle so beta/FOV/vC hold peak through the coast) and the Vega D3.2 SHOT-BRIEF (five-beat sheet over the >=1.5s window: cutoff 150ms envelope fade -> drift beat -> C2 smootherstep rotation over sub-window [0.465,0.545] with RCS puff pairs -> settle beat -> retro relight ~0.3s capped shoulder; camera explicitly unchanged; PlumeParams burn-envelope threaded additively). Corrected the implementation plan's D3.2 section with a dated note superseding its broken floor formula",
    "status": "complete",
    "start": "2026-07-24T00:00:00Z",
    "end": "2026-07-24T00:00:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "THE DESIGN PASS EARNED ITS KEEP: the implementation plan's prescribed floor mechanism ('if 0.12*warpDur < FLIP_MIN_MS, stretch warpDur to FLIP_MIN_MS/0.12, ~12.5s floor never triggers') is ARITHMETICALLY BACKWARDS -- warpDur spans 1400-4200ms so 0.12*warpDur spans 168-504ms, ALWAYS below 1500ms: the branch fires on EVERY journey and sets every warp to 12.5s (3-9x inflation), the exact opposite of its own parenthetical; and its 'home stays snappy via WARP_MIN_MS' claim is false under ANY correct fixed floor (no proportional window can deliver a fixed wall-clock minimum without stretching short journeys). Implementing D3.2 as written would have been the D0.1 lesson repeated ('probe a prescribed mechanism before implementing on top of it'). SECOND DESIGN FINDING: the naive alternative (dilate k through the window, keep the eased position curve) produces a 1/r (~4x at polaris) WORLD-VELOCITY LURCH at both window edges -- an engines-off coast that visibly brakes, the same CONTRADICTS class D3.1 just removed from the chase. THE CHOSEN MECHANISM (ADR-0011) solves both simultaneously and is MORE honest than the shipped triangle: a trapezoid profile with a genuine constant-velocity coast (which is literally Astra 3.2's choreography -- cut, coast-and-rotate, relight -- and which the triangle cannot express: today the HUD velocity SAGS through an engines-off coast), with the floor delivered by an in-window rate multiplier whose coast-slope compensation (ds/dk jumps by 1/r exactly where dk/dt drops by r) keeps world velocity continuous by construction. The profile is symmetric (s(0.5)=0.5 for every r, algebraic identity) so the existing shape-generic warpEase unit pins survive. THIRD FINDING (blast-radius sweep): a HIDDEN FOURTH consumer of the old k-thresholds -- NEBULA_REVEAL.decelStart is a 0.53 literal in nebula-field.ts; without re-keying to 0.56 the destination gas would begin revealing MID-FLIP. The other consumers were already safe: wphase sources the shared constants (D3.1's one-place-change setup paying off), chase waypoints are k-keyed and benefit (the camera pan slows to a near-hold through the dilated window, which is exactly the 'camera eases a few degrees' shot 3.2 asks for), frame-ladder fades sit outside the window on both sides. OWNER-VISIBLE CONSEQUENCE flagged in the ADR and the brief's owner-eyes list, not slipped through: every journey grows ~1.0-1.3s and HOME ROUGHLY DOUBLES (1.4s -> 2.7s) -- FLIP_MIN_MS is the single tuning knob if 1.5s reads long on the home hop. GAP-17 CONSEQUENCE: the >=1.5s window cannot be skipped by a single slow frame, so the documented flip-skip flake class (TR-081, seen again during D3.1's gate) dies with the D3.2 implementation -- the plan requires asserting frame-rate robustness explicitly. L15's compression magnitudes shift under the new duration table; the post-D3 REALISM-AUDIT re-states them. VEGA'S BEAT SHEET spends the floored window on anticipation-commit-settle grammar (cutoff / drift beat / 1.0s C2 rotation with RCS couples / settle beat / relight) so the three narrative events never overlap -- rotation gets [0.465,0.545], NOT the full window, because back-to-back state changes are double punctuation. PlumeParams gains the burn envelope ADDITIVELY (absent field = today's boolean behaviour) so existing plume pins hold. NO CODE WRITTEN this pass -- design only; D3.2 IMPLEMENT is next, on these two artefacts. Meanwhile the D3.1 gate's full-suite E2E re-run (after the invalid-run incident recorded in TR-093) was still executing in the background throughout this pass; TR-093's verdict remains honestly PENDING until it lands."
  },
  {
    "project": "ishanjain-portfolio",
    "deliveryPlan": "PF-11-cinematic-journey-and-scale-honesty",
    "phase": "D3.2 -- flip choreography (v4 coast profile + screen-time floor + five-beat manoeuvre)",
    "detail": "ship-dynamics.ts: warpEaseV4 (trapezoid: quadratic burn / constant-velocity coast across the flip window / quadratic brake), warpSpeedNorm (single cue driver, holds 1 through the coast), warpFlipRate + FLIP_MIN_MS=1500. babylon-ship.ts: window widened 0.47/0.53 -> 0.44/0.56, flipPhase now C2 smootherstep across a ROTATION SUB-window [0.465,0.545], new burnEnvelope (eased cutoff/relight shoulders), plumeParamsForWarp gains optional relightK -> env. ship-dynamics PlumeParams gains optional env, blended via byEnv into flare/alpha/throttle (additive: absent = original booleans). babylon-engine.ts: _flipRate/_relightK captured per journey in _beginWarp, k advanced at slow*rate inside the window (reusing advanceWarpProgress's existing slowFactor arg -- no signature change, composes with the belt proximity slowdown), warpEaseV4 drives BOTH camera and hull, beta + FOV + vC now read warpSpeedNorm, RCS puff couples via _emitRcsPuffs reusing the ember buffer, sceneStats gains flipRate/burnEnv. nebula-field.ts: NEBULA_REVEAL.decelStart 0.53 -> 0.56. eslint.config.js: playwright-report/ + test-results/ added to ignores. +12 unit tests",
    "status": "complete",
    "start": "2026-07-24T00:00:00Z",
    "end": "2026-07-24T00:00:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "TR-094 + ADR-0011 (design pass, same day) + Vega SHOT-BRIEF. THE SLICE DID NOT FOLLOW ITS OWN PLAN STEP, and that was the point: the design pass proved the implementation plan's prescribed floor formula ARITHMETICALLY BACKWARDS -- 'if 0.12*warpDur < FLIP_MIN_MS stretch warpDur to FLIP_MIN_MS/0.12 (never triggers)' fires on EVERY journey because 0.12*warpDur is 168-504ms against a 1500ms floor, which would have set every warp to 12.5s (3-9x inflation). Implementing it as written would have been D0.1's lesson repeated. WHAT SHIPPED INSTEAD is more honest than the shipped triangle rather than merely correct: a trapezoid with a GENUINE constant-velocity coast, which is literally Astra 3.2's choreography (cut, coast-and-rotate, relight) and which the triangle could not express -- under the old profile every speed cue SAGGED through exactly the window where the narrative says the engines are off. The floor rides an in-window k-rate multiplier whose coast slope m/r makes ds/dk rise by 1/r precisely where dk/dt falls by r, so WORLD VELOCITY IS CONTINUOUS at both window edges (unit-asserted); the naive dilation lurches ~4x at polaris and was rejected in the ADR. Implementation detail worth keeping: the rate rides advanceWarpProgress's EXISTING slowFactor argument (slow * rate), so no signature change and the B4 belt proximity-slowdown composes with the flip dilation for free. TWO NAMED TEST CHANGES (#15), both in babylon-ship.test.ts and both direct consequences of the delivery plan's own window widening: the 0.47/0.53 threshold pin -> 0.44/0.56, and the flipPhase midpoint probe re-aimed at the ROTATION sub-window's midpoint (the flip window's own midpoint is no longer the half-rotated pose now that the window holds cutoff/drift/rotation/settle beats) -- same property asserted, measured where it now lives. warpEase's pins and every chaseOffsetAt test untouched. THE TEST-INSTRUMENT LESSON COST TWO RED RUNS AND THE FAILURES WERE MINE, NOT THE CODE'S: (1) the world-velocity-continuity probe sampled slopes at KA +/- 1e-3, but the burn slope rises LINEARLY so a probe 1e-3 before the join reads ~0.23% low -- the algebraic property is exact, the instrument wasn't; replaced with an algebraic recovery of m from a mid-segment slope. (2) the C2 check used a NESTED finite difference AT the clamp join, a stencil that straddles the piecewise boundary and reports the curve's cubic growth as a ~3.9 second-derivative spike; replaced with the smootherstep signature f/t^3 -> 10 versus smoothstep's 3/t (~300 at t=0.01), a 30x discriminator that is numerically stable. Also recorded: 10t^3 is only the LEADING term of smootherstep, so comparing to it at 9-decimal precision fails against the exact 10t^3-15t^4+6t^5 (measured 9.8506e-6 vs 1e-5). BLAST-RADIUS SWEEP CAUGHT A HIDDEN FOURTH CONSUMER of the k thresholds: NEBULA_REVEAL.decelStart was a 0.53 literal in nebula-field.ts -- left alone the destination gas would begin revealing while the ship is still mid-rotation. The other consumers were already safe, and wphase moved automatically because D3.1 had sourced it from the shared constants. A LATENT REPO DEFECT UNBLOCKED ON THE WAY: playwright-report/ and test-results/ are declared generated in .gitignore but were missing from the eslint ignores, so any local E2E run with the default html reporter dropped ~2,900 lint errors of Playwright's own bundled trace-viewer assets into the tree and BLOCKED npm run build (which is lint && check && astro build) -- added to eslint.config.js with the reason inline. OWNER-VISIBLE PACING CONSEQUENCE flagged in the ADR, the brief's owner-eyes list and the TR rather than slipped through: every journey grows ~1.0-1.3s and HOME ROUGHLY DOUBLES (1.4 -> 2.7s), the unavoidable cost of a fixed wall-clock floor; FLIP_MIN_MS is the single tuning knob. GAP-17 CONSEQUENCE: the >=1.5s window cannot be skipped by one slow frame, so the documented flip-skip flake class (TR-081, and the single failure in D3.1's own gate hours earlier) should die with this slice -- the plan requires asserting that explicitly. Gate: 707/707 unit (+12), astro check 0/0/0, build clean; full E2E + budgets recorded in TR-094. Owner-eyes items per the SHOT-BRIEF: does the flip read as three distinct events, do the RCS puffs read as cold gas rather than fire, does the sky correctly NOT slow during the coast, and the home-journey pacing question. Astra's post-D3 full-journey REALISM-AUDIT (the plan's D3 exit criterion, after D3.3) must re-state L15's compression magnitudes, which shift under the new duration table. POST-GATE INVESTIGATION, the slice's best story: run 1 came back 118/119 with GAP-17 PASSING (the flip-skip flake class dead on its first full-suite outing, as ADR-0011 predicted) and ONE DETERMINISTIC failure -- the belt proximity-slowdown assertion (warpSlowMin < 0.9, got 0.948/0.948/0.978 in isolation x3, NOT the rotating-flake class). The kill chain: (1) load-sparsity hypothesis rejected by deterministic isolation; (2) a scratch vitest sim with the REAL functions exonerated the v4 math (warpSlowMin 0.4623 at 60/15/5 fps, matching the true line minimum at e=0.326); (3) a scratch in-browser trajectory probe found the truth -- the whole 4.66s m42 journey renders in 10 FRAMES (~2 fps SwiftShader) and the two samples around the belt sat at e=0.169 and e=0.442, stepping ~140 wu clean over the tube. THE REAL FINDING: warpSlowMin was ALWAYS a point-sampled instrument and its own spec comment ('no timing sensitivity') was never true -- the old triangle passed at 2fps by GEOMETRIC ACCIDENT (its k~0.35 sample landed on the tube edge at slow~0.83), and v4's gentler burn moved the sample off the edge, EXPOSING a pre-existing latent defect rather than creating one. THE FIX IS REAL, NOT TEST SURGERY: new pure minSlowAlongSegment (sigma/2 substeps, capped 24) makes the RECORD the minimum over each frame's traversed segment -- same journey now reports the same minimum at 2fps as at 60 (the repo's deterministic-under-variable-frame-times doctrine applied to an instrument); the per-frame point sample still drives the flight-feel feedback, unchanged on real hardware. +4 unit tests (711 total); the spec passes 3/3 in isolation post-fix; no E2E assertion touched. Read-across recorded in the impl plan: any per-frame-sampled min/max journey stat is frame-rate-dependent unless integrated over the segment. OWNER-REQUESTED REVIEW of D3.1+D3.2 (same session, Fable 5): ONE REAL DEFECT found and fixed same turn -- the floor had a SINGLE-FRAME HOLE. The in-window rate was picked once from frame-start k, so a frame beginning just below WARP_ACCEL_END advanced its whole step at rate 1, and at the 0.5s dt cap dk~0.13 > the 0.12 window: one capped frame could still jump the ENTIRE flip window, the exact TR-081 skip class that ADR-0011, the delivery plan and TR-094 were all claiming dead (and 0.5s frames demonstrably occur under load -- the trajectory probe measured 2fps). Fixed with advanceWarpV4, a piecewise integrator that splits the step at the window boundaries and spends the remainder at rate r -- time-exact under any dt sequence; 3 unit tests pin the worst-case capped frame landing INSIDE the window, the window consuming exactly max(0.12*dur, FLIP_MIN_MS) wall-clock under awkward mixed-dt integration, and r=1 degenerating to the plain advance (714 unit total). Cleared on review: RCS Math.random matches the existing ember convention, latches reset per journey, envelope staleness benign, relight cap arithmetic correct at both extremes, vC keeps its historical nominal calibration (Astra's post-D3 item). GATE SEQUENCING RECORDED HONESTLY: run 2 was deliberately STOPPED at 56/119-clean because the review fix superseded the build under test; run 3 on the final code is the binding gate."
  },
  {
    "project": "ishanjain-portfolio",
    "deliveryPlan": "PF-11-cinematic-journey-and-scale-honesty",
    "phase": "D3.3 -- mid-journey input policy (goHome abort + retarget queue), engine state machine",
    "detail": "ship-dynamics.ts: new pure module flightInputPolicy(mode, request) -> proceed|queue|abort|ignore, with FlightMode/FlightRequest/FlightInputPolicy types. babylon-engine.ts: public queuedTargetId field; travelTo resolves the body first then branches on the policy (queue sets last-wins + emits cosmos:retarget-queued{id,quiet}); goHome branches on the policy (abort clears the queue, nulls arrivedId, emits cosmos:abort{toHome,abandonedId,quiet} and _beginWarps home from the live cam); beginAscent and disconnectedCallback clear the queue; the k>=1 arrival block drains the queue through a cancellable _retargetTimer setTimeout(0) with a same-id no-op guard. space-engine.d.ts: queuedTargetId?: string|null added to the SpaceEngineElement contract as OPTIONAL (the archived engine has no queue). SpaceScene.tsx: sectionTravelRef becomes {id, sec} and the cosmos:arrive handler only consumes it on an id match; cosmos:home clears it. +5 unit tests, +4 E2E (new tests/e2e/mid-warp-input.spec.ts)",
    "status": "complete",
    "start": "2026-07-24T00:00:00Z",
    "end": "2026-07-24T00:00:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "TR-095. THE SMALLEST D3 SLICE AND IT STILL FOUND A SECOND DEFECT. Closes the phase D3 audited-baseline item 'travelTo/goHome silently no-op mid-warp (both engines)' per the owner ruling in ADR-0010. THREE THINGS WORTH KEEPING. (1) THE DECISION IS A PURE MODULE, not a mode list: flightInputPolicy(mode, request). The pre-D3.3 shape was the same 'mode === warp || aim || ascent' disjunction copy-pasted into travelTo, goHome and beginAscent, and THREE surfaces now have to agree about what a press does -- engine, HUD badge (next pass), D5.2 console. Pure -> unit-testable off-GPU (babylon-engine.ts cannot be imported in jsdom, which is why babylon-engine-freelook.test.ts only tests exported pure functions) and one import for the console rather than a rule to re-derive. ASCENT IS NOW THE ONLY SURVIVING NO-OP AND IT IS NAMED 'ignore' -- a unit test asserts exhaustively that it is the only (mode, request) pair yielding it, which IS the slice's property. (2) THE IMPLEMENTATION PLAN'S 'this mostly works by construction' WAS RIGHT, verified rather than assumed: _beginWarp reads this.cam and _tickWarp keeps it integrated, so the abort launches from the live position with zero new bookkeeping and k=0 replays a full D3.2 accel/coast-flip/brake home. (3) A PRE-EXISTING UI DEFECT THIS SLICE WOULD HAVE MADE WORSE, fixed at the seam rather than worked around: goSection set sectionTravelRef.current = sec UNCONDITIONALLY and the cosmos:arrive handler consumed it on the NEXT arrival whatever that was -- already the wrong body under the old no-op, and under queuing the INTERRUPTED journey's arrival would consume the intent so the section opened early at the wrong body and never at the right one. The ref now carries {id, sec} and matches; cosmos:home clears it so an abort discards a pending intent. dispatchRoute needed nothing -- it already guards warp.mode !== idle, so route sync cannot abort a journey. TEST-INSTRUMENT DISCIPLINE APPLIED AT DESIGN TIME (cost nothing this slice, recorded so it stays free): interrupts are driven inside ONE page.evaluate off the page's own rAF, because TR-094 measured whole journeys rendering in ~10 SwiftShader frames -- a Playwright-side poll round-trips between 'we saw k=0.2' and 'we called goHome' and the interrupt would land after the ship parked, with every downstream assertion passing FOR THE WRONG REASON; each spec therefore reports and asserts progAtInterrupt/modeAtInterrupt/distAtInterrupt first. Related, and deleted before it could flake: a poll for arrivedId === FIRST, which the deferred drain makes true for only a few milliseconds -- asserted from the captured cosmos:arrive EVENT instead. READ-ACROSS: never poll for a state a deferred drain makes transient; capture the event. Same family as D3.2's warpSlowMin per-frame-sample defect. GATE: 719/719 unit (+5), 123/123 E2E (+4) in 25.4 min -- THE FIRST FULLY CLEAN FULL SUITE OF THIS PHASE, GAP-17 included, which is the first full-suite confirmation that ADR-0011's screen-time floor killed the flip-skip class outright rather than merely reducing its rate (TR-093 and TR-094 both gated at 118/119 with it as the one failure). New spec 12/12 deterministic across three runs before the gate. NO existing test modified -- no #15 change in this slice. Build, budgets, docs:check green. TWO THINGS THE NEXT SLICE MUST CARRY, NOT INHERIT SILENTLY. HUD STRINGS ARE DELIBERATELY ABSENT (owner direction -- handed to a separate Sonnet 5 pass): the engine emits cosmos:abort{toHome,abandonedId,quiet} and cosmos:retarget-queued{id,quiet} and exposes queuedTargetId, but NOTHING LISTENS YET, so the policy is correct and still VISUALLY SILENT and 'never a silent no-op' is only half-met. Funnel instrumentation likewise deferred so the events and their milestones land together. THE OWNER-EYES MANUAL REGRESSION IS OUTSTANDING and is stated as such rather than glossed: the harness browser pane runs HIDDEN (document.hidden === true, requestAnimationFrame never fires, the moon journey sat at warp.prog === 0 indefinitely), so the state machine was confirmed LIVE on the built preview only in 'aim' mode -- queue, last-wins, abort-discards-queue and cosmos:abort abandonedId='moon' all verified against the shipped bundle -- while NO RENDERED FRAME OF AN ABORT WAS EVER SEEN BY A HUMAN. Screenshots would have proven nothing (a hidden pane reads blank regardless of correctness). ONE UNRESOLVED DESIGN ITEM FLAGGED RATHER THAN TUNED SILENTLY: the abort's velocity discontinuity. Restarting the profile at k=0 drops world speed from mid-burn to zero at the press and opens with a 900ms aim hold before accelerating home. The implementation plan pre-accepted this ('acceptable and cinematic') and it shipped exactly as specified, but it is the same CLASS as owner complaint R6 (motion that does not decelerate legibly) -- candidate for a Vega SHOT-BRIEF in a D3 polish pass; levers are a shortened aim for aborts, or a real retro-burn blend from the live velocity. D3.3 is code-complete and gate-green but is NOT the D3 phase exit: HUD strings, the owner-eyes pass, and Astra's full-journey REALISM-AUDIT (which must re-state L15's compression magnitudes against the D3.2 duration table) all remain."
  },
  {
    "project": "ishanjain-portfolio",
    "deliveryPlan": "PF-11-cinematic-journey-and-scale-honesty",
    "phase": "D3.3 -- HUD/console strings (Sonnet 5 follow-up pass, same slice's remaining scope)",
    "detail": "space/types.ts: NavNotice type ({kind: 'abort'|'retarget-queued', text}); SceneState gains notice: NavNotice|null and queuedTargetId: string|null. SpaceScene.tsx: NAV_NOTICE_MS=2200 constant; bodyDisplayName(id) factored out of warpDestName's inline IIFE (module-level, raw-id fallback instead of ''); noticeTimeoutRef alongside vistaTimeoutRef; new cosmos:abort and cosmos:retarget-queued listeners (set notice + queuedTargetId, auto-clear notice via timeout); cosmos:select and cosmos:arrive handlers extended to clear queuedTargetId when detail.id matches it (select = queued journey launching; arrive = the engine's same-id no-op case, added to all four of that handler's branches); cosmos:home clears it defensively. WarpOverlay.tsx: notice/queuedName props, transient notice pill (data-testid=nav-notice) above the main readout, persistent queued badge (data-testid=queued-badge) under the velocity line. MissionControlBar.tsx: queuedName prop, WHERE TO label swaps to QUEUED / {NAME} for the badge's duration. tests/e2e/mid-warp-input.spec.ts: +2 E2E specs (queued-retarget HUD/console strings; abort acknowledgement + console resting state), reusing TR-095's driveInterrupt technique against real rendered DOM instead of only engine state.",
    "status": "complete",
    "start": "2026-07-24T00:00:00Z",
    "end": "2026-07-24T00:00:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "TR-096. Owner switched the model to Sonnet 5 between TR-095 and this pass specifically for 'HUD and console strings' -- this closes exactly that, and only that: no engine code touched, flightInputPolicy and the state machine (TR-095) are untouched. TWO PIECES OF STATE BECAUSE TWO DIFFERENT QUESTIONS: notice answers 'did my press register' (transient, 2.2s), queuedTargetId answers 'is something still queued' (persistent, mirrors the engine's own field) -- collapsing them into one would have made the badge disappear the moment the toast faded, which is just a SLOWER silent no-op. CLEARING THE PERSISTENT BADGE NEEDED THREE LISTENER SITES because the DOM event stream gives no single 'the queue is gone' signal: cosmos:select (queued journey launches), cosmos:arrive under the SAME-ID NO-OP CASE TR-095 introduced (no cosmos:select ever fires for it -- added to all four of that handler's branches, not just the common one, since a queue surviving the section-match branch because it looked like a different code path would be exactly the silent residue this slice removes elsewhere), and cosmos:abort (discards it). CONSOLE STRINGS ARE DELIBERATELY MINIMAL, NOT D5.2: MissionControlBar.tsx has no mid-warp branching today; building the plan's fuller 'console reflects the queue' would mean implementing D5.2's own combobox rewrite ahead of its own design pass. What shipped is a one-line label swap where the visitor was looking, not a new surface. TEST DISAMBIGUATION FOUND BY THE FIRST ISOLATION RUN, NOT DESIGNED IN ADVANCE: the first draft of both new specs matched RETARGET QUEUED text bare, which is ambiguous for ~2.2s while the transient notice and the persistent badge show the IDENTICAL string -- Playwright's strict mode correctly refused to guess and failed the run. Fixed with permanent data-testid hooks (nav-notice / queued-badge), not a looser matcher, since the ambiguity is real and intentional (both elements ARE supposed to show the same text for a while). GATE-RUN INCIDENT, root-caused rather than shrugged off (CLAUDE.md #14): the first full-suite attempt hit ONE failure -- accessibility.spec.ts's 'scene chrome stays keyboard-operable on the Babylon path' (a .focus()/toBeFocused() race on the mission-control input) -- and isolating it 4x on the same build reproduced 1/4. Rather than accept 'pre-existing flake' on a coin-flip sample, ran it 4x against a git-stash-ed pre-session baseline (4/4 clean) and then 8x on the actual session code AFTER stopping every other concurrent background process (12x13.9s vs the earlier run's 4x1.4m -- an order of magnitude slower per test, the fingerprint of CPU contention from this session's own concurrent builds/test runs, not a logic defect) -- 8/8 clean. Conclusion: an uncalibrated instrument (a test run sharing the CPU with several other heavy node/playwright processes this session itself spawned) produced the one failure, matching this repo's own documented class of defect (TR-032/033 broken instruments, TR-081/TR-094 SwiftShader load-timing). The FINAL gate below ran with nothing else competing for CPU. NO EXISTING TEST WAS MODIFIED to make anything pass -- the two locator changes were made to specs that had not yet appeared in any prior TR's gate, a same-session authoring fix, not a post-hoc weakening (CLAUDE.md #15). Gate: 719/719 unit (unchanged, presentational-only pass), build/budgets green (JS gz +0.5 KB). OWNER-EYES CONFIRMATION STILL DIDN'T HAPPEN, stated as such rather than implied: the Browser-pane harness never composites a frame in this environment (document.visibilityState reports 'hidden' even with the tab fronted; the PreFlight dossier's own FIRST FRAME: STALLED line confirms it) and computer.screenshot() times out outright. What WAS verified: synthetic cosmos:* events dispatched on window (the same transport babylon-engine.ts's emit() uses) produced the correct DOM text at the correct times against the real built bundle -- proof the React wiring is correct, not proof of how it looks. Funnel instrumentation deliberately still not extended: FunnelEventName is a closed vocabulary tied to the UX research plan's nine named milestones, and adding abort/retarget-queued to it is a research-instrumentation decision outside what 'HUD and console strings' actually asked for -- left named as a real gap, not silently absorbed."
  },
  {
    "project": "ishanjain-portfolio",
    "deliveryPlan": "PF-11-cinematic-journey-and-scale-honesty",
    "phase": "post-D6.4 owner bug fixes (home-orbit drag reset, planet-sphere pick occlusion) + D5.1 console renames",
    "detail": "babylon-engine.ts: new private field _homeLookOverridden (latched true on the first real drag since the home orbit was last armed; reset false at all four arming sites -- beginAscent's two paths, skipAscent, the regular goHome-via-warp arrival); the idle home-orbit tick's auto-aim guard changed from '!this._dragging' to '!this._dragging && !this._homeLookOverridden'; exposed on sceneStats() as homeLookOverridden. New private method _planetOcclusionDist(camPos, dir) wrapping ship-dynamics.ts's raySphereDist against the shared planet mesh (gated on isVisible). _pick refactored to compute the cursor ray once (cursorRayDir) and reject any body candidate the sphere occludes; the field-star fallback skipped outright when occluded. _pickField refactored to accept the precomputed ray/camPos instead of recomputing its own NDC math (removes a duplicate). ship-dynamics.ts: two new pure exports, cursorRayDir and raySphereDist, unit-tested (10 new tests). MissionControlBar.tsx: RNG -> RANDOM JUMP (compressed JUMP under 400px via CSS breakpoint, aria-label/title 'Jump to a random destination'), SOL -> RETURN HOME (compressed HOME, aria-label/title 'Return home to Earth orbit'). CollectorCard.tsx and SectionOverlay.tsx: RETURN TO SOL -> RETURN HOME. Four E2E test files updated their RNG-button locator to the new accessible name (accessibility.spec.ts, engine-select.spec.ts, funnel.spec.ts, space-scene.spec.ts). New tests/e2e/d64-owner-bugfixes.spec.ts, 4 tests.",
    "status": "complete",
    "start": "2026-07-24T00:00:00Z",
    "end": "2026-07-24T00:00:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "TR-097. Owner reported three issues after using the built site; cross-validated against the actual PF-10/PF-11 delivery record before touching anything (per the owner's own explicit request to cross-validate) rather than assuming all three were fresh bugs. #1 and #2 turned out to be GENUINE REGRESSIONS IN D6.4 (TR-088, 2026-07-23) -- confirmed by grep that TR-088's own verification contains no drag or occlusion assertion at all, so neither was ever caught. #3 turned out NOT to be a new bug: D5.1's exact replacement names were already owner-approved in ADR-0010 on 2026-07-22, recorded in both the delivery and implementation plans with the compressed sub-400px variant fully specced -- the code was simply never written. This distinction mattered for how each was handled: #1/#2 needed root-cause analysis and new fixes; #3 needed only to implement a decision already made, not a fresh naming call. ROOT CAUSE #1: the home-orbit's per-frame camera driver re-derived _yaw/_pitch from the orbit position on EVERY frame the visitor wasn't actively mid-drag, including the very first frame after a drag ENDED -- so releasing a drag snapped the view straight back to Earth. The code's own comment ('free-look drag still overrides') was true only for the instant of the drag itself, contradicting the sibling 'ambient idle drift' branch's explicitly stated philosophy three lines below ('a visitor who has already dragged/looked around keeps their own orientation'). The ORBITAL POSITION (R16, correctly delivered in TR-088) was never the problem -- only the forced re-aim of the VIEW was, and the fix (_homeLookOverridden) touches only that, leaving the position update completely untouched. ROOT CAUSE #2: _pick/_pickField are a SCREEN-SPACE NEAREST-PROJECTED-POINT search over this.bodies plus a cone-angle sweep over the star field -- NEVER a real depth test, and harmless before D6.4 because nothing solid was ever on screen for the camera to look past (an arrival's planet sphere fills the frame and the camera is PINNED to look at it). D6.4 put a 26-world-unit opaque sphere at a fixed point the visitor can now freely look around, and the picker kept working exactly as before, oblivious to it -- Babylon's own mesh.isPickable=false on the sphere is a complete red herring here since this picker never calls scene.pick() in the first place. Fixed with a ray/sphere occlusion test run once per pick against the SAME cursor ray the field-star cone test already used (cursorRayDir factored out of _pickField's own inline NDC math, which used to be a second, driftable copy). BOTH PURE FUNCTIONS UNIT-TESTED DIRECTLY (10 new tests: dead-ahead hit, miss, behind-camera, tangent-edge, camera-inside-sphere, Earth-geometry-scale check for raySphereDist; screen-centre/unit-vector/left-right/up-down checks for cursorRayDir) -- these are closed-form geometry with zero loop/hang risk, confirmed by the diagnostic below. #3 SHIPPED EXACTLY AS SPECCED (compressed variants, aria-label as the single source of truth at every width via two spans + a CSS breakpoint) PLUS ONE ADDITION NOT NAMED IN THE PLAN: SectionOverlay.tsx carried the identical 'RETURN TO SOL' string as CollectorCard (the plan's D5.1 entry named only CollectorCard explicitly) -- updated too, since leaving it would have shipped a fresh inconsistency in the act of fixing an old one, and the rename's own stated rationale ('one-phrase-per-action') applies uniformly rather than to one component by name. A GENUINE TEST-HARNESS INCIDENT, DIAGNOSED METHODICALLY RATHER THAN WORKED AROUND BLINDLY (CLAUDE.md #14): the first two isolation attempts at the drag-persistence spec failed with what LOOKED LIKE a code defect (a locator.evaluate call timing out at 120s, or homeLookOverridden never flipping true within a 5s poll) -- but ruled out as CPU contention FIRST (this session's own prior lesson from an earlier accessibility flake): confirmed via tasklist that zero stray node processes were running, re-ran clean, STILL failed identically 5/6 times. Rather than accept 'flaky, move on', called the engine's _pick() DIRECTLY via page.evaluate (bypassing Playwright's input layer entirely): returned INSTANTLY and CORRECTLY (hoverId: null), proving the application code was never at fault. Then dispatched real PointerEvents straight at the canvas via canvas.dispatchEvent (still genuine DOM events reaching the real _bindPointer listeners, just not routed through Playwright's CDP-level OS input simulation): _dragging, _yaw/_pitch, and _homeLookOverridden all updated exactly as expected, reliably, every time. CONCLUSION: Playwright's page.mouse.move() hangs unreliably against this SPECIFIC scene state (idle, home-orbit, slow ambient revolution, no warp/animation driving continuous frames) -- the repo's ONE existing precedent test using page.mouse for a drag (engine-select.spec.ts's 'free-look drag stays authoritative') only ever does so MID-WARP, while the scene is continuously animating, and that distinction is exactly what breaks here. All pointer interaction in the new spec (drag AND hover AND click) now goes through the same proven dispatchEvent technique -- recorded as a genuine environment/harness finding for any future spec touching the idle home-orbit state, not silently patched around with a longer timeout. GATE: 729/729 unit (+10), 129/129 E2E zero failures 36.8min (+4 -- the D5.1 rename changed only locator strings in FOUR existing test files, named changes per CLAUDE.md #15, no assertion loosened). Determinism: the new spec ran clean across 3 separate passes this session (4/4 initial fixed-version run, then 8/8 on a --repeat-each=2 determinism pass) -- 12 total clean passes after the dispatch-technique fix landed. Build/budgets/lint/typecheck all green. OWNER-EYES VISUAL CONFIRMATION STILL OUTSTANDING, named honestly rather than implied -- same harness constraint recorded in TR-095/096 (Browser pane does not composite frames in this environment). What specifically needs eyes: does the drag now feel natural at the moment auto-aim first disengages, does the planet's visual occlusion boundary match its rendered silhouette exactly (should coincide by construction but only a real frame proves it), and the D5.1 label text at a genuinely narrow (<400px) viewport. D5-AC5 (the plan's own post-approval label-comprehension sanity check) has not run -- recorded open, not silently dropped. The occlusion fix is GENERAL, not Earth-specific (applies to whichever body the shared planet sphere currently shows, gated on isVisible) -- only the home-orbit case was reported and is directly tested; the same class of bug at a non-Earth arrival has no dedicated regression test in this pass."
  },
  {
    "project": "ishanjain-portfolio",
    "deliveryPlan": "PF-11-cinematic-journey-and-scale-honesty",
    "phase": "D4.1 -- arrival vista dismissal (owner R7)",
    "detail": "ArrivalVista.tsx: real role=dialog (pointer-events-auto, aria-label, focused on mount / restored on unmount via existing moveFocusTo/restoreFocusTo), z-[65] (above #ij-mission-bar's 62 -- the TR-086 click-interception fix), click-anywhere-except-the-button and Space-at-dialog-level dismiss, pointer-media-queried hint copy replacing the dead-on-touch 'HOVER THE BODY' line. SpaceScene.tsx: removed the 5.5s vistaTimeoutRef auto-dismiss entirely; new dismissVista(via) is the one path that closes the vista (arrivedId/camera untouched), records the funnel event, sets a sr-only 'Resumed flight at {name}' announcement, and arms a 300ms click-swallow window (a capture-phase pointerdown listener on document, scoped to the engine element, that stops the engine's own _dragStart from ever arming without disrupting its pointerup _dragging reset). New src/lib/focus-utils.ts export useEscapeStack -- a generic priority dispatcher (exactly one active layer's onEscape fires per Escape press) -- replaces the old blanket close-everything handler; wired with four layers in order card -> vista -> suggestions -> sectionOpen. Named test change: tests/e2e/funnel.spec.ts's OPEN COLLECTOR CARD test had two workarounds (the 5.5s timeout race, the z-index click-interception) that this slice's fixes made unnecessary; both removed, now a real .click(). New tests/unit/focus-utils.test.ts (5 tests, useEscapeStack) and tests/e2e/vista-dismissal.spec.ts (7 tests, D4-AC1/AC2/AC5/AC6).",
    "status": "complete",
    "start": "2026-07-25T00:00:00Z",
    "end": "2026-07-25T00:00:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "TR-098. Scoped to D4.1 only, per the owner's own D4 breakdown (D4.2 field-object travel parity, D4.3 card polish/focus trap/hover hygiene, D4.4 content pass all untouched). ONE DOCUMENTED DEVIATION FROM THE IMPLEMENTATION PLAN: its D4.1 bullet says Escape is handled 'at the dialog level' alongside Space, but it isn't -- Escape is handled EXCLUSIVELY by the new global useEscapeStack, not also inside ArrivalVista, because focus lands inside the dialog on mount and a native window-level Escape listener plus a React onKeyDown on the dialog root would both fire for the same keypress (harmless in final state but a duplicate funnel record and a needless second dispatch) -- single-owner-per-key beats matching the plan's literal phrasing. THE FOUR-LAYER ESCAPE STACK IS NOT SPECULATIVE SCOPE even though card+vista never actually coexist in this codebase's state machine today (every path that opens one nulls the other): vista+suggestions genuinely CAN coexist right now, because D4.3's focus trap for the card hasn't landed yet and nothing yet stops Tab from reaching the search input while a vista is still shown. GATE-RUN INCIDENT, ROOT-CAUSED (CLAUDE.md #14/#27): the first full-suite run showed 2 failures, both in the new vista-dismissal.spec.ts, both the SAME root cause -- the new tests never called test.setTimeout(...), so Playwright's default 30s per-test ceiling landed mid-poll on a real Sirius journey in an environment where other specs already budget 45-180s for exactly this reason (SwiftShader here is measurably slower/more variable than typical CI, TR-032/033/081/094/096/097). A compounding factor, confirmed after the fact: a Browser-pane manual-regression attempt was running CONCURRENTLY with that background E2E run -- the same CPU-contention class TR-096 already diagnosed (this session independently rediscovered it before checking the precedent). Fixed by adding test.setTimeout(90000) to all seven tests (a missing budget added, not a loosened assertion) -- isolated per CLAUDE.md #27 (never re-run the full suite to characterize one failing spec): vista-dismissal.spec.ts alone, 3 passes (7/7, 7/7, 14/14 on --repeat-each=2) -- 21/21 clean with no concurrent process. The subsequent full-suite re-run (the actual gate) then hit ONE unrelated failure -- ascent.spec.ts's LAUNCH-ascent test, a 6000ms ascentMode poll timing out -- in a code path (beginAscent/AscentSkip/PreFlight/ascentMode) this slice's diff never touches at all. Isolated the same way: 3 passes (5/5, 10/10 on --repeat-each=2) -- 15/15 clean, same intermittent-timing-under-load class as this repo's own documented GAP-17 flake. GATE: 734/734 unit (+5), 135/136 E2E on the final gate (+7 new, all passing; the one failure isolation-confirmed unrelated). Build/budgets/lint/typecheck/docs:check all green. OWNER-EYES VISUAL CONFIRMATION STILL OUTSTANDING, named honestly -- same standing Browser-pane compositing constraint as TR-095/096/097/TR-088 (a travelTo call issued through the pane left warp.mode stuck at 'aim' for 20s+ with zero progress, matching the 'rAF never fires while backgrounded' finding first noted at TR-081); verified instead via the pane for static non-animated checks (console-clean boot, PreFlight dossier structure, a travelTo call producing the correct aim-phase warp object) and via the real Playwright E2E suite (a genuinely different, compositing rendering pipeline) for every behavioural claim. D4-AC3 (fresh hover / no stale tooltip under a later card) deliberately NOT covered by the new E2E spec -- its actual fix (clearing stale hover state, gating HoverTooltip on !cardId && !vista) is D4.3's explicit scope, not D4.1's; an assertion against behaviour D4.3 hasn't shipped would be testing the wrong slice. mcOpen (types.ts) noted as dead state (nothing currently sets it true) but not investigated/removed -- out of scope, flagged for whoever next touches MissionControlBar/mobile-menu state."
  },
  {
    "project": "ishanjain-portfolio",
    "deliveryPlan": "PF-11-cinematic-journey-and-scale-honesty",
    "phase": "D4.2 + D4.3 + D4.4 batch 1 -- field-object travel, card polish, cluster content (owner R7/R8)",
    "detail": "D4.2: babylon-engine.ts new private _fieldBody(id) synthesizes an fs- travel target (position/dir from the retained _field store, ly from fieldInfo(i) not the legacy L*3.9 formula), wired into travelTo's body-resolution chain BEFORE the D3.3 policy gate so mid-warp field-object picks queue correctly; new pure export fieldStarTarget(id, positions, count) owns the id-parse/bounds/3-float-stride contract, unit-tested (12 tests). aimAt (?testhooks) extended to resolve fs- ids via the same _fieldBody path. D4.3: CollectorCard.tsx gets a real focus trap (new trapFocus(container, event) in focus-utils.ts, reusing D4.1's moveFocusTo/restoreFocusTo rather than re-authoring), aria-labelledby the name heading, 24x24px style-dot buttons wrapping the original 14px decorative dot (aria-label/aria-pressed added), stat-bar fill divs aria-hidden, vestigial e.r==='field' ternary deleted. SpaceScene.tsx: hover:null added explicitly at both card-open sites, HoverTooltip gated on !cardId && !vista, CTA string 'CLICK TO OPEN DOSSIER' -> 'OPEN COLLECTOR CARD'. D4.4 batch 1: new override-only src/data/celestial/celestial-content-overlay.js patches f/lo for the 25 deferred cluster ids (never hand-edits celestial-clusters.js, CLAUDE.md #22); wired into SpaceScene.tsx's import chain as its own .then() sequenced strictly AFTER the base-catalog Promise.all resolves. New tests/unit/field-travel.test.ts (12), tests/unit/focus-utils.test.ts +7 (trapFocus), tests/unit/celestial-content-overlay.test.ts (6), tests/e2e/field-travel.spec.ts (4), tests/e2e/collector-card.spec.ts (6). New docs/analysis/2026-07-25-card-content-sources.md (per-cluster source list).",
    "status": "complete",
    "start": "2026-07-25T00:00:00Z",
    "end": "2026-07-25T00:00:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "TR-099. Continuation of the same session as TR-098; D4.2/D4.3 implemented on Opus 4.8, D4.4 content research and the rest of this slice on Sonnet 5 after a mid-session model switch. TWO REAL DEFECTS IN THE IMPLEMENTATION PLAN'S OWN D4.2 PRESCRIPTION, caught before shipping rather than after (both corrected in the plan document itself, superseded not edited away): (1) STRIDE -- the plan said port space-engine.js's field-index math verbatim, but the legacy fieldF array is 4 floats/record while Babylon's _field.positions is 3; a literal port silently reads a NEIGHBOURING star's coordinates, no crash, no visual error, just travel to the wrong object. (2) DEPTH CONVENTION -- the plan's prescribed ly:L*3.9 is correct only for the base HIP field (type 0); PF-10's typed bonus layers are log-depth (fieldInfo's type>0 branch), where L*3.9 reports a record at roughly 10^3 ly instead of its real distance -- not cosmetic, since that value drives warpDurationForLy, localFieldVisibility (D2.2's extragalactic collapse), and _farDestLy (the impostor's size), so the literal port would have left D2.2 dead for exactly the deep-field objects it exists to serve. Shipped routes ly through fieldInfo(i) itself, the same source the tooltip/card already read. AN E2E TEST FOUND ITS OWN FIRST-DRAFT BUGS DURING DEVELOPMENT, NOT IN THE FINAL GATE (worth recording as method, not just outcome): a draft field-info test scanned the CATALOG TAIL for a log-depth record on the (wrong) assumption bonus layers are appended there, found none, and SILENTLY SKIPPED ITSELF -- caught by noticing the skip rather than trusting a passing run; fixed by a throwaway histogram probe (since deleted) that found typed records actually live around index ~118,000, then scanning forward instead. Separately, a hover test picked field index 5000 naively and it turned out to be within the 34px body-pick radius of a real catalog body (ngc752) at that camera angle, so the draft was testing catalog-body travel while CLAIMING to test field travel -- fixed with a clearFieldIndex helper that scans for a field star angularly clear of every catalog body/station first. D4.3'S DOCUMENTED PLAN DEVIATION: the implementation plan describes moveFocus(el,restoreTo) and useEscapeStack(layer,handler) as NEW functions D4.3 creates: both already existed with DIFFERENT signatures by the time D4.3 started, because D4.1 landed first exactly as the plan's own 'if D1 lands first, create it here' note anticipated, and shipped moveFocusTo(el)/restoreFocusTo(el) as two functions plus useEscapeStack(layers: readonly EscapeLayer[]) taking a priority array. D4.3 reused them as built rather than renaming D4.1's shipped code to match this doc's literal signatures after the fact; only trapFocus was genuinely new. TWO GENUINE GATE-RUN INCIDENTS THIS SESSION, BOTH ROOT-CAUSED (CLAUDE.md #14/#27), NEITHER A PRODUCT DEFECT: (1) a SELF-INFLICTED INSTRUMENT CORRUPTION -- an npm run build was run to pick up D4.3's source changes WHILE an already-in-flight npm run test:e2e gate (started for D4.2) was still executing against the previous dist/; Astro's static build overwrote files on disk out from under the live Playwright run mid-flight, so later tests in that run were served a mix of old and rebuilt files. Symptoms: the run's own captured output was truncated to its last 60 lines and it reported one failure (mid-warp-input.spec.ts, a D3.3 test this slice's diff never touches) that did NOT reproduce on a clean re-run. Diagnosed via TaskOutput(block:false) confirming the run was still genuinely executing rather than hung (the working theory before that check, given how unusually long the run had taken), then via re-running the FULL gate with nothing else touching dist/ concurrently, which then completed cleanly -- generalizes TR-096's CPU-contention finding: it is not just concurrent LOAD that invalidates a running gate, concurrently REBUILDING THE ARTIFACT THE GATE IS SERVING invalidates it outright and must never happen. The corrupted run's results were discarded entirely, not reported or massaged into a passing summary. (2) two new collector-card.spec.ts tests failed on their first real run, both genuine test-code bugs: 'focus restored to the trigger on close' assumed the vista's OPEN COLLECTOR CARD button survives closing the card, but it doesn't -- clicking it dismisses the vista in the SAME action that opens the card, so restoreFocusTo's isConnected guard correctly no-ops against an already-detached element (that guard is unit-tested directly); fixed by asserting the real contract instead (document.activeElement lands on BODY, the browser's own fallback, never orphaned inside the hidden dialog). 'Hovering the parked body shows the renamed CTA' assumed arrival leaves the camera looking at exact viewport-centre; it doesn't reliably enough -- idle drift plus the ARRIVE_STANDOFF approach geometry left it hovering a background SDSS galaxy (NGC 2146) instead of Sirius; fixed with the same aimAt + settle-frame + throttle-aware retry technique D4.2's own field-travel.spec.ts had already established for the identical class of problem. D4.4 BATCH 1 OWNER-APPROVAL PROCESS FOLLOWED AS SPECIFIED, NOT SKIPPED: the overlay module and its full per-cluster source list (docs/analysis/2026-07-25-card-content-sources.md) were built and unit-tested but deliberately left UNWIRED from SpaceScene's live import chain until the owner reviewed and explicitly approved batch 1 mid-session (AskUserQuestion, 'Approve batch 1, wire it live now') -- only then was the one-line .then() wiring added and the build/gate re-run. Every lo entry traces to a WebSearch performed this session against a primary or authoritative secondary source (arXiv, NASA/ESA, Wikipedia citing primary literature) -- no fact was drafted from memory alone without a corresponding search confirming it; numerical facts (distance/age/magnitude) were NOT re-derived, reusing each record's already-Astra-reviewed st field as the factual anchor. Batch 2 (the 41+ NGC2000 nebula entries) is NOT started -- recorded open, not silently dropped. FINAL GATE: 759/759 unit (+25 over TR-098's 734 -- 12 fieldStarTarget + 7 trapFocus + 6 celestial-content-overlay tests), 146/146 E2E on the clean re-run, ZERO failures, 36.9 min (+10 new -- 4 field-travel.spec.ts, 6 collector-card.spec.ts, both isolated 3x clean before the final gate: field-travel 12/12, collector-card 18/18 across three passes after its two fixes). Build/budgets (JS gz 1191.3 KB / 1200, +4.6 KB for the batch-1 content)/lint/typecheck/docs:check all green. No existing test's assertion was loosened at any point. OWNER-EYES VISUAL CONFIRMATION STILL OUTSTANDING, named honestly -- same standing Browser-pane compositing constraint as TR-095/096/097/098. D4-AC5 (D5.1's post-approval label-comprehension check) remains unrun, carried forward again. Phase D4 is now functionally complete except D4.4 batch 2."
  },
  {
    "project": "ishanjain-portfolio",
    "phase": "D4 close-out -- isFarField fix + D4.4 batch 2 drafted (owner-requested verification)",
    "detail": "SpaceScene.tsx: isFarField(id) switched from catalog().find(...)?.ly to entryFor(id)?.ly -- the former always missed fs- (field-object) ids since entryForFieldStar synthesizes those on demand from the engine's live fieldInfo() and never writes them into window.CELESTIAL, so the HUD's MILKY WAY ASTERN line silently could never fire for a field-object arrival regardless of true distance. entryFor() already has the correct fs- fallback and is reused as-is, no new logic. New E2E test in field-travel.spec.ts (near/far control pair, isolated 4/4 clean across 2 runs before the full gate). D4.4 batch 2: 5 parallel research agents (one per ~8 objects) verified every one of the 41 NGC2000 ids' real-world identity via live search before drafting (not trusting supplied guesses), grounded every claim in a source found this session. New sibling module celestial-content-overlay-ngc2000.js (mirrors celestial-content-overlay.js's pattern, CLAUDE.md #22) + celestial-content-overlay-ngc2000.test.ts (6 tests) + docs/analysis/2026-07-25-ngc2000-card-content-sources.md. Deliberately kept OUT of SpaceScene.tsx's import chain (batch 1's overlay is already wired live, so adding batch-2 keys to that same file would ship them without a separate approval gate) -- staged exactly as batch 1 was before its own approval.",
    "status": "complete",
    "start": "2026-07-25T00:00:00Z",
    "end": "2026-07-25T00:00:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "TR-100. Owner asked to verify two specific open D4 items (isFarField, D4.4 batch 2 status) plus any other remaining D4 work, then opted (AskUserQuestion) to have batch 2 drafted now rather than left as a status-only report. TWO REAL DATA-QUALITY FINDINGS SURFACED DURING NGC2000 RESEARCH, FLAGGED NOT FIXED (celestial-ngc2000.js is generated/pipeline output, out of scope for a content-only overlay): (1) ngc2000-crescent-nebula's own ly field (5, ~1.5 pc) is very likely a data-pipeline error -- the real Crescent Nebula (NGC 6888) is ~4,700-5,200 ly away, roughly three orders of magnitude off; the authored text never states a distance figure for this id specifically, unit-tested directly so the anomaly can't leak into shipped copy. (2) ngc2000-butterfly-nebula and ngc2000-bug-nebula looked like a pipeline duplicate mid-research (same popular nickname, different catalog distances) but were confirmed as two genuinely different real objects by checking each record's own ra/dec/ly fields directly: ngc2000-bug-nebula's own d field reads 'NGC 6302 - nebula' with matching coordinates; ngc2000-butterfly-nebula's coordinates match Minkowski 2-9 (M2-9) almost exactly, confirmed via a live search this session against M2-9's published ~2,100 ly distance against this record's ly:2100. A related naming collision (ngc2000-box-nebula / ngc2000-little-gem-nebula both informally nicknamed 'Box Nebula') was resolved the same way by one research agent independently checking the source file's own d field rather than trusting the nickname -- also caught that the little-gem-nebula prompt's own suggested identity (NGC 6818) was wrong; the record's own d field said NGC 6445, and content was drafted for the object the data actually names. CORRECTION RECORDED AGAINST TR-099 (additive, not rewritten, per this repo's docs contract): TR-099's Known-limitations note 'D4-AC5 (D5.1's post-approval label-comprehension check) remains unrun' conflates two different criteria -- the real D4-AC5 (auto-timeout can't ambush) is already covered by vista-dismissal.spec.ts's dedicated test, shipped with D4.1/TR-098; the item TR-099 actually meant is D5-AC5, an unrelated and still genuinely open item. OWNER-EYES VISUAL CONFIRMATION ATTEMPTED AGAIN via astro-preview: page-screenshot equivalent failed immediately with the same standing 'Browser pane not displayed, not compositing frames' message as TR-095 through TR-099; NEW EVIDENCE this session (not just a recollection of the prior finding) -- a live travelTo() issued through the pane left warp.mode stuck at 'aim' with arrivedId null after 10+ real seconds, and the async PF-10 bonus-layer field merge never completed after ~30s of waiting, both consistent with TR-081/088/098's 'rAF never fires while the tab isn't actively rendering' finding; console stayed clean throughout, the one class of check the pane can still confirm regardless of compositing. FINAL GATE: 765/765 unit (+6 over TR-099's 759 -- the new celestial-content-overlay-ngc2000.test.ts), 147/147 E2E on the clean gate, ZERO failures, 42.5 min (+1 new -- field-travel.spec.ts's isFarField test, isolated 4/4 across 2 runs before the full gate). Build/budgets (JS gz 1191.3 KB / 1200, UNCHANGED from TR-099 -- the isFarField fix is pure logic and D4.4 batch 2 is staged unwired, neither ships new bytes yet)/lint/typecheck/docs:check all green. No existing test's assertion was loosened. D4.4 batch 2 is now the one item in Phase D4 blocked purely on an owner approve-and-wire decision, not further engineering. Owner-eyes visual confirmation remains the one standing open item across the whole phase."
  },
  {
    "project": "ishanjain-portfolio",
    "phase": "D4.4 batch 2 -- approved and wired (owner-requested, follows TR-100)",
    "detail": "SpaceScene.tsx: one-line addition to the existing import chain -- celestial-content-overlay-ngc2000.js now loads via its own .then(), chained after batch 1's overlay, same non-Promise.all-ordering reasoning as batch 1's own sequencing comment. No other production code changed. Header comments in the overlay module, its unit test, and the source-list analysis doc updated from staged-not-yet-wired to reflect approval (docs-only). budgets.config.mjs: totalJsGz raised 1200 -> 1205 with a comment + TR-101 reference, per that file's own documented rule against silent raises -- the measured 1200.9 KB (0.9 KB over the prior ceiling) is deliberate, owner-approved content growth (41 real sourced entries), not drift.",
    "status": "complete",
    "start": "2026-07-25T00:00:00Z",
    "end": "2026-07-25T00:00:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "TR-101. Owner approved D4.4 batch 2 (drafted/staged in TR-100) and asked to wire it in the same session. Isolated the 9 NGC2000/collector-card/field-travel-relevant specs first (9/9 clean, 3.6 min) before the full E2E gate, per CLAUDE.md #27 verification economics -- this is a docs+config+one-line-wiring change layered on content already unit-tested in TR-100, so the gate's job was confirming the wiring itself doesn't regress anything already covered, not re-deriving the content's correctness. BUDGET INCIDENT, ROOT-CAUSED AND HANDLED PER THE REPO'S OWN DOCUMENTED PROCESS, NOT A DEFECT: total JS (gz) measured 1200.9 KB against the prior 1200 KB ceiling, a 0.9 KB overage. This is exactly the case budgets.config.mjs's own header anticipates -- deliberate, owner-approved content growth (41 real sourced NGC2000 field-notes/lore, proportional to batch 1's own +4.6 KB for 25 entries) gets the ceiling walked in with a comment and TR reference, not the content cut to fit or the budget silently raised without explanation. Raised to 1205 (smallest ceiling that clears the measured value with a small buffer, not a round-number jump) and re-verified green. FINAL GATE: 765/765 unit (unchanged from TR-100 -- no new tests needed, celestial-content-overlay-ngc2000.test.ts already exercises the real module-loading path this wiring now uses live), 147/147 E2E on the clean gate, ZERO failures, 40.2 min (unchanged count -- no new E2E tests for this slice). Build/budgets (1200->1205 KB, documented above)/lint/typecheck/docs:check all green. No existing test's assertion was loosened. PHASE D4 IS NOW FULLY COMPLETE except the one standing item across the whole phase: owner-eyes visual confirmation, a harness constraint (Browser-pane backgrounded-tab compositing/rAF), not a product defect, unchanged in kind since TR-095."
  },
  {
    "project": "ishanjain-portfolio",
    "phase": "post-D4 owner manual-regression feedback -- arrival camera aim + classic-view scroll jank",
    "detail": "babylon-engine.ts: the k>=1 arrival branch's w.target case now computes the unit vector from the camera's just-snapped standoff position to the body's world position and sets _yaw/_pitch AND _camQuat directly from it, instead of leaving orientation wherever the chase-look quatDamp (driven by travel direction, not body direction) had converged to. Reuses the exact vector math the ?testhooks-only aimAt() method already had in a code comment describing this precise failure mode -- made real for every arrival rather than only a harness. w.home arrivals untouched (R16's home-orbit look-at-Earth system was already correct). SpaceScene.tsx: the station-sprite positioning rAF loop's heroCopy 'dodge' lookup (3 getBoundingClientRect() calls, each forcing a synchronous layout flush) gated on `atHome && travel` instead of `atHome` alone -- it was already unreachable in classic view (show requires travel regardless of dodge), so this removes pure waste with zero behavior change in travel mode. New E2E test in collector-card.spec.ts proving the arrival-aim fix without the aimAt crutch a sibling test needed.",
    "status": "complete",
    "start": "2026-07-25T00:00:00Z",
    "end": "2026-07-25T00:00:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "TR-102. Owner ran the manual-regression checklist handed off after TR-101 and reported seven items across a single message. Investigated all seven via parallel research agents before touching any code (arrival-camera-framing mechanism, classic-view scroll/render-loop behavior, Babylon.js 9.2.1's real existence and actual documented changes) rather than guessing at fixes. TWO ROOT-CAUSED AND FIXED THIS PASS, BOTH TURNED OUT TO SHARE A ROOT CAUSE WITH A SECOND REPORTED SYMPTOM: items #2 ('camera pointing away from Mars on arrival') and #4 ('the DSO is off-screen after dismissing the vista/card') are the exact same bug -- arrival position was already correct (this.cam = w.to, pre-existing) but orientation was derived from travel direction, not from the direction toward the body, and neither dismiss handler ever re-aims the camera (by design -- 'ship stays parked exactly where it landed'), so a bad arrival aim simply persisted through both dismiss paths. A DISCOVERY DURING INVESTIGATION THAT CHANGED THE PLAN: the owner's other ask attached to #2 -- 'zoom out a little... so the star field is also partially visible' -- is NOT a safe standalone tweak. ARRIVE_STANDOFF (38 units, the arrival distance) is unit-tested as an EXACT equality to both ASCENT_END_STANDOFF (the launch cinematic's end distance) and HOME_ORBIT_RADIUS (R16's home-orbit radius), and separately feeds visiblePatchDegrees, the virtual-texturing patch-visibility budget calculation -- at the current distance a PLANET_SPHERE_RADIUS=26 sphere already subtends roughly 86 degrees of frame. A naive constant bump would risk breaking the ascent cinematic's framing, the home-orbit radius's own tuned geometry, or the VT tile-residency budget, none of which were re-verified this pass -- deliberately NOT implemented blind; reported to the owner as needing either a genuinely new, decoupled planet-class distance or a coordinated change across all three systems. CLASSIC-VIEW JANK (#6) ROOT-CAUSED via a parallel research pass: the station-sprite rAF loop's hero-copy dodge computation ran 3 getBoundingClientRect() calls (forcing synchronous layout) every animation frame regardless of mode, even though the sprite-visibility gate downstream already required travel mode to ever matter -- fixed by extending the existing atHome gate to atHome && travel, a pure waste-removal with no behavior change in travel mode. Flagged, NOT the full fix: the deeper cause (Babylon's own render loop ticking every per-frame function unconditionally in classic view, only cadence-halved past 55vh scroll) is the ALREADY-TRACKED, PARTIALLY-FIXED GAP-13 from the PF-09 WebGL-to-Babylon cutover gap analysis -- closing it further is a larger, separate engine-tick-throttling change, out of scope for this pass. THREE ITEMS DELIBERATELY NOT IMPLEMENTED, REPORTED WITH FINDINGS INSTEAD: #1 (local star-field density looks low-resolution vs. the far/SDSS field) -- root-caused as real (168,959 local stars, most sub-pixel, vs. 3.64M SDSS points with a 1.15x size multiplier -- ~21x the point count) but very likely ALREADY the exact gap PF-11's own D8 phase exists to close (ADR-0010's D8.1 decision: ALL 2,552,302 Gaia DR3 Tiny stars ship as a D9 opt-in layer) -- not a new gap to fix out of sequence. #3 (user-adjustable zoom control, keyboard/wheel/on-screen scale) -- confirmed as a genuine new feature request, not scoped or implemented this pass; connects naturally to the standoff-distance question above (a working zoom control could make the exact DEFAULT distance less critical). #7 (Babylon.js 8.56.2 -> 9.2.1 upgrade) -- verified via WebSearch that 9.2.1 is a real, current stable release (April 2026, not a hallucinated version) with genuine relevant gains (Frame Graph v1 promoted to full feature with ~40% GPU memory savings in some scenarios per Babylon's own 9.0 announcement, Clustered Lighting, Volumetric Lighting, Havok FloatingOrigin Multi-Region) -- but confirmed via the official changelog/breaking-changes docs that Babylon's own 9.0 changelog entry lists no explicit 'Breaking Changes' bullets (unusual for a major version, and not fully trustworthy as 'nothing breaks' given the docs' own breaking-changes.md file stops recording at 8.10.1), and one structural note ('removed packages/lts layer, wired public packages directly to dev') is a real subpath-import-stability risk this repo's own CLAUDE.md #1 rule depends on -- verified this repo's exact tree-shaking assumption (sideEffects field) still holds in 9.2.1's published package.json as one spot-check, but a full spike (shader-twin compilation, thin-instance/Havok compatibility, budget-gate impact) plus an ADR per this repo's documentation contract is the right next step, not a same-turn version bump. FINAL GATE: 765/765 unit (unchanged -- neither fix touches a unit-tested pure function; ARRIVE_STANDOFF itself untouched so every test asserting equalities to it still passes unchanged), 148/148 E2E on the clean gate, ZERO failures, 42.5 min (+1 new -- collector-card.spec.ts's arrival-aim test, isolated 4/4 clean -- 1 run plus 3 repeats -- plus the 14 collector-card/mobile-responsive specs most likely to interact with either change, all clean, before the full gate). Build/budgets (JS gz 1200.9/1205 KB, UNCHANGED from TR-101 -- both fixes are pure logic, no new bytes)/lint/typecheck/docs:check all green. No existing test's assertion was loosened."
  },
  {
    "project": "ishanjain-portfolio",
    "phase": "post-D4 owner feedback -- planet-class arrival standoff + user zoom control (Vega SHOT-BRIEF)",
    "detail": "ship-dynamics.ts: new PLANET_ARRIVE_STANDOFF (80, decoupled from ARRIVE_STANDOFF which stays untouched), PLANET_SPHERE_RADIUS_FOR_ZOOM/PLANET_ZOOM_NEAR_MARGIN (literal duplicate of planet-sphere.ts's constant, agreement unit-tested), ZOOM_MIN_MULT/ZOOM_MAX_MULT/ZOOM_STEP/ZOOM_LAMBDA, new pure clampZoomDistance + dampScalar (the non-wrap-aware sibling of the existing dampAngle). babylon-engine.ts: travelTo picks the new standoff for planet/moon/dwarf-typed bodies only; new _parkedTargetPos/_camBearing/_zoomDist/_zoomTargetDist/_zoomRestDist fields; arrival extends TR-102's aim-fix block to also set a fixed camera-to-target bearing and reset zoom; idle branch eases zoom distance every frame (instant snap under reduced motion) and repositions the camera along the fixed bearing (a no-op unless zoom has actually been touched), or feeds the eased distance into homeOrbitPosition's existing radius parameter for the home-orbit case; new _adjustZoom(steps) method wired to a new wheel listener (_bindPointer) and +/-/=/_ keys (_bindKeys). HUD hint line and engine aria-label updated. New tests/e2e/camera-zoom.spec.ts (5 tests) plus 8 new unit tests across ship-dynamics.test.ts and planet-sphere.test.ts.",
    "status": "complete",
    "start": "2026-07-25T00:00:00Z",
    "end": "2026-07-25T00:00:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "TR-103. Owner asked (after TR-102's report) to proceed with the camera-framing/zoom design AND the Babylon 9.2.1 upgrade spike in parallel, explicitly requesting sub-agents for efficiency. VEGA SHOT-BRIEF OBTAINED FIRST, PER PROCYON'S OWN DELEGATION DOCTRINE: invoked the vega skill directly (camera-grammar/framing decisions route to Vega, same-turn on inline surfaces) rather than guessing standoff numbers -- returned a full design (80-unit planet standoff targeting ~37.7 degree angular diameter vs the old 86.3 degree edge-to-edge framing, zoom-as-dolly-not-FOV, fixed-bearing composition with free-look, absolute-vs-multiplicative bounds policy, damping curve and timing) before any implementation. TWO PARALLEL BACKGROUND AGENTS LAUNCHED CONCURRENTLY WITH THE CAMERA-FEATURE IMPLEMENTATION: (1) a dependency audit (read-only, no installs) confirmed every package current or a safe patch/minor bump except eslint-plugin-astro (1 to 3, two major versions, needs a new eslint-plugin-jsx-a11y peer dependency and a parser-backend change -- real work, not a blind bump) and testing-library/jest-dom (6 to 7, low actual risk since the new peer is already satisfied transitively) and sharp (0.34 to 0.35, pre-1.0 so a 0.x bump can carry breaking changes, worth testing against the asset-pipeline scripts specifically); (2) a worktree-isolated Babylon.js 8.56.2 to 9.x upgrade spike (registry has moved to 9.18.0 since the original 9.2.1 confirmation) found lint/typecheck/unit tests (729/729) and Havok physics all clean and unchanged, zero peer-dependency conflicts, no barrel-import regression (largest single chunk actually shrank), but a genuine +185.8 KB gz (+15.7%) total bundle growth from Babylon 9's own internal restructuring -- the glTF loader's default material-creation path now transitively pulls in a new Gaussian-Splatting-mesh chunk via an OpenPBR material mixin, entirely inside Babylon's own dependency graph, not caused by anything in this repo's subpath-import discipline. VERDICT RECORDED AS NEEDS FIXES, NOT ADOPTED: would require either a deliberate, justified budget-ceiling raise or investigating whether the glTF loader's material path can be steered away from the splatting mixin, plus a full Playwright E2E run (deliberately not run in the spike given its ~40+ minute cost against a throwaway experiment) before real adoption -- correctly NOT implemented in the production repo this pass; the spike's own isolated worktree and throwaway branch were cleaned up after its findings were captured. A REAL PROCESS MISTAKE CAUGHT DURING THE CAMERA FEATURE'S OWN VERIFICATION: the first camera-zoom.spec.ts E2E run showed every arrival distance at the OLD 38-unit value instead of the new 80, which read at first glance like the type-based branch in travelTo silently failing -- actually dist/ was stale, because npm run build was not re-run after the zoom code landed before Playwright's webServer served it (Playwright serves a prebuilt dist/, CLAUDE.md's own standing rule) -- caught specifically because the readings were suspiciously IDENTICAL to pre-feature behavior rather than assumed to be a fresh defect; rebuilding resolved it immediately, no product code was at fault. TWO GENUINE TEST-DESIGN ISSUES SURFACED AND FIXED DURING ISOLATION RUNS, NOT WORKED AROUND: (1) the bounds-clamping test used a fixed 500ms wall-clock wait for the zoom spring to converge after an aggressive single large wheel delta, which undershot convergence under this environment's slower/contended render rate -- root-caused to SHIP_MAX_DT's existing 50ms-per-frame dt cap meaning the spring's convergence tracks ACCUMULATED IN-ENGINE dt, not wall-clock time 1:1, under a low real framerate (same timing-sensitivity class this repo's own TR-032/033/081/094/096/097 already document for other damped values) -- fixed with expect.poll instead of a fixed-duration guess, matching this suite's own established idiom for every other asynchronous-settle assertion. (2) a test asserting the arrived planet stays hoverable/centered after zooming initially used _hoverId and failed with hoveredId null -- traced to a PRE-EXISTING, NOT INTRODUCED HERE, quirk in _pick's planet-sphere occlusion check: it nulls a candidate whenever the candidate's own catalog-point distance exceeds the sphere's near-surface intersection distance along the cursor ray, which is ALWAYS true for the arrived body's own point once its sphere is rendered in front of it (the sphere's near surface is necessarily closer than its own center) -- every EXISTING hover-after-arrival test in this suite deliberately uses a star target specifically to avoid this exact case, which is why it had never surfaced before; fixed the new test by reading _projectBody's own sx/sy screen-projection output directly (computed before the occlusion check runs) instead of _hoverId, which is what the test actually needed to prove (screen position stays centered) rather than what it happened to reach for first. FINAL GATE: 773/773 unit (+8 over TR-102's 765 -- clampZoomDistance/dampScalar tests plus the PLANET_SPHERE_RADIUS_FOR_ZOOM agreement and angular-target sanity checks), 153/153 E2E on the clean gate, ZERO failures, 42.2 min (+5 new -- camera-zoom.spec.ts, isolated 10/10 clean across two full runs before the final gate). Build/budgets (JS gz 1201.3/1205 KB, +0.4 KB -- pure logic plus a couple of new event listeners)/lint/typecheck/docs:check all green. No existing test's assertion was loosened. Owner-eyes visual confirmation remains the standing gap across this whole session, unchanged in kind since TR-095 -- specifically whether the new 80-unit default reads right and whether zoom feels responsive, neither judgeable from code alone."
  },
  {
    "project": "ishanjain-portfolio",
    "phase": "D5.2/D5.3/D5.4 -- Where-To console v2 (search, class rows, missing-body audit)",
    "detail": "New src/lib/destination-search.ts (ranked matching, id/designation normalisation, empty-query featured list, honest totalMatches); MissionControlBar.tsx rebuilt as a real ARIA combobox with the suggestion dropdown now opening upward (was downward, ran off the bottom of the viewport at every width -- a real pre-existing defect, not mobile-only). New optional engine method nearestFieldOfType(typeByte), memoized per camera-position epoch, delegating to a new pure nearestOfType in star-catalog.ts for off-GPU testing. New src/data/celestial/celestial-missing-moons.js: real curated entries for Mimas/Iapetus/Phobos/Triton/Charon (img:null, no source imagery exists), positions preserving real orbital-radius rank against already-shipped Saturn moons where a sibling exists. New destination-search-surface.test.ts (D5-AC3 guard: travel surface subset-of search index, forever).",
    "status": "complete",
    "start": "2026-07-29T00:00:00Z",
    "end": "2026-07-29T00:00:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "TR-108. Owner asked to proceed through PF-11 Phases 5 and 6 in sequence, using sub-agents and sister skills as needed, stopping only for genuine questions. ONE DRAFTED SCOPE ITEM BUILT THEN REVERTED: the delivery plan's literal 'mid-warp feedback (IN TRANSIT * ARRIVING AT {dest})' wording was implemented as a generic any-active-warp console label, then reverted after finding it broke mid-warp-input.spec.ts's existing TR-096 contract that the console shows its RESTING label throughout an abort -- re-reading the plan's own phrasing in context confirmed the ask was always the QUEUE state specifically (already shipped, TR-095/096), not a second independent indicator; recorded in the implementation plan so a future reader doesn't re-attempt it. Verified end-to-end in the browser pane (DOM/structural, not pixel -- standing harness constraint): featured list, ranked search, honest truncation, and station searchability all confirmed live via get_page_text against a real boot. FINAL GATE (isolated targeted verification per slice throughout; the mandatory full-suite close is recorded in TR-109 since D6 landed on top in the same session): 786 unit after D5.2, 790 after D5.3, 795 after D5.4; 6+1+1 new E2E tests (destination-search.spec.ts), all passing in isolation alongside the existing accessibility/space-scene/collector-card/engine-select suites re-run as regression checks after each slice."
  },
  {
    "project": "ishanjain-portfolio",
    "phase": "D6.2/D6.6/D6.7 -- belt frame re-expression, atlas cells + Jupiter moon ranks, PF-10 header correction -- PLUS the combined-session full-suite gate",
    "detail": "babylon-asteroids.ts: new toBeltSpace/fromBeltSpace rotation helpers (OBLIQUITY_J2000_DEG=23.439281, mirroring asteroid-kepler.mjs's eclipticToEquatorial), every belt-model function (beltPullAccel/beltDensityAt/beltOrbitPosition) re-expressed to convert world<->belt-local correctly (translate-then-rotate in, rotate-then-translate out, matching eclipticToWorld's own composition order -- a real bug found and fixed mid-implementation, not the first draft). heliocentricRadius needed ZERO code change (rotation-invariant by construction), which is also the proof the final design is correct. Both shader twins updated with the identical basis-change block. Real Gaia DR3 data regenerated with --frame equatorial (154,662 objects, Kirkwood gaps unchanged). asteroid-kepler.mjs: new equatorialToEcliptic, distanceToBeltSpine gained the same frame parameter eclipticToWorld already had (the physics-subset SELECTION needed it too). Showcase route retired from m42 (peak belt density collapsed 0.98->0.0001, Orion isn't zodiacal) to Aldebaran (real zodiacal star, peak density ~0.96) rather than retuning the belt's center to force the old crossing -- a numerically-tested, not assumed, replacement. New scripts/patch-atlas.mjs (three-mode, never deletes): 3 new atlas cells for Tethys/Dione/Rhea in the one genuinely free (measured blank) macro-cell of the packed 16x16 grid, mozjpeg quality-90 re-encode chosen as the conservative middle point on a real budget tradeoff. celestial-catalog.js: Io/Europa ra/dec swapped for rank-honest Jupiter-moon offsets. PF-10 delivery-plan header: additive strikethrough correction of the stale '221 MB' figure. Astra REALISM-AUDIT of D6.2: no BROKEN PHYSICS found.",
    "status": "complete",
    "start": "2026-07-29T00:00:00Z",
    "end": "2026-07-29T00:00:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "TR-109. Continuation of the same owner request as TR-108 (proceed through PF-11 Phases 5 and 6). Invoked the astra skill directly for D6.2's REALISM-AUDIT per Procyon's own delegation doctrine (scientific realism routes to Astra), same-turn on this inline surface. A REAL CORRECTION SURFACED DURING IMPLEMENTATION, NOT AT REVIEW: the first basis-transform draft assumed ASTEROID_BELT.center rotated along with the belt ring; it does not (eclipticToWorld translates AFTER rotating, a plain world-frame offset since the belt's placement is a scene/gameplay choice, not real astrometry) -- caught by re-deriving the Sun's true world position analytically before trusting any test result, fixed before any test was written against the wrong model, and recorded in both the implementation plan and this TR rather than silently corrected. THE M42-TO-ALDEBARAN ROUTE SWAP WAS VERIFIED NUMERICALLY, NOT GUESSED: a small Node script computed peak belt density along several already-catalogued destinations' real sky directions against the corrected belt geometry before picking Aldebaran, and separately confirmed that retuning the belt's center to preserve the old m42 crossing would require an ~80-unit shift, rejected as reintroducing the same fitted-coincidence error class the slice exists to remove. TWO REAL ENVIRONMENT ISSUES HIT AND FIXED DURING D6.2, NEITHER A PRODUCT DEFECT: sharp/Node's file read-then-write-same-path pattern is flaky on Windows (fixed with a temp-file-plus-rename, patch-atlas.mjs); a naive JSON.stringify(map,null,2) re-serialization of atlas-map.json would have reformatted all 260+ pre-existing single-line entries into one-array-element-per-line, caught before committing and replaced with a textual splice that reparses-and-verifies its own output. ONE REAL TEST BUG CAUGHT AND FIXED BEFORE TRUSTING THE RESULT: the belt-crossing E2E test initially traveled straight from m42 to Aldebaran, which flies an entirely different real-space line than the origin-to-Aldebaran ray the density numbers were measured against (the ship was still parked at m42 from an earlier assertion block) -- fixed by returning home first. MANDATORY FULL-SUITE GATE FOR THE COMBINED D5+D6 SESSION (CLAUDE.md #25), run once at the end per explicit owner scope after per-slice targeted verification throughout: 805/805 unit (+10 over TR-108's D5.4 count of 795 -- star-catalog nearestOfType, asteroid-dr3-belt basis/cross-module-consistency, patch-atlas geometry, moon-offset-rank), 161/161 E2E (workers=1, 31.9 min), ZERO failures across the entire suite. Budget: totalJsGz 1205->1215 KB (measured 1206.1, D5+D6 code growth) and asset totalMB 257.0->257.4 / (root) 59.7->60.0 (measured 256.80/59.87 MB, D6.2's regenerated belt PNG + D6.6a's atlas re-encode) -- both raised deliberately with the measured numbers in the same commit, never silently. Build/lint/typecheck/docs:check all green. No existing test's assertion was loosened -- every named test change (belt-slowdown route swap, torus-bounds basis check, rigid-rotation invariant, catalog-count assertion) is recorded with its reason. PHASE D5 AND PHASE D6 ARE BOTH NOW FULLY COMPLETE except D5-AC5 (D5.1's post-approval label check, carried forward unrelated) and D6.5 (never a code item, resolved into D9 by owner decision) -- D9 itself remains future scope, not started this session."
  },
  {
    "project": "ishanjain-portfolio",
    "phase": "D5/D6 code-review remediation -- all 10 findings of the 2026-07-29 REVIEW pass fixed, with regression tests (TR-110)",
    "detail": "HIGH: MissionControlBar.tsx onChange now re-opens the listbox -- Escape closed it without moving focus, so a visitor who pressed Escape and kept typing got a permanently dead Where-To box; reproduced by a throwaway RTL test BEFORE any fix was written, and missed by all six of D5.2's E2E specs because none of them types after an Escape. MEDIUM: star-catalog.ts nearestOfType read the type byte via unpackTypeAndColour, allocating an object per record while its own doc claimed 'no per-iteration allocation' -- now Math.floor, claim now true, pinned across the whole 8x256 type/colour domain including the ciByte-255 boundary CI_PACK_DIV exists for. D6.2's toBeltSpace/fromBeltSpace tuple returns had put allocation BACK into the render loop (beltPullAccel 1->2 arrays per rock per frame in both the kinematic and Havok loops; beltDensityAt 0->1 per call, and up to 26x/frame during warp) -- rotations inlined, new beltPullAccelInto(out) scratch path plus a _beltPullScratch field on the engine, both hot loops now allocate NOTHING per rock, and three tests pin the duplicated math against toBeltSpace/fromBeltSpace so it cannot drift. SpaceScene's getFeaturedSuggestions() rebuilt a ~4,834-entry Map every render (~7.5/s idle, even with the console never focused) to resolve six hard-coded ids -- now resolved once inside getSearchState()'s existing cache, which also hands MissionControlBar a stable array reference. patch-atlas.mjs default mode called apply(PATCHES) unconditionally and wrote even when nothing changed, so a second 'npm run assets:atlas' re-encoded the whole 4096x4096 atlas and generation-lossed all ~260 already-shipped cells -- now stale-only (missing map entry OR source newer than atlas) with a new --force escape hatch that warns what it costs; decision logic extracted as stalePatches() and unit-tested without touching image files. LOW: CLAUDE.md non-negotiable #22 rewritten -- D6.6b hand-edited celestial-catalog.js while D5.4 used an overlay module for the same class of change IN THE SAME SESSION, and the rule turned out to be factually wrong (src/data/celestial/*.js is hand-curated prototype-ported data, NOT machine output, so nothing regenerates it and a wrong RA/Dec had nowhere to be fixed) -- now separates GENERATED files from the curated ports and states enforceable rules (overlay for additive; direct edit only for factual corrections, minimal field change, named in a TR), with the now-inaccurate 'byte-identical' claims in .prettierignore/eslint.config.js corrected in place rather than deleted. MissionControlBar.tsx gained its first 10 unit tests (which surfaced that vitest's globals:false means RTL auto-cleanup never registers -- afterEach(cleanup) added explicitly with the reason). New searchWithTotal() collapses search()+totalMatches()' two independent full scans into one pass, with the honest-truncation guarantee explicitly re-tested (total stays UNCAPPED, never derived from the capped list -- the original rationale was right, it just never required a second SCAN). .claude-flow/ gitignored (it was prettier-ignored only, so it sat permanently in git status one 'git add -A' from being committed).",
    "status": "complete",
    "start": "2026-07-29T00:00:00Z",
    "end": "2026-07-29T00:00:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "TR-110. Owner request: 'implement all 10 fixes, run the unit tests as you progress through High, medium and low findings' -- unit suite run at each severity tier as directed (HIGH -> 815, MEDIUM -> 826, LOW/final -> 828), not batched at the end. FINDING 9 WAS SETTLED BY MEASUREMENT, NOT BY PICKING A SIDE: D6.2 recorded Aldebaran's peak belt density as ~0.97 in babylon-asteroids.ts and engine-select.spec.ts but ~0.96 in TR-109/the delivery plan/the realism review -- one measured number with two values. Re-swept beltDensityAt along each destination's origin ray at 0.05-unit steps: Aldebaran 0.9639 at r~165, m42 0.0002. The DOCS were right and the source comment was wrong; m42's recorded ~0.0001 was itself slightly off and is corrected to the measured 0.0002 rather than left standing. Both figures are now pinned by a unit test that doubles as a guard against a future ASTEROID_BELT change silently restoring the m42 crossing D6.2 deliberately retired. SCOPE HELD HONESTLY: passageDeflectForce in the same Havok loop also allocates a tuple per rock per frame, but that is PRE-EXISTING since PF-09 B4 rather than introduced by D6.2, so it was recorded as a D7 follow-up instead of being folded into this pass. Likewise the coverage-threshold enforcement hole (vitest declares 80% over src/components/islands/** but 'npm run test' never passes --coverage) is recorded, not quietly fixed. GATE: 828/828 unit (+23 over TR-109's 805, ALL ADDITIVE -- no existing assertion weakened, no test modified to make the suite pass; CLAUDE.md #14/#15). lint / check (163 files, 0 errors) / build / budget:check / docs:check all green. Budgets did NOT move: totalJsGz measured 1206.2 KB against the 1215 ceiling TR-109 already raised, and assets stayed at 256.80 MB / 59.87 MB precisely BECAUSE the atlas was not re-encoded -- finding 5's fix demonstrating itself. E2E DELIBERATELY NOT RUN -- the owner elected to run npm run test:e2e manually, so TR-110 is explicitly NOT a closed CLAUDE.md #25 gate and says so, listing exactly which specs each fix exposes (destination-search.spec.ts and accessibility.spec.ts for findings 1/4/8; engine-select.spec.ts's belt-slowdown assertion for finding 3; none for the rest). npm run build was run last, so dist/ is current for Playwright."
  },
  {
    "project": "ishanjain-portfolio",
    "phase": "D7 -- memory & runtime optimisation, all 6 audited slices (TR-111)",
    "detail": "D7.1: mesh.geometry?.clearCachedData() after the final upload for the star/SDSS/belt bulk meshes (~430 MB CPU-side estimate) -- verified safe against Babylon's own source (picking reads _field, a plain typed array, never mesh geometry; setVerticesData always builds a fresh Geometry so clearing after the first star-field upload cannot break the bonus-layer rebuild), context-loss trade-off documented inline. D7.2: new _disposePlanetBodyTextures() releases the six real per-body planet textures plus any VT detail atlas on departure with no next body queued, rebinding shared placeholders (CLAUDE.md #9); the unconditional-ultra-fetch half of this audit item was already fixed by D6.3.2, found stale during implementation. Landed early in this slice: the VT streamer's toDataURL PNG round-trip replaced with a direct RawTexture/getImageData upload. D7.3: new src/workers/catalog-decode.worker.ts moves the SDSS decode + billboard-build off the main thread (base catalog and belt stay synchronous, 20-24x smaller); new decodeStarCatalogFromRGBA reads packed records directly from the RGBA buffer getImageData returns, eliminating decodeStarCatalog's stripped-RGB copy pass, 5 new equivalence tests pin it byte-for-byte against the existing decoder. D7.4 allocation sweep: passageDeflectForceInto (the item TR-110 explicitly deferred here) with a new _deflectScratch field; _pushAberration's per-frame 7-element array replaced by 6 inline _setAberrationUniforms calls; cosmos:warp->React throttled to 10 Hz with an immediate-emit carve-out on phase transitions (analyzed against the ~195ms flip window before implementing); a new _bodyById Map replaces three bodies.find() O(n) scans (bodies is assigned exactly once at boot); plume/ember GPU re-uploads now gated on real parameter change. Evaluated and deprioritized: quatRotate/travelFrame/_projectBody/_cameraBasis's small per-call array allocations (O(1)/frame, ~15-20 call sites, an order of magnitude smaller than the belt loop already fixed -- recorded as a follow-up, not silently dropped). D7.5: new _setBeltPhysicsAwake(awake) actually calls PhysicsBody.setMotionType(STATIC/DYNAMIC) on every belt Havok body on the D2.1 fade flip -- _tickAsteroids's own comment had named this exact gap as deferred to D7.5 (skipping this file's own force-application loop never stopped Havok's solver from still stepping the bodies); new sceneStats().beltBodiesStatic diagnostic proves the bodies themselves changed state, not just the pre-existing flag. D7.6: MilkyWayBandBuilder's ~2 MB retained buf nulled immediately after its one-time texture upload (previously only cleared on disconnectedCallback); _baseCatalogRgb's ~2.4 MB nulled in _loadBonusStarLayers's finally block (its last possible read, on every exit path).",
    "status": "complete",
    "start": "2026-07-29T00:00:00Z",
    "end": "2026-07-29T00:00:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "TR-111. Owner request: implement D7 and D9 in sequence; write unit AND E2E coverage for both, but run only unit tests -- E2E is a manual owner run once CPU/GPU capacity is available. TWO OF D7'S SIX AUDITED ITEMS HAD ALREADY MOVED SINCE THE 2026-07-22 AUDIT, FOUND DURING IMPLEMENTATION RATHER THAN ASSUMED CURRENT: D6.3.2 (shipped 2026-07-23) already tier-gated the planet ultra-texture fetch (D7.2's other half), and TR-110's own code review (2026-07-29, same day) had already landed beltPullAccelInto and explicitly named passageDeflectForce's matching allocation as D7's item to fix -- both recorded as corrections rather than silently redone or silently skipped. THE cosmos:warp THROTTLE WAS RISK-ANALYZED BEFORE IMPLEMENTING, NOT AFTER: engine-select.spec.ts's existing phase-order assertion depends on catching the ~195ms-wide flip window through the event stream; a flat 10 Hz (100ms) throttle is provably safe by pigeonhole (window width > sample period guarantees at least one sample), and a phase-transition carve-out was added anyway so the throttle can never be the reason a transition is missed regardless of real-world frame-time variance. THE PhysicsMotionType ENUM ACCESS USED A typeof import(...) TYPE-ONLY REFERENCE specifically to avoid adding a second Havok-adjacent import outside the existing lazy _setupPhysicsInner dynamic import (CLAUDE.md #1's bundle discipline applied to a physics enum, not just the Babylon core barrel). GATE: 837/837 unit (+9 over TR-110's 828, all additive -- no existing assertion weakened). A new mergeStarFields helper (star-field.ts) also fixes the other half of D7.5's audit row: the bonus-layer merge no longer re-decodes the ~168,959 already-decoded base catalog records (rebuilding billboards for the whole merged set is unchanged, recorded as a follow-up). lint/typecheck (164 files)/build/budget:check all green; budget margin now ~7 KB on the 1215 KB ceiling, flagged explicitly for D9's upcoming RenderConsole UI addition rather than left as a surprise. E2E NOT RUN and real-device heap/VRAM measurement NOT RUN -- both explicitly owner-deferred this session; D7's own delivery-plan exit criteria ('measured deltas in the TR') is NOT met by TR-111, which says so rather than presenting static-analysis estimates as measured ones."
  },
  {
    "project": "ishanjain-portfolio",
    "phase": "D9 -- Render Console: user-configurable layer rendering (ADR-0010, TR-112)",
    "detail": "D9.1: new src/lib/render-layers.ts (pure) -- LayerId union (11 layers incl. gaia-tiny for D8 forward-compat), LayerMeta with a new implemented field (false for star-field/bonus-stars/gaia-tiny, each with a documented architectural reason), parseLayersParam/resolveLayers (URL -> localStorage ij-layers -> tier default), formatBytes/formatVerts. babylon-engine.ts: setLayers()/layers observed attribute, _applyLayerToggle covering sdss-field and belt-visual (dispose+guard-reset on disable, fetch-or-setEnabled on enable), belt-physics folded into the existing D2.1 _setBeltPhysicsAwake call as a second AND-gate, nebula-volumes/gd1-trail/milky-way-band new setEnabled-or-uFade toggles, constellations reusing the pre-existing _showConstellations mechanism, planet-hires read live in _tickPlanetSphere alongside the existing ultra-progressive tier gate. Initial boot fetches (sdss-field, belt-visual) and initial enabled states (nebula, gd1, constellations, band) all gated on the resolved layer state too, not just post-boot toggles. New sceneStats().layers mirror. D9.2: new RenderConsole.tsx (dossier-format dialog, DATA & LICENSES precedent, CraftQualityControl arm-length engine access pattern, focus-utils.ts trap/moveFocusTo/restoreFocusTo the same way CollectorCard uses them) -- per-layer checkbox/number-input rows with real cost labels, LITE/BALANCED/FULL/EVERYTHING presets, RESET TO AUTO (live re-apply, no reload), live fps via the existing cosmos:perf:req/cosmos:perf pair ?perf=1 already established, URL-override chip. Wired into SpaceScene.tsx (renderConsoleOpen state, escape-stack entry) and HUD.tsx (RENDER ▸ button, desktop-only like DATA & LICENSES, absent when engineKind !== babylon). D9.3: budgets.config.mjs gains layerBytes (real measured bytes for every fetch-based layer) as the single source LAYERS.assetBytes reads from -- the bootCriticalDownloadKB gate itself already existed (landed with D1.1, found ahead-of-schedule during this pass). D9.4: defaults recorded as owner-pending -- every defaultByTier mirrors CURRENT shipped behaviour (everything-on-every-tier for the always-shipped layers, QUALITY_BUDGETS.asteroids/planetTexture exactly for belt-physics/planet-hires), not a D0.3-calibrated number; ADR-0010 addendum added stating this explicitly.",
    "status": "complete",
    "start": "2026-07-29T00:00:00Z",
    "end": "2026-07-29T00:00:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "TR-112. Same owner request as TR-111 (implement D7 and D9 in sequence; unit tests only, E2E is a manual owner run). THREE OF 11 REGISTERED LAYERS ARE HONESTLY MARKED implemented:false RATHER THAN FAKED: star-field (always on by ADR-0010 itself), bonus-stars (merged permanently into the base star mesh for one draw call -- discovered while implementing D9.1, not anticipated in the plan text, recorded as a real architectural constraint and a follow-up rather than building a bigger rebuild-without-merge mechanism under this session's time budget), gaia-tiny (D8 has not shipped its data pipeline). THE cosmos:warp THROTTLE RISK ANALYSIS FROM TR-111 CARRIED FORWARD HERE TOO: belt-physics's AND-gate with the existing D2.1 fade was verified by re-reading _updateFrameLadder's call site before changing it, confirming both conditions combine correctly rather than one silently overriding the other. THE BUDGET WAS RAISED AHEAD OF THE WIRE, NOT AT IT: TR-111 already flagged ~7 KB margin after D7's worker chunk; D9's measured 1210.7 KB left only 4.3 KB on the OLD 1215 KB ceiling, so totalJsGz was raised to 1225 in the same commit as the measurement, per budgets.config.mjs's own \"raise deliberately, with a why\" rule. GATE: 880/880 unit (+43 over TR-111's 837, all additive). lint/typecheck (169 files)/build/budget:check (raise documented above)/docs:check all green. ONE UNRELATED SINGLE-RUN FLAKE (a Header.astro clipboard-copy test, touching no file this session changed) did not reproduce on two immediate re-runs -- noted per this repo's measure-don't-assert discipline, not chased further. E2E NOT RUN and D9.4's real-device calibration NOT RUN -- both explicitly owner-deferred; TR-112 says so rather than presenting the current-behaviour defaults as a measured recommendation. PF-11 Phases D7 and D9 are both now implemented in this session, in the sequence the owner asked for; D8 (depends on D9's now-ready layer mechanism) remains."
  },
  {
    "project": "ishanjain-portfolio",
    "phase": "D7/D9 remediation -- render-loop freeze + Render Console defects (TR-113)",
    "detail": "C1 (CRITICAL): removed geometry.clearCachedData() from _applyStarFieldGeometry in babylon-engine.ts -- the one of D7.1's three clear sites that runs TWICE (boot, then the bonus-layer merge). Babylon derives the mesh BoundingInfo from the CPU-side position cache; with the cache dropped the rebuild left getBoundingInfo() undefined and RenderingGroup._RenderSorted threw inside scene.render(), before renderFrames++, freezing the renderer ~10 frames into every load on every backend. The two one-shot meshes (SDSS ~361 MB, belt ~15 MB) KEEP their clears; only the rebuildable star mesh (~54 MB) gives its copy back. H1: _loadBonusStarLayers now gated on _layerState['bonus-stars'], making ?layers=bonus-stars:0 a real boot-time escape hatch for the heaviest post-first-frame work. H3: setLayers(config, {persist}) added; RESET TO AUTO is the only persist:false caller, so its removeItem finally sticks. H4: render-layers.ts LayerMeta.implemented:boolean replaced by status: live|always-on|reload|unavailable plus isLiveToggleable()/isAvailable() -- one boolean had been carrying three meanings and the panel guessed wrong. M1: RenderConsole rows now status-driven (unavailable renders OFF, always-on renders ON) instead of hardcoded checked. M3: LayerMeta.maxCount (belt-physics 192) + clampLayerValue() applied at all four entry points (URL param, stored JSON, panel input, setLayers). M4: a pre-connection `layers` attribute is now folded into connectedCallback's resolution order instead of being overwritten by it. M2: render-console.spec.ts's WebGL1 test locator scoped to the HUD button (it strict-mode-violated against the mobile menu twin on a HEALTHY build). L1: COPY LINK button added, so serializeLayersParam's documented caller now exists.",
    "status": "complete",
    "start": "2026-07-29T00:00:00Z",
    "end": "2026-07-29T00:00:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "TR-113. Supersedes TR-111/TR-112's READY TO PROCEED verdicts, which were issued with E2E unrun against CLAUDE.md #25; both TRs now carry additive supersession notes rather than edited verdicts. ROOT CAUSE WAS PROVEN, NOT INFERRED: four ?layers= isolation runs ruled out the SDSS and belt meshes, and a rebuilt variant with the single line removed took renderFrames from a frozen 10 to 49 with zero page errors. A NEGATIVE RESULT IS RECORDED BECAUSE IT CHANGED THE FIX: reconstructing the bounding info (refreshBoundingInfo() + _createGlobalSubMesh(true)) immediately before clearing was tried and measured NOT to work -- Babylon re-derives and re-nulls it from the absent cache -- so the cache stays. THE HONEST COST: D7's own memory goal loses the star mesh's ~54 MB, and that number was never measured on a real device in the first place (D0.3 still owns it). THE MISSING GUARD IS NOW IN PLACE: engine-select.spec.ts asserts starCount grows past the base catalog AND renderFrames still advances afterwards AND zero pageerror -- it cannot pass by never merging. H2 is recorded as a process finding rather than fixed: 880 green unit tests certified a build whose renderer was dead, because no jsdom test instantiates a GPU. D8 IS UNBLOCKED: the status enum makes shipping gaia-tiny a one-value change plus its data pipeline, and the rule 'never clearCachedData() on a mesh that will be rebuilt' is written at the fix site because D8's Gaia DR3 Tiny merge reuses exactly that path."
  }
]
```
