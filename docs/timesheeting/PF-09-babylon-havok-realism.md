# Delivery Timesheet — PF-09 Babylon + Havok Realism Engine

Plan: [docs/delivery-plan/PF-09-babylon-havok-realism.md](../delivery-plan/PF-09-babylon-havok-realism.md)

```json
[
  {
    "project": "ishanjain-portfolio",
    "deliveryPlan": "PF-09-babylon-havok-realism",
    "phase": "B0 — Foundations & dual-engine scaffold",
    "detail": "Babylon deps, ?engine=babylon flag, BabylonScene island, perf-telemetry harness",
    "status": "complete",
    "start": "2026-07-18T01:30:00Z",
    "end": "2026-07-18T02:00:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "COMPLETE (TR-027; start approximate). engine-select.ts (?engine= resolver, default webgl), perf-telemetry.ts (engine-agnostic PerfMonitor → window.__ijPerf() + ?perf=1 overlay; times mount→cosmos:ready, rAF-sampled fps, device tier), babylon-engine.ts (self-registering <babylon-scene>, minimal Babylon WebGL2 scene, contract stubs for B2), SpaceScene post-hydration swap keeping the default path byte-identical. Added @babylonjs/core@^8.56.2 (MIT, US-jurisdiction). VERIFIED: 80/80 unit (+13), 51/51 E2E full suite on the production preview build (+3 B0 specs incl. babylon renders — startupMs-reported requires a real frame), build/lint exit 0. Babylon confirmed isolated in a lazy dynamic-import chunk; default page JS unchanged. Strategy Gate: no 3feature scorecard; milestone card governed (exception). Model Opus 4.8 = card's Tier-A. MEASURED FINDING for B1: @babylonjs/core barrel = 1.1 MB gz (>900 KB budget) — B1 must use tree-shaken subpath imports + WebGPUEngine. Interactive browser-pane visual blocked by pane/dev-server connectivity (chrome-error) + slow 5MB dev-compile — env issue, not a defect; production E2E is the authoritative proof. B0 does NOT supersede ADR-0002 (that is the B1 gate)."
  },
  {
    "project": "ishanjain-portfolio",
    "deliveryPlan": "PF-09-babylon-havok-realism",
    "phase": "B1 — Renderer parity spike (GO/NO-GO GATE)",
    "detail": "Babylon+WebGPU parity of the hero scene; measure on desktop+Android+iPhone; write superseding ADR",
    "status": "complete",
    "start": "2026-07-18T02:20:00Z",
    "end": "2026-07-18T23:10:00Z",
    "llmStrategy": "docs/llm/llm-strategy-scorecard-5milestone.md",
    "notes": "GATE DECIDED 2026-07-18 — CONDITIONAL GO (ADR-0003, end timestamp approximate). Owner confirmed the iPhone/Android rows were Chrome DevTools EMULATION (no physical phone available), which settles the open question from TR-033. DECISION: proceed to B2 on desktop evidence — real Windows hardware, Babylon 140fps/1070ms vs current 142fps/1357ms against a 60fps floor (parity on fps, BETTER on startup), bundle 329KB gz already under the ~900KB budget, default-path JS unchanged at 260KB, 92 unit / 52 E2E green incl. pixel proof. NOT a plain GO: mobile perf is UNMEASURED, not passed — emulation runs on the desktop GPU at desktop refresh (hence the impossible 135-144fps above any iPhone's 120Hz ceiling), so it validated functional/layout behaviour at phone viewports but no performance dimension. Recording it as passed would convert a known unknown into false certainty, and mobile is exactly where a 330KB engine + ~23MB of billboard vertex data + a 168k-star field is most likely to fail. THREE CONDITIONS gate the B6 cutover (not B2 development): (1) real-device mobile fps — one physical Android over chrome://inspect is free and sufficient; (2) confirm the on-canvas badge reads BABYLON WEBGPU, since the gate table's Backend column was pre-filled from my template and NOT observed — the WGSL twin's real-hardware execution remains unproven; (3) billboard memory reduction (~23MB vs the live engine's ~2.7MB). Current WebGL1 engine remains the shipping default throughout B2-B5, so the live site carries no risk; the B0 dual-engine seam keeps reversal near-free if condition 1 fails. ADR-0002's zero-runtime-3D-dependency stance is superseded for the renderer layer only as far as desktop evidence supports. --- GATE ROUND 2 INCONCLUSIVE 2026-07-18 (TR-033): owner re-measured with the v2 instrument. Startup figures now credible (1.0-1.6s, matching CI 840-2626ms) — Round 1's sub-40ms values were fps-at-startup transcribed into the ms column, since corrected. TWO blockers remain. (1) MY BUG: displayHz used the MINIMUM frame delta with a 4ms floor, so one pair of coalesced rAF callbacks latched it to the 250Hz ceiling — the table's 217-250Hz values are artefacts, no such panel exists. Fixed to a 10th-percentile estimate (verified: a 60Hz stream containing one 1ms frame now reports 60Hz, not 250; production estimate dropped from a phantom 106-250Hz to an honest 13Hz). Relabelled as a WEAK self-check since it derives from the same rAF stream as fps. (2) OPEN QUESTION: iPhone rows report RENDER FPS 135 and 144 — physically impossible, since rAF is vsync-locked and iPhone panels cap at 120Hz ProMotion / 60Hz. With all six rows in a 135-144 band across three device classes, leading hypothesis is DevTools device EMULATION rather than physical hardware (emulation changes viewport/UA but runs on the desktop GPU at desktop refresh). NOT yet confirmed — settled by pasting JSON.stringify(window.__ijPerf().device) from the real iPhone. FIX SHIPPED: the overlay previously carried NO hardware information, so a recorded row had no evidence of origin; it now ends with a device signature line and self-flags a UA claiming Android/iPhone while reporting >=12 CPU cores as LOOKS EMULATED. VERIFIED: 92/92 unit (+1), 52/52 E2E, build/lint exit 0, overlay device line confirmed in a real browser. Desktop rows stand; only the two mobile classes need re-running on hardware. B1 REMAINS GATED — no ADR, ADR-0002 stands. --- GATE ROUND 1 VOID 2026-07-18 (TR-032): owner submitted the first device readings; review before deciding found the INSTRUMENT could not produce a valid reading — my defect, not their method. `fps` counted host requestAnimationFrame ticks rather than engine renders (measured 139 host ticks vs 82 real renders, ~1.7x over-report); 140fps on an iPhone is impossible (rAF is vsync-locked; panels are 60/120Hz); 20-39ms startup is impossible for the current engine (cosmos:ready waits on the star stream: >=350ms of chunk timers alone, CI measures 840-1270ms). Instrument v2 adds renderFps (engine's own frame counter over a 2s window — THE gate metric), displayHz (hard ceiling, makes impossible readings self-evident), renderFrames (0/null proves the scene isn't drawing) and a device block (makes rows attributable); overlay shows RENDER FPS with host rAF for contrast. Gate procedure now records Round 1 as VOID with reasons plus a Round 2 table and explicit rejection rules. VERIFIED: 91/91 unit (+5), 52/52 E2E on the final run (3 runs: 51/52, 51/52, 52/52 — two different singles, each green in isolation; TR-025-class parallel-WebGL flakiness, not regression), build/lint exit 0. Also fixed a self-inflicted per-frame document.querySelector inside the measured loop. B1 REMAINS GATED — no ADR, ADR-0002 stands, re-measure needed. --- B1-CONTINUED 2026-07-18 (TR-029): WGSL twin delivered, and it surfaced a technique-level finding — WebGPU has NO gl_PointSize equivalent (WGSL dropped point_size; point-list = 1x1 px; Babylon ignores pointSize on WebGPU), so the live engine's variable-size point-sprite starfield CANNOT port. Stars re-architected as billboard quads in one merged indexed mesh (buildStarBillboards, pure+tested); Babylon thin instances tried and rejected (require a per-instance matrix buffer, else thinInstanceCount stays 0 and nothing draws — proven via the new sceneStats() diagnostic). Tree-shaking gotcha recorded: subpath imports drop prototype augmentation (thinInstanceMesh side-effect import). GLSL+WGSL twins selected by backend via ShaderLanguage. Added a pixel-proof E2E which immediately earned its place — the first attempt booted, emitted cosmos:ready and drew NOTHING. VERIFIED: 86/86 unit (+2), 52/52 E2E (+1), build/lint exit 0, bundle unchanged at 330 KB gz, in-browser 1,013,754 active indices / 675,836 verts / 237,959 lit pixels. HONEST LIMIT: the WGSL path has never run on real WebGPU hardware (CI has no adapter) — the owner's device run is its first execution. Billboards cost ~23 MB vertex data vs ~2.7 MB for point sprites (~8x) — a B2 optimisation target. --- ORIGINAL B1 ENTRY: GATED on owner real-device measurement (TR-028; start approximate). Spike built & CI-verified: tree-shaken subpath imports replacing the B0 barrel, WebGPU primary (dynamic import when navigator.gpu) with WebGL2 fallback, and the fps-critical 168,959-point custom-ShaderMaterial star cloud (star-field.ts, pure+seeded). VERIFIED: 84/84 unit (+4), 51/51 E2E (babylon renders the star cloud + telemetry via WebGL2/SwiftShader fallback, no errors), build/lint exit 0. DECISIVE BUNDLE RESULT: Babylon 329 KB gz tree-shaken (vs 1.1 MB barrel at B0 = 3.4× cut; under the ~900 KB budget) → GO on bundle; default-path JS 260 KB gz unchanged → GO on regression. THE GATE IS NOT FULLY RESOLVABLE IN CI: headless renders WebGL via SwiftShader (CPU, no WebGPU), so representative fps/startup + the WebGPU path require the owner's real desktop/Android/iPhone — procedure at docs/validation-checklist/2026-07-18-pf09-b1-gate-measurement.md. No fps proxy reported (SwiftShader would mislead). ADR-0002 NOT superseded until a GO reading. Strategy Gate: milestone card governed (no 1spike/3feature on disk; exception). Model Opus 4.8 = Tier-A. B1-continued: WGSL shader twin for native WebGPU; real streaming-catalog + photometric port. Owner action: run the gate procedure → GO writes the superseding ADR + proceeds to B2; NO-GO keeps the current engine + cherry-picks."
  },
  {
    "project": "ishanjain-portfolio",
    "deliveryPlan": "PF-09-babylon-havok-realism",
    "phase": "B2 — Cinematic flight port",
    "detail": "Vertex expansion → catalog swap → flight model → chase camera → distance pacing → reduced-motion",
    "status": "inprogress",
    "start": "2026-07-19T01:25:00Z",
    "end": null,
    "llmStrategy": null,
    "notes": "Started 2026-07-19 after ADR-0003 conditions 1 and 2 were both discharged on real Android hardware. Scope extended with the vertex-index expansion (ADR-0003 condition 3, folded in from B3). Step 1 of 6 COMPLETE (TR-036): quad corner derived from gl_VertexID / vertexInputs.vertexIndex instead of stored, star vertex buffers 21.9 → 16.8 MiB (-5.2 MiB, 24%). Verified 95/95 unit, 52/52 E2E incl. the Babylon pixel proof, build exit 0. HONEST GAP: the pixel proof runs on Playwright chromium which has no WebGPU adapter, so it validates the GLSL twin only; the WGSL twin is evidenced by materialReady=true (effect compiled) + 1,013,754 active indices + zero console errors, but not by pixels — a WebGPU canvas cannot be read back via drawImage locally. Step 2 of 6 COMPLETE 2026-07-19 (TR-038): real Gaia/Hipparcos catalog swap + photometric shader port, closing the ~6x apparent-density gap the owner reported on physical Android (TR-037). New pure decoder star-catalog.ts reads the SAME PNG-packed assets the live engine streams (117,964 Hipparcos + 50,995 Gaia deep = 168,959 = LIVE_STAR_COUNT exactly), so no new payload ships — this is a decode port, not a new asset. Both shader twins now run the live engine's photometric path (Pogson's law over the Hipparcos magnitude window, Planckian O->M chromaticity ramp, all 7 deep-layer object classes). Object type + colour index packed into ONE float (type + ci/256) rather than a third vertex attribute, preserving step 1's -5.2 MiB saving; the /256 rather than /255 is deliberate and tested — ci=255 would pack to exactly type+1.0 and floor() would misclassify every reddest star in the catalog, an off-by-one that only bites at one end of the range. Billboard quads offset in CLIP space (corner * px * clip.w / uViewport) so the px mapping is exact under any projection and handedness-independent (Babylon is left-handed, the live engine is not); geometric extent floored at 1.5px because a 1px quad can fall between pixel centres and vanish where a 1px point sprite always rasterises, while visibility stays photometric. VERIFIED: 107/107 unit (+12), 53/53 E2E (+1), astro check 0/0/0 across 73 files, eslint+prettier exit 0, Babylon chunks 281 KB gz (budget ~900 KB), default-path JS unchanged (catalog code only in the lazy chunk), star vertex memory 16.8 MiB unchanged from step 1. THE NEW E2E ASSERTION EARNED ITS PLACE IMMEDIATELY: loadChunkRGB read bmp.width AFTER bmp.close() (which zeroes it), so the catalog decoded to zero records and silently fell back to the procedural placeholder — and the existing pixel proof PASSED anyway, because the fallback draws a perfectly good sky. Only the starSource === 'catalog' assertion distinguished downgrade from success. Fixed. HONEST LIMITS: the WGSL twin remains unvalidated on real WebGPU hardware (Playwright chromium has no adapter, so CI exercises the GLSL twin only — unchanged from step 1); and CI proves the real catalog loads and that faint magnitudes collapse to the alpha floor, but whether the sky now LOOKS right against the live engine is an owner observation on a real screen. Relativistic aberration/Doppler deliberately not ported — they need the flight model's velocity (steps 3-5); halo runs at top tier until B5 formalises tiers. Steps 3-6 (flight model, chase camera, distance pacing, reduced-motion) NOT started. --- CORRECTION 2026-07-19 (TR-039): owner challenged TR-038's honest-limits claim that the WGSL twin was unvalidated on real hardware and that Playwright chromium has no WebGPU adapter. Revalidated empirically rather than re-asserted. TWO ERRORS found, both mine. (1) The 'unvalidated on real hardware' claim directly contradicted ADR-0003 condition 2, ALREADY discharged 2026-07-19 on physical Android, BEFORE B2 step 2 was even written — I failed to cross-check the ADR. (2) 'Playwright chromium has no WebGPU adapter' was imprecise: navigator.gpu DOES exist on a secure context (my original check used about:blank, which the WebGPU spec excludes); requestAdapter() genuinely returns null on the BUNDLED Testing-build Chromium under 5 flag combinations tried, but launching via channel:'chrome' (the real installed system browser) yields a REAL adapter (AMD/NVIDIA) with ZERO flags needed. Shipped tests/e2e/webgpu-hardware.spec.ts: manually launches channel:'chrome' (bypasses the page fixture, so playwright.config.ts and CI are untouched), fails OPEN (test.skip, not red) if the real channel/adapter is unavailable, and when available asserts sceneStats().backend === 'webgpu' + materialReady + the B2-step-2 catalog identity — ADR-0003 condition 2's exact assertion, now automatable. VERIFIED: 1/1 new spec pass on this real-GPU machine, 54/54 E2E (+1), 107/107 unit (unchanged), astro check 0/0/0 (74 files, caught a real navigator.gpu type-narrowing bug), eslint+prettier exit 0. Deliberately NOT added to CI — ubuntu-latest runners have no GPU and CI only installs bundled chromium, so channel:'chrome' would almost certainly still skip there; flagged as a named scope boundary, not silently omitted. TR-038 corrected in place (struck the wrong claim, pointer to TR-039), matching the TR-037-corrects-TR-036 pattern already used in this plan. --- STEP 3 of 6 COMPLETE 2026-07-19 (TR-040): flight model port — ship-dynamics.ts's pure spring/quaternion math wired to a real Babylon camera for travelTo/goHome/randomBody. INSPECT surfaced a blocking prerequisite before any travel code was written: SpaceScene.tsx's engineEl() queried document.querySelector('space-engine') UNCONDITIONALLY, so on the Babylon path every host-driven call (travelTo, goHome, HUD sync, station sprites) silently no-op'd — steps 1-2 were correct but entirely unreachable from the real UI, only from babylon-scene's own self-running preview. Fixed with the smallest surgical change: engineEl() now queries 'space-engine, babylon-scene' (exactly one is ever mounted). New pure math in ship-dynamics.ts (raDecToDir, bodyDepth, bodyWorldPosition, ARRIVE_STANDOFF=38), pinned by unit test against space-engine.js's private (non-importable) formulas so a drift can't silently send the Babylon camera to the wrong place. Camera POSITION: three independent springStep calls toward the target reusing SHIP_SPRING_OMEGA/ZETA as-is — a linear damped spring's settle time is distance-independent, so the live engine's ship-sprite tuning (~1.5s settle) works unchanged at world-space scale; this also means travel currently takes roughly fixed wall-clock time regardless of distance, which is exactly what step 5 (distance-scaled pacing) is staged to add on top — not a gap, a deliberate sequencing already called out in the plan. Camera ORIENTATION: quaternion-slerp via quatDamp toward quatFromUnitVectors(BABYLON_FORWARD, travelDir). HANDEDNESS VERIFIED, NOT ASSUMED: ship-dynamics.ts's header says its conventions match the live engine's RIGHT-handed −Z-forward camera; Babylon defaults LEFT-handed, +Z-forward — checked directly against @babylonjs/core's TargetCamera/Vector3.Forward source rather than trusted from memory (this session's established pattern after the WebGPU claim correction), confirmed (0,0,1) at identity rotationQuaternion on this scene's left-handed default; the quaternion math itself (quatFromUnitVectors/quatSlerp/quatDamp) is handedness-agnostic vector math, so reuse is safe as long as BABYLON_FORWARD (not the live engine's constant) is the reference. DEBUGGING DETOUR WORTH RECORDING: manual verification in the interactive Browser Pane showed renderFrames stuck at 0 after wiring travel, looking like a real regression; a temporary try/catch proved no exception was ever thrown, and a fresh Playwright run against the IDENTICAL build showed travel working perfectly (renderFrames 255->654, camera converged within 0.4 world units of the Sirius target). The Browser Pane's UA revealed why: Claude Desktop's own Electron shell (Electron/42.5.1) is a THIRD distinct Chromium build, neither Playwright's bundled binary nor real system Chrome (the two TR-039 already characterized) — it reports a real WebGPU adapter but its render loop doesn't advance in this scene, an environment quirk of that embedded browser, not a code defect. Recorded so a future session doesn't re-diagnose the same false alarm. VERIFIED: 113/113 unit (+6, incl. exact-value pins at cardinal ra/dec points), 55/55 E2E (+1), astro check 0/0/0 (74 files), eslint+prettier exit 0. New E2E test deliberately clicks the RNG button rather than calling travelTo() directly — proves the FULL chain (button -> SpaceScene -> engineEl() -> randomBody() -> travelTo() -> cosmos:* events -> HUD React state), exactly the chain the prerequisite bug broke. DELIBERATELY DEFERRED to step 4: chase-camera waypoint choreography (chaseOffsetAt/travelFrame already exist in ship-dynamics.ts, unused here by design), launch-at-click ship turn, arrival framing, screen-space station projection (verified safe when absent, not just assumed: SpaceScene.tsx reads b.vis ? b.sx : b.ex, and undefined != null is false, so sprites stay hidden rather than crash). Free-look drag/orbit controls and field-star hover/picking also not in scope. Live WebGL engine remains the shipping default throughout; a click-driven Babylon journey today is a direct camera glide, not yet the cinematic chase — an intentional intermediate state. --- STEP 4 of 6 COMPLETE 2026-07-19 (TR-041): chase camera, launch-at-click, arrival framing — the PF-08 choreography, superseding step 3's damped-spring stand-in entirely (spring state/imports removed, not left dead). Position now follows a fixed-duration eased path (new warpEase in ship-dynamics.ts, pinned by 3 unit tests) offset by the behind-the-thruster waypoint table (chaseOffsetAt/travelFrame, unused since B1, wired for the first time). NO SHIP MESH EXISTS on the Babylon path (craft loading is a separate track) so the camera itself plays the 'ship' role a virtual position leads by SHIP_VIEW_DEPTH each frame, exactly as TR-040 flagged it would need to. ARRIVAL IS NOW EXACT, not asymptotic: chaseOffsetAt(0) and chaseOffsetAt(1) both equal [0,0,SHIP_VIEW_DEPTH] by construction, so cam(1)=to holds algebraically — verified to 6 decimal places in a new E2E test, not just asserted in a comment. Launch-at-click reinterpreted honestly for a ship-less scene: during 'aim' the camera holds position and previews the orientation turn toward the route, mirroring what the ship's nose would do if one existed. Arrival framing is NOT separately coded — it falls out of the chase-look damping (look target = virtual-ship-position + CHASE_LOOK_AHEAD, which converges near the arrived body as k->1), recorded explicitly as an emergent property rather than a ported feature, since there is no ship sprite to apply the live engine's scale/fade arrival bump to. HUD READOUT PORTED TOO: accel/flip/decel phase classification + apparent-velocity formula (vC = lyTotal*dsdk/warpDur/3.1688e-8), identical to the live engine's — WarpOverlay.tsx is engine-agnostic (reads only React state.warp from cosmos:warp payloads), so the SAME transit HUD text (ACCELERATION BURN / FLIP & BURN / DECELERATION BURN / APPARENT VELOCITY N c) now renders for a Babylon journey with zero UI changes, verified directly in E2E (not inferred from the event shape). VERIFIED: 116/116 unit (+3), 56/56 E2E (+1), astro check 0/0/0 (74 files), eslint+prettier exit 0. DELIBERATELY OUT OF SCOPE, re-confirmed: distance-scaled pacing (step 5 — warp duration still fixed at 2400ms regardless of target distance, matching the live engine's own current non-scaled behaviour); screen-space station projection (not one of the plan's six named B2 sub-steps, still verified-safe-when-absent per TR-040); reduced-motion parity (step 6 — AIM_DUR_MS/WARP_DUR_MS use only the live engine's non-reduced values). NEWLY POSSIBLE BUT NOT ABSORBED INTO SCOPE: relativistic aberration/Doppler in the star shader — a real velocity profile (dsdk/vC) now exists, which is the exact blocker TR-038's shader comment named, but wiring it is flagged as a follow-up rather than opportunistically added to a step named specifically for chase camera/launch/arrival. HONEST LIMIT: wphase==='flip' (the ~144ms window at k in [0.47,0.53]) is implemented and emits correctly but not separately asserted in E2E — window too narrow to poll reliably; covered indirectly by observing both accel and decel, which proves the state machine transits through flip between them. --- STEPS 5 AND 6 of 6 COMPLETE 2026-07-19 (TR-042), implemented and verified together in one pass — THIS COMPLETES ALL SIX B2 SUB-STEPS. Step 5 (distance-scaled travel): checked space-engine.js FIRST and confirmed it hardcodes warpDur=2400 regardless of distance — there was NO existing behaviour to port, this is new math designed for Babylon. New warpDurationForLy(ly) in ship-dynamics.ts: log10-scaled, bounded to 1400-4200ms. Bounds picked AFTER grepping every celestial-*.js file for the real ly range (0 to 13 BILLION ly, a distant SDSS galaxy) rather than guessing — a linear mapping would have made a single deep-catalog click either absurd or forced every near hop to instant. Same warpEase accel-flip-decel curve as step 4, just stretched longer for far targets — no new easing shape invented, phase thresholds (k<0.47 etc.) untouched since they key off the fraction not the duration. goHome always uses the floor (WARP_MIN_MS) regardless of current position. Step 6 (reduced-motion parity): a DIRECT port, unlike step 5 — read space-engine.js's reduced branches in full before implementing. Three behaviours ported exactly: (1) fixed short durations (200/350ms) that OVERRIDE step 5's formula entirely, not a fraction of it — reduced motion means 'get there fast', not 'get there fast, proportionally'; verified directly by traveling to Orion Nebula (the single longest non-reduced journey in the whole catalog) under reduced motion and confirming warpDur is still exactly 350. (2) CHASE_OFFSET_REST instead of the waypoint table. (3) snapped orientation (_camQuat = targetQ directly) instead of quatDamp, matching the live engine's yaw/pitch snap. _reduced is a ONE-SHOT matchMedia snapshot taken once in _boot(), same pattern as space-engine.js's own this.reduced (neither engine has a live-updating listener — not a gratuitous improvement). VERIFIED: 121/121 unit (+5), 58/58 E2E (+2), astro check 0/0/0 (74 files), eslint+prettier exit 0. New E2E tests: (a) sun (0.0000158 ly) vs m42/Orion Nebula (1344 ly, the plan's own named example) — real catalog ids, warp.warpDur values compared directly, far > near+500ms; (b) reduced motion forced via page.emulateMedia BEFORE navigation (matches the established space-scene.spec.ts pattern), confirms _reduced===true, warp.warpDur===350 for the catalog's longest journey, and real arrival within 2s. B2 EXIT CRITERION STATUS: PF-08 journey reproduces with distance-driven pacing (yes, steps 3-5), star memory materially below B1 (yes, 16.8 vs 21.9 MiB since step 1, unchanged), suite green (yes) — ALL THREE MET. Fourth condition, 're-measure fps/startup after this phase', remains OPEN: needs the owner's real device, exactly like the B1 gate itself (TR-032/033 already established this specific measurement cannot be substituted with CI or any desktop-browser reading, no matter how thorough). B2 is CODE-COMPLETE across all six sub-steps; the delivery-plan status line and this timesheet's status field are left as 'inprogress' rather than 'complete' to keep that open condition honest, following the same convention the B1 gate used while its own conditions were pending. DELIBERATELY OUT OF SCOPE, final B2 accounting: screen-space station projection (never one of the six named sub-steps); relativistic aberration/Doppler in the star shader (a real velocity profile has existed since step 4, making this newly possible but still not absorbed into any named step); free-look drag/orbit controls; field-star hover/picking; craft/ship mesh loading (separate track — the camera has played the 'ship' role throughout steps 3-6). HONEST LIMIT: warpDurationForLy's bounds are a first design pass with no live-engine behaviour to validate against (unlike steps 1-4) and no owner feedback yet on how the pacing actually feels; reduced-motion coverage is state-level only, not a visual/perceptual check that the reduced journey reads as calmer. --- DEMO-DEPLOY REPORT 2026-07-19 (TR-043): owner redeployed and reported real device metrics plus functional complaints. Desktop: Babylon WebGPU, startup 3023ms (OVER the B1 desktop budget of <=2.5s), render fps 144, host rAF 142/164Hz. Android: startup 267ms, 60fps. Functional: 'the background remains static ... no glimmer, no 360 nav, no spaceship. Stars appear, but the scene is blurry.' TRIAGED EACH ITEM RATHER THAN REACTING TO ALL OF THEM AS BUGS. TWO WERE REAL: (1) BLUR, root-caused against @babylonjs/core source: WebGPUEngine's constructor has NO positional adaptToDeviceRatio slot (unlike the WebGL2 Engine, which took it as a 4th arg and was already correct since B1) -- it must be set via options.adaptToDeviceRatio, which the code never did, so it silently defaulted to false and the canvas rendered at 1x CSS-pixel resolution on any HiDPI display then got stretched by the browser to fill the physical pixel grid. FIXED: added adaptToDeviceRatio:true to the WebGPUEngine options. Verified on REAL WebGPU hardware with deviceScaleFactor:2 (simulated HiDPI), confirming the drawing-buffer resolution actually scales (ratio >1.8), not just that the option was passed. (2) STATIC BACKGROUND, partly real: the live engine has an ambient idle-at-home yaw drift (yaw += 0.00012/frame) that Babylon never ported, so nothing animated at all while parked -- reads as 'the scene is dead' not 'parked'. FIXED: two new pure helpers in ship-dynamics.ts (quatFromAxisAngle, quatMultiply), unit-tested, applied each idle frame at home. Rate CONVERTED not copied: 0.00012/frame at the live engine's implicit ~60fps assumption is frame-rate DEPENDENT (its own minor bug); the Babylon version uses 0.0072 rad/s properly scaled by dt, so it drifts at the same rate on any monitor. Explicitly NOT free-look nav -- passive ambient motion at rest only, gated off the instant real travel starts. TWO WERE KNOWN, NOT NEW: 'no 360 nav' (free-look drag) and 'no spaceship' (craft mesh) have been named, documented scope boundaries since TR-040/B0 -- confirmed as not regressions, not fixed here, communicated clearly rather than silently ignored. STARTUP NUMBERS FLAGGED, NOT ACTED ON: applying the same skepticism the B1 gate itself required (TR-032/033 -- Round 1 voided over an instrument bug, Round 2 caught a display-Hz artefact) rather than reacting to either figure at face value. Desktop 3023ms is real and over budget but NOT attributable to this session's changes (DPR fix and idle drift both only touch per-frame render-loop behaviour, not the one-time boot/catalog-decode path) -- flagged for future profiling. Android 267ms is FLAGGED AS IMPLAUSIBLE: too fast for a 2MB+ PNG-packed catalog decode via ImageBitmap+canvas on mobile hardware; recording it as real without confirming a cold load (not a cached/warm reload) would risk exactly the false certainty TR-033 warned against. Recommendation issued, not yet actioned: re-run both using the established gate procedure (cold load, device signature visible) before deciding whether desktop startup needs a profiling pass. The B2 exit criterion's open 're-measure fps/startup' condition therefore remains OPEN -- this session's data doesn't close it, it identifies why the data isn't trustworthy yet. VERIFIED: 127/127 unit (+6), 60/60 E2E (+2), astro check 0/0/0 (74 files), eslint+prettier exit 0. ONE TRANSIENT FLAKE OBSERVED AND DIAGNOSED, not silently ignored: two pre-existing tests missed poll timeouts under 4 parallel workers (multiple real GPU-heavy scenes booting concurrently on this dev machine), passed cleanly both in isolation (--workers=1) and on a clean full-suite re-run -- consistent with the project's own documented TR-018 precedent (worker-count-vs-SwiftShader-saturation), not a new regression, not investigated further."
  },
  {
    "project": "ishanjain-portfolio",
    "deliveryPlan": "PF-09-babylon-havok-realism",
    "phase": "B3 — Volumetric & particle rendering",
    "detail": "GPU-particle thrusters/shooting-stars, volumetric nebulae, heat-shimmer post-pass",
    "status": "inprogress",
    "start": "2026-07-19T12:15:00Z",
    "end": null,
    "llmStrategy": null,
    "notes": "Started 2026-07-19 (start approximate). SEQUENCING DECISION recorded before any code was written: three of the five named items (thrusters, heat-shimmer, docking-approach polish) are ship-attached -- space-engine.js places heat-shimmer explicitly 'behind the nozzle' -- and NO SHIP MESH EXISTS on the Babylon path yet. craft-loader.ts (the live engine's GLB loader) is hand-rolled raw-WebGL (gl.createBuffer/gl.useProgram directly), not portable to Babylon without its own build-out; the camera has played the 'ship' role throughout B2 (TR-040 onward). These three items are BLOCKED on an unscoped ship-mesh loading track, named explicitly rather than silently dropped. Volumetric nebulae (WebGPU compute) is unblocked but a materially larger effort of its own (genuine compute shaders + raymarching), deferred to its own increment. IDLE SHOOTING STARS DONE (TR-044): NOT a port -- grepped space-engine.js and confirmed no shooting-star/meteor/idle-particle system exists at all, only the ship-attached, CPU-stepped F3 embers. New pure module shooting-stars.ts: buildShootingStars(count,radius,seed) builds a merged indexed mesh of tapered quads (same quad-not-line-primitive choice TR-029 already established), each particle spawning on a shell with a TANGENTIAL direction (perpendicular to its own radial vector, reusing travelFrame -- the same pure function the chase camera uses). GENUINELY GPU-DRIVEN, unlike the live engine's ember pattern: every particle's head/tail position and fade are computed IN THE VERTEX SHADER from a single uTime uniform via age=(uTime+seed*cycle) mod cycle -- zero per-frame CPU cost after the initial buffer upload, no stepping function, no re-upload. Shader twins (GLSL+WGSL) follow the established convention (line-for-line parallel, corner-from-vertexID derivation). THE WGSL TWIN CAUGHT A REAL BUG DURING REVIEW, before it ever ran: a first draft wrote vertexInputs.uTime (a vertex-input reference) where it needed uniforms.uTime (a uniform reference) -- caught by re-checking the star shader's own established uniforms.X pattern, fixed before the real-hardware test ran, compiled clean on the first real-adapter run. VERIFIED: 140/140 unit (+13), 61/61 E2E (+2), astro check 0/0/0 (76 files), eslint+prettier exit 0. Two E2E tests: extended webgpu-hardware.spec.ts's existing real-adapter test (WGSL twin compiles + mesh ready on real hardware) rather than adding a parallel one; new dedicated test on the default WebGL2-fallback project (the path CI actually runs continuously -- the real-hardware WGSL check remains opt-in/local-only per TR-039's established constraint). HONEST LIMITS: no visual/perceptual confirmation that the particles actually look like shooting stars on a real screen (mesh-ready + shader-compiles + correct pure math verified, not appearance -- same caveat pattern as TR-037, which needed the owner's own eyes to catch a compiled-and-rendering-fine placeholder reading 6x too dense); tuning values (24 particles, 5s cycle, 60-150 unit/s speed, 220-unit spawn shell) are a first design pass with no live-engine behaviour to validate against and no owner feedback yet, same caveat as warpDurationForLy in TR-042. --- REGRESSION FOUND AND FIXED SAME-DAY 2026-07-19 (TR-045): owner validated the redeployed demo on real desktop Google Chrome and reported a flat background, no stars, no shooting stars. TR-044's own verification had reported READY TO PROCEED with shootMeshReady/shootMaterialReady both true -- that verdict was WRONG, recorded here plainly rather than softened. ROOT CAUSE: the shooting-star WGSL vertex shader used 'meta' (attribute name) and 'ref' (local variable) -- BOTH ARE RESERVED WGSL KEYWORDS. Chrome's WGSL compiler rejected the shader module outright (confirmed via real browser console output: \"error: 'meta' is a reserved keyword\", \"error: 'ref' is a reserved keyword\"). WHY TR-044 DIDN'T CATCH IT: WebGPU shader-module creation does NOT throw synchronously on invalid WGSL -- the browser validates asynchronously and reports failures ONLY via an 'uncaptured error' console event, which materialReady's synchronous check cannot see and TR-044's test never listened for. Because Babylon submits a scene's draws in ONE command buffer, the single invalid shooting-star pipeline blanked the ENTIRE frame -- the star field went dark too, despite being completely correct and unrelated. LIVE DEBUGGING NEARLY REACHED THE WRONG CONCLUSION TOO: first diagnostic attempt sampled canvas pixels via drawImage()+getImageData() (the same method the WebGL2 pixel-proof E2E test uses) and got ZERO lit pixels BOTH BEFORE AND IMMEDIATELY AFTER the fix -- which would have wrongly suggested the fix failed. Actual cause: a WebGPU canvas cannot be reliably read back via drawImage() at all -- a limitation THIS PROJECT ALREADY DOCUMENTED in TR-036's own honest-limits section ('a WebGPU canvas cannot be read back via drawImage locally') and then forgot to apply here under live-debugging pressure. Switching to page.screenshot() (browser-compositor level, not a JS canvas API) resolved it immediately: pre-fix screenshot matched the owner's report exactly (flat background, badge showing 168,959 stars, nothing visible); post-fix screenshot showed the full correct photometric star field. FIX: renamed both reserved identifiers consistently across GLSL+WGSL+JS wiring (meta->shootMeta, ref->refAxis) even though GLSL doesn't reserve these words -- keeping names identical across both twins avoids exactly this class of future divergence. shoot.meta (the pure shooting-stars.ts module's own JS field name) was NOT renamed -- it's a plain object property, never the problem. VERIFICATION GAP CLOSED, not just the bug fixed: webgpu-hardware.spec.ts's real-hardware test now listens to the browser console throughout and asserts zero error/warning messages -- the check that actually would have caught TR-044's regression, since materialReady provably does not (it reported true on the broken build). VERIFIED: 140/140 unit (unaffected, shader-only fix), 61/61 E2E, astro check 0/0/0, eslint+prettier exit 0, real screenshots captured before and after confirming the fix visually not just numerically. PROCESS NOTE: second time this session a 'compiles/reports ready' signal was trusted past what it actually proves -- first was TR-040's Electron-pane renderFrames:0 (a false alarm the OTHER direction: code fine, environment misleading); this time code was genuinely broken and the readiness check was misleading. Standing lesson now encoded as a test, not just a one-off fix: for this project's WebGPU path, neither 'no exception thrown' nor 'materialReady:true' is sufficient evidence of correctness -- console output must be checked, and pixel evidence must come from page.screenshot(), not drawImage(). TR-044 left uncorrected in place with a pointer to TR-045, matching this plan's established TR-037-corrects-TR-036 / TR-039-corrects-TR-038 pattern."
  },
  {
    "project": "ishanjain-portfolio",
    "deliveryPlan": "PF-09-babylon-havok-realism",
    "phase": "B4 — Havok physics showcase",
    "detail": "Asteroid collisions, proximity slowdown + debris deflection, idle collisions, impact camera shake, docking contact",
    "status": "planned",
    "start": null,
    "end": null,
    "llmStrategy": null,
    "notes": "The portfolio centrepiece (owner-requested showcase). Havok lazy-loaded; body counts tier-gated; needs WASM SIMD (iOS ≥16.4) else visual-only."
  },
  {
    "project": "ishanjain-portfolio",
    "deliveryPlan": "PF-09-babylon-havok-realism",
    "phase": "B5 — Responsiveness & quality tiers",
    "detail": "Per-device tiers, WebGPU→WebGL2 fallback, touch parity, reduced-motion/no-WebGL paths",
    "status": "planned",
    "start": null,
    "end": null,
    "llmStrategy": null,
    "notes": "Every device class must meet its budget-table fps floor and startup target on real hardware."
  },
  {
    "project": "ishanjain-portfolio",
    "deliveryPlan": "PF-09-babylon-havok-realism",
    "phase": "B6 — Harden & rollout",
    "detail": "CI perf-budget gates, accessibility re-audit, ?engine cutover, archive WebGL1 engine",
    "status": "planned",
    "start": null,
    "end": null,
    "llmStrategy": null,
    "notes": "Babylon becomes default; old engine archived behind a flag for one release before removal."
  }
]
```
