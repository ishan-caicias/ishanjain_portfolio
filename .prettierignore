dist/
node_modules/
.astro/
.claude/
coverage/
pnpm-lock.yaml
package-lock.json

# Untracked reference material (PF-07 prototype source + raw dataset pipeline)
Interactive Outerspace Portfolio/
gaia_datasets/

# Untracked local-only working dirs: resources/ is reference material (prototype
# source, raw datasets, 3D model exports); .superpowers/ is tool scratch. Neither
# is tracked or shipped, so neither is ours to format.
resources/
.superpowers/

# Untracked local plugin state (ruflo/claude-flow harness), not ours to format or ship.
.claude-flow/

# Hand-curated data ported from the prototype source - see PF-07 delivery plan
# Phase 1. (space-engine.js left this list on 2026-07-17 — forked for ship-v2
# per ADR-0002 and formatted/linted like normal source.)
#
# "Byte-identical to their prototype source" was the original claim here; corrected
# 2026-07-29 — PF-11 D6.6b corrected Io/Europa's RA/Dec in celestial-catalog.js, so
# byte-identity is now a strong DEFAULT, not an invariant. Additive changes go through
# overlay modules; direct edits are for factual corrections only, named in a TR.
# CLAUDE.md non-negotiable #22 carries the full rule.
src/data/celestial/*.js

# Generated by scripts/gaia-asteroids-pngpack.mjs (PF-10 C3) — one real asteroid per line by
# design, so a diff after regeneration is readable. Reformatting it would only be undone by the
# next run. Never hand-edited (CLAUDE.md non-negotiable #22).
src/data/asteroids-dr3-physics.ts

# Rendered report snapshots (self-contained HTML artifacts; reformatting could
# alter whitespace inside pre-wrap blocks)
docs/checkpoint/*.html

# Vendored meshopt decoder (copied verbatim from the meshoptimizer package so
# Babylon can load it same-origin under the CSP — see babylon-engine.ts)
public/assets/craft/meshopt_decoder.js
